---
phase: 13-config-directory-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/carl-directories/full/.carl
  - tests/fixtures/carl-directories/minimal/.carl
autonomous: true
gap_closure: true
requirements: [CONFIG-04]

must_haves:
  truths:
    - "Unit tests pass when loading fixture directories"
    - "Fixture directories are named .opencarl instead of .carl"
  artifacts:
    - path: "tests/fixtures/carl-directories/full/.opencarl"
      provides: "Full test fixture directory"
      min_lines: 1
    - path: "tests/fixtures/carl-directories/minimal/.opencarl"
      provides: "Minimal test fixture directory"
      min_lines: 1
  key_links:
    - from: "tests/javascript/unit/loader.test.ts"
      to: "tests/fixtures/carl-directories/"
      via: "copyFixture function"
      pattern: "copyFixture"
---

<objective>
Rename test fixture directories from .carl to .opencarl to match updated test code expectations.

Purpose: Test code was updated to reference .opencarl paths but fixture source directories still use old .carl names, causing 9 unit tests to fail with ENOENT errors.
Output: Renamed fixture directories that match test expectations.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-config-directory-migration/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Rename fixture directories from .carl to .opencarl</name>
  <files>
    tests/fixtures/carl-directories/full/.carl
    tests/fixtures/carl-directories/minimal/.carl
  </files>
  <action>
    Use git mv to rename the fixture directories to preserve history:
    
    1. `git mv tests/fixtures/carl-directories/full/.carl tests/fixtures/carl-directories/full/.opencarl`
    2. `git mv tests/fixtures/carl-directories/minimal/.carl tests/fixtures/carl-directories/minimal/.opencarl`
    
    These directories contain manifest files and domain files that tests load via copyFixture(). The test code already expects .opencarl paths - this just aligns the fixture source with that expectation.
  </action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=loader.test.ts 2>&1 | grep -E "(PASS|FAIL|passed|failed)"</automated>
  </verify>
  <done>Both fixture directories renamed, loader.test.ts tests pass (9 tests that were failing now pass)</done>
</task>

</tasks>

<verification>
- Run: `npm run test:unit -- --testPathPattern=loader.test.ts`
- Verify: All tests pass (previously 9 were failing)
- Check: `ls tests/fixtures/carl-directories/full/.opencarl` exists
- Check: `ls tests/fixtures/carl-directories/minimal/.opencarl` exists
</verification>

<success_criteria>
- Fixture directories renamed from .carl to .opencarl
- Unit tests in loader.test.ts pass
- No .carl directories remain in tests/fixtures/carl-directories/
</success_criteria>

<output>
After completion, create `.planning/phases/13-config-directory-migration/13-05-SUMMARY.md`
</output>
