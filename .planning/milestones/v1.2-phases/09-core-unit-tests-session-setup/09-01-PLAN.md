---
phase: 09-core-unit-tests-session-setup
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/helpers/session-state-factory.ts
  - tests/helpers/setup-mocks.ts
autonomous: true
requirements: [UNIT-03, UNIT-07, UNIT-08]
must_haves:
  truths:
    - "Developer can create test session override states with partial overrides"
    - "Developer can mock fs module for setup tests"
    - "Helper factories follow Phase 8 patterns"
  artifacts:
    - path: tests/helpers/session-state-factory.ts
      provides: "Session override state creation"
      exports: ["createTestSessionOverrides", "createTestOverrideValue"]
    - path: tests/helpers/setup-mocks.ts
      provides: "Mocked fs module for setup tests"
      exports: ["mockFs", "createMockFs", "setupMockFs", "teardownMockFs"]
  key_links:
    - from: "tests/helpers/session-state-factory.ts"
      to: "src/carl/session-overrides.ts"
      via: "type imports"
      pattern: "SessionOverrides"
---

<objective>
Create test helper factories for session state and fs mocking to support Phase 9 tests.

Purpose: Provide reusable test utilities following Phase 8 factory patterns.
Output: Two new helper files in tests/helpers/
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/helpers/domain-factory.ts
@tests/helpers/match-factory.ts
@src/carl/session-overrides.ts
@src/carl/setup.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create session-state-factory.ts</name>
  <files>tests/helpers/session-state-factory.ts</files>
  <action>
Create session state factory following Phase 8 patterns (see tests/helpers/domain-factory.ts and match-factory.ts for reference).

Create two factory functions:
1. `createTestSessionOverrides(overrides?: Partial<SessionOverrides>): SessionOverrides`
   - Default: sessionId "test-session", empty domains, exists: false
   - Supports partial override pattern with spread operator

2. `createTestOverrideValue(value?: SessionOverrideValue): SessionOverrideValue`
   - Default: "inherit"
   - Returns the value as-is for explicit override testing

Import SessionOverrides and SessionOverrideValue types from src/carl/session-overrides.ts.
Use JSDoc comments explaining purpose. No dependencies on other test helpers.
  </action>
  <verify>
<automated>npx tsc --noEmit tests/helpers/session-state-factory.ts 2>&1 | grep -c "error" | xargs -I {} bash -c 'if [ {} -eq 0 ]; then echo "OK"; else echo "FAIL"; fi'</automated>
  </verify>
  <done>Factory exports createTestSessionOverrides and createTestOverrideValue with correct types</done>
</task>

<task type="auto">
  <name>task 2: Create setup-mocks.ts</name>
  <files>tests/helpers/setup-mocks.ts</files>
  <action>
Create fs mocking utilities for setup tests per user decision "use jest.mock('fs')".

Create these exports:
1. `createMockFs()` - Returns object with mocked fs methods (existsSync, mkdirSync, readdirSync, copyFileSync, accessSync, promises versions)
2. `setupMockFs(mockFs: MockFs)` - Sets up jest.mock('fs', () => mockFs)
3. `teardownMockFs()` - Clears mocks

Use jest.mock pattern:
```typescript
const mockFs = {
  existsSync: jest.fn(),
  mkdirSync: jest.fn(),
  readdirSync: jest.fn(),
  copyFileSync: jest.fn(),
  accessSync: jest.fn(),
  promises: {
    mkdir: jest.fn(),
    readdir: jest.fn(),
    copyFile: jest.fn(),
    access: jest.fn(),
  },
};
```

Include types for MockFs interface. Add JSDoc explaining "Mock entire fs module" decision.
  </action>
  <verify>
<automated>npx tsc --noEmit tests/helpers/setup-mocks.ts 2>&1 | grep -c "error" | xargs -I {} bash -c 'if [ {} -eq 0 ]; then echo "OK"; else echo "FAIL"; fi'</automated>
  </verify>
  <done>Mock utilities export createMockFs, setupMockFs, teardownMockFs with proper typing</done>
</task>

</tasks>

<verification>
- [ ] session-state-factory.ts exists and exports correct functions
- [ ] setup-mocks.ts exists and exports mock utilities
- [ ] TypeScript compiles without errors
- [ ] Follows Phase 8 factory patterns (partial override, sensible defaults)
</verification>

<success_criteria>
- Test helpers created in tests/helpers/
- Factory functions follow partial override pattern
- Mock utilities follow jest.mock('fs') pattern
- All exports properly typed
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-unit-tests-session-setup/09-01-SUMMARY.md`
</output>
