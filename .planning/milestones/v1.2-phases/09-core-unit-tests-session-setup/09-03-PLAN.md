---
phase: 09-core-unit-tests-session-setup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/javascript/unit/validate.test.ts
autonomous: true
requirements: [UNIT-09]
must_haves:
  truths:
    - "Developer can verify parseDomainRules extracts rules correctly"
    - "Developer can verify resolveDomainFile finds correct domain paths"
    - "Developer can verify CONTEXT domain bracket parsing works"
    - "Developer can verify malformed domain rules generate warnings"
  artifacts:
    - path: tests/javascript/unit/validate.test.ts
      provides: "Complete validation module tests"
      contains: "describe('parseDomainRules')"
      min_lines: 400
  key_links:
    - from: "tests/javascript/unit/validate.test.ts"
      to: "src/carl/validate.ts"
      via: "import parseDomainRules, resolveDomainFile"
      pattern: "parseDomainRules|resolveDomainFile"
---

<objective>
Extend validate.test.ts with parseDomainRules and resolveDomainFile tests.

Purpose: Complete UNIT-09 - Validation module tests for domain rules parsing.
Output: Extended tests/javascript/unit/validate.test.ts
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/carl/validate.ts
@tests/javascript/unit/validate.test.ts
@tests/helpers/manifest-factory.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Add parseDomainRules tests</name>
  <files>tests/javascript/unit/validate.test.ts</files>
  <action>
Extend existing validate.test.ts by adding new describe block for parseDomainRules.

Use temp directory pattern from existing tests (beforeEach/afterEach with fs.mkdtempSync).

Add describe('parseDomainRules') with nested blocks:
- describe('valid domain rules')
- describe('CONTEXT domain brackets')
- describe('COMMANDS domain rules')
- describe('malformed input')
- describe('edge cases')

Test cases for valid rules:
- should parse valid RULE_N entries in order
- should ignore comment lines starting with #
- should skip blank lines
- should trim whitespace from keys and values
- should warn on malformed lines without =
- should warn on invalid rule keys

Test cases for CONTEXT domain:
- should parse FRESH_RULES flag
- should parse MODERATE_RULE_N entries
- should populate bracketRules for CONTEXT domain
- should populate bracketFlags for CONTEXT domain

Import: Already has parseManifest, add parseDomainRules, resolveDomainFile.
Create domain files inline using fs.writeFileSync in tempDir.

Per user decision: "Hybrid approach - Use test fixtures for valid cases, inline mock data for edge cases"
  </action>
  <verify>
<automated>npx jest tests/javascript/unit/validate.test.ts --testNamePattern="parseDomainRules" 2>&1 | tail -5</automated>
  </verify>
  <done>parseDomainRules tests cover valid, CONTEXT, malformed, and edge cases</done>
</task>

<task type="auto">
  <name>task 2: Add resolveDomainFile tests</name>
  <files>tests/javascript/unit/validate.test.ts</files>
  <action>
Add describe('resolveDomainFile') block with tests:

Test cases:
- should return path when domain file exists with lowercase name
- should return null when domain file does not exist
- should warn when domain file has wrong case (UPPERCASE instead of lowercase)
- should warn when .carl directory does not exist
- should handle multiple domain lookups

Create test .carl/ directories with domain files for testing:
```typescript
const carlDir = path.join(tempDir, '.carl');
fs.mkdirSync(carlDir);
fs.writeFileSync(path.join(carlDir, 'development'), 'DEVELOPMENT_RULE_1=test');
```

Test that warnings array is populated correctly for missing/mismatched files.
Verify domain name normalization (uppercase in manifest → lowercase file lookup).
  </action>
  <verify>
<automated>npx jest tests/javascript/unit/validate.test.ts --testNamePattern="resolveDomainFile" 2>&1 | tail -5</automated>
  </verify>
  <done>resolveDomainFile tests cover found, missing, and case-mismatch scenarios</done>
</task>

<task type="auto">
  <name>task 3: Run full validation test suite</name>
  <files>tests/javascript/unit/validate.test.ts</files>
  <action>
Run complete test suite to verify all tests pass together.

Verify coverage of validate.ts:
- parseManifest: already tested (existing tests)
- parseDomainRules: new tests
- resolveDomainFile: new tests

Check that coverage meets 80% threshold:
```bash
npx jest tests/javascript/unit/validate.test.ts --coverage --coverageReporters=text
```

Per user decision: "Representative error coverage - test 2-3 variations per error type"
  </action>
  <verify>
<automated>npx jest tests/javascript/unit/validate.test.ts --coverage --coverageReporters=text 2>&1 | grep -E "validate.ts\s+\d+"</automated>
  </verify>
  <done>All validation tests pass with >80% coverage of validate.ts</done>
</task>

</tasks>

<verification>
- [ ] parseDomainRules tests added with BDD structure
- [ ] CONTEXT bracket parsing tested
- [ ] resolveDomainFile tests added
- [ ] Case-sensitivity warnings tested
- [ ] All tests pass
- [ ] Coverage >= 80%
</verification>

<success_criteria>
- Extended validate.test.ts with parseDomainRules and resolveDomainFile tests
- All tests pass
- Coverage of validate.ts >= 80%
- Follows existing test patterns in file
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-unit-tests-session-setup/09-03-SUMMARY.md`
</output>
