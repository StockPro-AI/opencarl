---
phase: 09-core-unit-tests-session-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/javascript/unit/command-parity.test.ts
autonomous: true
requirements: [UNIT-06]
must_haves:
  truths:
    - "Developer can verify *carl star command is detected and parsed"
    - "Developer can verify *carl docs subcommand triggers docs guidance"
    - "Developer can verify custom star commands resolve to command payloads"
    - "Developer can verify unresolved tokens are tracked"
  artifacts:
    - path: tests/javascript/unit/command-parity.test.ts
      provides: "Command parity unit tests"
      contains: "describe('command-parity.ts')"
      min_lines: 200
  key_links:
    - from: "tests/javascript/unit/command-parity.test.ts"
      to: "src/carl/command-parity.ts"
      via: "import resolveCarlCommandSignals"
      pattern: "resolveCarlCommandSignals"
---

<objective>
Create comprehensive unit tests for star-command parsing and resolution.

Purpose: Validate UNIT-06 - Star-command parser handles *carl, *carl docs, custom commands, edge cases.
Output: tests/javascript/unit/command-parity.test.ts
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/carl/command-parity.ts
@tests/javascript/unit/matcher.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create command-parity.test.ts with star command detection tests</name>
  <files>tests/javascript/unit/command-parity.test.ts</files>
  <action>
Create unit tests for command-parity.ts following Phase 8 BDD patterns (see matcher.test.ts for structure).

Use nested describe blocks:
- describe('command-parity.ts')
  - describe('resolveCarlCommandSignals')
    - describe('star command detection')
    - describe('*carl docs handling')
    - describe('custom commands')
    - describe('edge cases')

Test cases for star command detection:
- should detect *carl in prompt text
- should detect multiple star commands (*carl *brief *dev)
- should extract command token from *carl
- should return empty array for prompt without star commands
- should handle empty/undefined prompt text
- should normalize tokens to uppercase

Test cases for *carl docs:
- should include docs guidance when 'docs' in prompt with *carl
- should NOT include docs guidance when *carl without 'docs'
- should include help guidance when provided via helpGuidance option

Test cases for command resolution:
- should resolve CARL command to command payload
- should return commandPayloads with correct domain
- should track unresolved tokens for unknown commands
- should respect commandOverrides array
- should deduplicate tokens (uniqInOrder)

Import: `import { resolveCarlCommandSignals, type CarlCommandResolutionInput } from '../../../src/carl/command-parity';`

Do NOT test parseCommandRules directly (it's internal). Test via resolveCarlCommandSignals with commandFilePath override.
  </action>
  <verify>
<automated>npx jest tests/javascript/unit/command-parity.test.ts --passWithNoTests 2>&1 | tail -1</automated>
  </verify>
  <done>All star command detection tests pass with meaningful assertions</done>
</task>

<task type="auto">
  <name>task 2: Add edge case and error handling tests</name>
  <files>tests/javascript/unit/command-parity.test.ts</files>
  <action>
Add additional test cases within the same file under describe('edge cases'):

Edge cases to test:
- should handle undefined commandsPayload gracefully
- should handle commandsPayload with state: false
- should handle missing commandFilePath
- should handle empty commandTokens array
- should preserve original case in commandTokens (normalized internally)
- should handle special characters in prompt (*carl-test, *carl_123)
- should handle very long prompts with many star commands
- should handle duplicate star commands (only once in result)

Per user decision: Test 2-3 variations per error type (representative coverage).

Verify commandDomains matches resolved commands. Verify commandPayloads has correct structure.
  </action>
  <verify>
<automated>npx jest tests/javascript/unit/command-parity.test.ts --coverage --coverageReporters=text 2>&1 | grep -E "command-parity.ts\s+\d+"</automated>
  </verify>
  <done>Edge case tests pass with >80% coverage of command-parity.ts</done>
</task>

</tasks>

<verification>
- [ ] command-parity.test.ts exists with BDD structure
- [ ] Star command detection tests cover happy path
- [ ] *carl docs handling tested
- [ ] Custom command resolution tested
- [ ] Edge cases covered (2-3 variations per category)
- [ ] Tests pass with >80% coverage
</verification>

<success_criteria>
- All tests pass
- Coverage of command-parity.ts >= 80%
- BDD-style nested describes
- Tests verify observable behavior
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-unit-tests-session-setup/09-02-SUMMARY.md`
</output>
