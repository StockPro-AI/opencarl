---
phase: 09-core-unit-tests-session-setup
plan: 04
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - tests/javascript/unit/session-overrides.test.ts
autonomous: true
requirements: [UNIT-03, UNIT-07]
must_haves:
  truths:
    - "Developer can verify loadSessionOverrides reads session files correctly"
    - "Developer can verify applySessionOverrides filters domains by override values"
    - "Developer can verify domain toggle works with true/false/inherit values"
    - "Developer can verify session state persists across operations"
  artifacts:
    - path: tests/javascript/unit/session-overrides.test.ts
      provides: "Session override unit tests"
      contains: "describe('session-overrides.ts')"
      min_lines: 250
  key_links:
    - from: "tests/javascript/unit/session-overrides.test.ts"
      to: "src/carl/session-overrides.ts"
      via: "import all functions"
      pattern: "loadSessionOverrides|applySessionOverrides"
    - from: "tests/javascript/unit/session-overrides.test.ts"
      to: "tests/helpers/session-state-factory.ts"
      via: "import factory"
      pattern: "createTestSessionOverrides"
---

<objective>
Create comprehensive unit tests for session override loading and domain toggling.

Purpose: Validate UNIT-03 (domain toggle) and UNIT-07 (session manager - load, apply).
Output: tests/javascript/unit/session-overrides.test.ts
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/carl/session-overrides.ts
@tests/helpers/session-state-factory.ts
@tests/javascript/unit/matcher.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create session-overrides.test.ts with load tests</name>
  <files>tests/javascript/unit/session-overrides.test.ts</files>
  <action>
Create unit tests for session-overrides.ts following Phase 8 BDD patterns.

Use temp directory with mocked session files. Import factory from Wave 0:
```typescript
import { createTestSessionOverrides } from '../../helpers/session-state-factory';
```

Structure:
- describe('session-overrides.ts')
  - describe('loadSessionOverrides')
    - describe('file loading')
    - describe('override value parsing')
  - describe('applySessionOverrides')
  - describe('helper functions')
  - describe('edge cases')

Test loadSessionOverrides:
- should load session overrides from .carl/sessions/{id}.json
- should return default (empty domains) when file does not exist
- should return default when sessionId is empty
- should parse domains with true/false/inherit values
- should normalize domain names to uppercase
- should handle invalid JSON gracefully (console.warn, return empty)
- should set exists: true when file exists

Create session files inline:
```typescript
const sessionsDir = path.join(carlDir, 'sessions');
fs.mkdirSync(sessionsDir, { recursive: true });
fs.writeFileSync(path.join(sessionsDir, 'test-session.json'), JSON.stringify({ domains: { DEVELOPMENT: 'false' } }));
```

Per user decision: "Fresh instance per test" - create new test data in each test.
  </action>
  <verify>
<automated>npx jest tests/javascript/unit/session-overrides.test.ts --testNamePattern="loadSessionOverrides" 2>&1 | tail -3</automated>
  </verify>
  <done>loadSessionOverrides tests cover file loading and value parsing</done>
</task>

<task type="auto">
  <name>task 2: Add applySessionOverrides and helper tests</name>
  <files>tests/javascript/unit/session-overrides.test.ts</files>
  <action>
Add tests for applySessionOverrides and helper functions:

Test applySessionOverrides:
- should filter out domains with override: false
- should keep domains with override: true (if in original list)
- should keep domains with override: inherit (no change)
- should return sorted domain list
- should handle empty overrides (return original list)

Test isDomainDisabledBySession:
- should return true when domain has override: false
- should return false when domain has override: true or inherit
- should normalize domain name to uppercase

Test getDomainOverride:
- should return override value for domain
- should return 'inherit' for unknown domains
- should normalize domain name lookup

Use createTestSessionOverrides factory for test data:
```typescript
const overrides = createTestSessionOverrides({
  domains: { DEVELOPMENT: 'false', CONTENT: 'true' },
  exists: true
});
```
  </action>
  <verify>
<automated>npx jest tests/javascript/unit/session-overrides.test.ts --testNamePattern="apply" 2>&1 | tail -3</automated>
  </verify>
  <done>applySessionOverrides and helper function tests pass</done>
</task>

<task type="auto">
  <name>task 3: Add edge cases and verify coverage</name>
  <files>tests/javascript/unit/session-overrides.test.ts</files>
  <action>
Add edge case tests under describe('edge cases'):

Edge cases:
- should handle sessions directory does not exist
- should handle .carl directory does not exist
- should handle empty domains object in session file
- should handle extra fields in session file (ignore them)
- should handle domains with various truthy values (yes, 1, true)
- should handle domains with various falsy values (no, 0, false)
- should handle concurrent lookups (different session IDs)

Run coverage check:
```bash
npx jest tests/javascript/unit/session-overrides.test.ts --coverage --coverageReporters=text
```

Per user decision: "Verify final state only - test behavior, not implementation"
  </action>
  <verify>
<automated>npx jest tests/javascript/unit/session-overrides.test.ts --coverage --coverageReporters=text 2>&1 | grep -E "session-overrides.ts\s+\d+"</automated>
  </verify>
  <done>Edge cases covered, all tests pass with >80% coverage</done>
</task>

</tasks>

<verification>
- [ ] session-overrides.test.ts exists with BDD structure
- [ ] loadSessionOverrides tested (file, missing, invalid JSON)
- [ ] applySessionOverrides tested (true/false/inherit)
- [ ] Helper functions tested
- [ ] Uses session-state-factory from Wave 0
- [ ] Coverage >= 80%
</verification>

<success_criteria>
- All tests pass
- Coverage of session-overrides.ts >= 80%
- Uses factory from Wave 0
- Fresh instance per test pattern
</success_criteria>

<output>
After completion, create `.planning/phases/09-core-unit-tests-session-setup/09-04-SUMMARY.md`
</output>
