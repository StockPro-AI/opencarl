---
phase: 09.1-close-phase-9-unit-test-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/javascript/unit/loader.test.ts
  - tests/javascript/unit/rule-cache.test.ts
  - tests/javascript/unit/signal-store.test.ts
autonomous: true
requirements: [UNIT-03, UNIT-07]
must_haves:
  truths:
    - "Domain manager tests cover load/list/override behaviors for .carl/ rule discovery"
    - "Session manager tests cover create/get/update flows and bounded session history"
  artifacts:
    - path: "tests/javascript/unit/loader.test.ts"
      provides: "Domain manager rule discovery tests"
      contains: "describe('loader.ts')"
      min_lines: 220
    - path: "tests/javascript/unit/rule-cache.test.ts"
      provides: "Rule cache session management tests"
      contains: "describe('rule-cache.ts')"
      min_lines: 160
    - path: "tests/javascript/unit/signal-store.test.ts"
      provides: "Session signal tracking tests"
      contains: "describe('signal-store.ts')"
      min_lines: 180
  key_links:
    - from: "tests/javascript/unit/loader.test.ts"
      to: "src/carl/loader.ts"
      via: "import loadCarlRules"
      pattern: "loadCarlRules"
    - from: "tests/javascript/unit/rule-cache.test.ts"
      to: "src/carl/rule-cache.ts"
      via: "import cache helpers"
      pattern: "getCachedRules|markRulesDirty|resetRulesCache"
    - from: "tests/javascript/unit/signal-store.test.ts"
      to: "src/carl/signal-store.ts"
      via: "import record/get helpers"
      pattern: "recordPromptSignals|getSessionSignals"
---

<objective>
Add missing domain manager and session manager unit tests that close Phase 9 gaps.

Purpose: Satisfy UNIT-03 and UNIT-07 with concrete tests for loader/rule-cache/signal-store behavior.
Output: Three new unit test files aligned to source modules.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/fixtures/README.md

@src/carl/loader.ts
@src/carl/rule-cache.ts
@src/carl/signal-store.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create loader.test.ts for domain discovery behaviors</name>
  <files>tests/javascript/unit/loader.test.ts</files>
  <action>
Create unit tests for loadCarlRules in src/carl/loader.ts using the existing fixtures under tests/fixtures/carl-directories.

Structure (BDD-style):
- describe('loader.ts')
  - describe('loadCarlRules')
    - describe('project/global/fallback resolution')
    - describe('domain payloads and sources')
    - describe('project opt-in + session overrides')

Implementation guidance:
- Use fs.mkdtempSync + fs.cpSync to copy fixture directories into a temp workspace per test (fresh instance per test).
- Use the options.overrides object to point to project/global/fallback .carl/ directories (avoid mocking findProjectCarl).
- Use both "minimal" and "full" fixture sets to cover multi-domain cases.

Test cases (balanced coverage, no skipped tests):
- loads active domains from project manifest and returns sorted domains list.
- prefers project domain payloads over global when names overlap (project override).
- honors projectOptIn=false by ignoring project rules even when project .carl/ exists.
- falls back to fallback .carl/ only when both project and global are missing.
- applies session overrides to filter disabled domains and updates sources accordingly.
- CONTEXT payload includes bracketFlags/bracketRules when CONTEXT domain is present.

Ensure test file name matches source module (loader.test.ts) and uses module → function describe structure.
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/loader.test.ts</automated>
  </verify>
  <done>loader.test.ts covers domain load/list/override behaviors with passing tests</done>
</task>

<task type="auto">
  <name>task 2: Create rule-cache.test.ts for session cache behaviors</name>
  <files>tests/javascript/unit/rule-cache.test.ts</files>
  <action>
Create unit tests for src/carl/rule-cache.ts with module-level cache isolation.

Structure:
- describe('rule-cache.ts')
  - describe('cache state + dirty flag')
  - describe('session opt-in + warning guard')
  - describe('getCachedRules reload conditions')

Implementation guidance:
- Use jest.isolateModules or jest.resetModules in beforeEach to reset module-level cache state per test.
- jest.mock('../../../src/carl/loader', () => ({ loadCarlRules: jest.fn() })) so tests can assert reload behavior without file IO.

Test cases:
- markRulesDirty toggles isCacheDirty and triggers reload on next getCachedRules.
- getCachedRules caches results and does not reload if options are unchanged and cache is not dirty.
- getCachedRules reloads when sessionId or opt-in flag changes (cacheOptionsDiffer path).
- resetRulesCache clears cache + dirty flag.
- setSessionProjectOptIn / getSessionProjectOptIn default to true and update per session.
- hasSessionWarned / markSessionWarned / clearSessionWarning behave as session-scoped guard.

No skipped or TODO tests.
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/rule-cache.test.ts</automated>
  </verify>
  <done>rule-cache.test.ts validates cache reload logic and session guards</done>
</task>

<task type="auto">
  <name>task 3: Create signal-store.test.ts for session signal tracking</name>
  <files>tests/javascript/unit/signal-store.test.ts</files>
  <action>
Create unit tests for src/carl/signal-store.ts, ensuring bounded history and token normalization.

Structure:
- describe('signal-store.ts')
  - describe('recordPromptSignals / getSessionPromptText')
  - describe('recordToolSignals / getSessionSignals')
  - describe('recordCommandSignals / consumeCommandSignals')
  - describe('bounded history')

Implementation guidance:
- Use jest.resetModules or jest.isolateModules to reset the internal sessionSignals Map per test.
- Build toolArgs with nested objects and path-like values to exercise path token extraction.

Test cases:
- recordPromptSignals stores last prompt and returns normalized tokens in getSessionSignals.
- recordToolSignals collects tool tokens and path tokens from nested args containing paths.
- recordCommandSignals + consumeCommandSignals returns the most recent entry and empties it.
- Empty sessionId returns empty defaults without throwing.
- Bounded history keeps only the last 20 entries (MAX_SIGNAL_ENTRIES).

Keep BDD-style describes and avoid skipped tests.
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/signal-store.test.ts</automated>
  </verify>
  <done>signal-store.test.ts verifies session signal create/get/update behaviors</done>
</task>

</tasks>

<verification>
- [ ] loader.test.ts exists and exercises load/list/override paths
- [ ] rule-cache.test.ts covers cache reload and session guard behaviors
- [ ] signal-store.test.ts covers prompt/tool/command signal tracking
- [ ] No skipped or TODO tests in new files
</verification>

<success_criteria>
- All three test files exist with BDD structure
- Each suite passes via its dedicated jest command
- UNIT-03 and UNIT-07 coverage gaps closed for loader/rule-cache/signal-store
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-close-phase-9-unit-test-gaps/09.1-01-SUMMARY.md`
</output>
