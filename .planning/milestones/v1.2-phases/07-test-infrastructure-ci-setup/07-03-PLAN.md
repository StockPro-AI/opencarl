---
phase: 07-test-infrastructure-ci-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .github/workflows/test.yml
  - README.md
autonomous: false
requirements:
  - CI-01
  - CI-02
  - CI-04

must_haves:
  truths:
    - "GitHub Actions runs test suite automatically on every PR"
    - "GitHub Actions runs test suite on push to main branch"
    - "Draft PRs do not trigger CI workflow"
    - "Coverage report is downloadable as GitHub Actions artifact"
    - "Coverage badge is visible in README.md"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "CI workflow configuration"
      contains: "pull_request"
    - path: "README.md"
      provides: "Coverage badge"
      contains: "coverage"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "npm test"
      via: "workflow step"
      pattern: "npm run test:coverage"
    - from: "workflow"
      to: "coverage/"
      via: "artifact upload"
      pattern: "actions/upload-artifact"
    - from: "README.md"
      to: "codecov"
      via: "badge URL"
      pattern: "codecov"
---

<objective>
Create GitHub Actions workflow for automated testing and add coverage badge to README.

Purpose: Automate test execution on every code change and provide visibility into coverage status.
Output: Working CI pipeline and live coverage badge.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@jest.config.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create GitHub Actions workflow</name>
  <files>.github/workflows/test.yml</files>
  <action>
Create `.github/workflows/test.yml` with the following configuration:

```yaml
name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    # Skip draft PRs (per user decision)
    types: [opened, synchronize, reopened, ready_for_review]

# Only run on non-draft PRs
concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    # Skip draft PRs
    if: github.event.pull_request.draft == false
    
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests with coverage
        run: npm run test:coverage
        env:
          CI: true
      
      - name: Upload coverage report (HTML)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage/lcov-report/
          retention-days: 30
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        if: always()
        with:
          files: ./coverage/lcov.info
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
```

Key configuration decisions (per user decision):
- **Triggers:** push to main + all PRs (CI-02)
- **Draft skip:** Only runs on non-draft PRs via `if: github.event.pull_request.draft == false`
- **Coverage artifact:** Uploads HTML report as downloadable artifact (CI-04)
- **Codecov:** Uploads lcov for coverage tracking and trending
- **Concurrency:** Cancels in-progress runs on same branch

Note: CODECOV_TOKEN secret should be added in repository settings (user action required).
  </action>
  <verify>
    <automated>test -f .github/workflows/test.yml && grep -q "pull_request" .github/workflows/test.yml && grep -q "upload-artifact" .github/workflows/test.yml && echo "OK: workflow file exists" || (echo "FAIL: workflow incomplete"; exit 1)</automated>
  </verify>
  <done>.github/workflows/test.yml exists with correct triggers, artifact upload, and Codecov integration</done>
</task>

<task type="auto">
  <name>task 2: Add coverage badge to README</name>
  <files>README.md</files>
  <action>
Add a coverage badge to the top of README.md (after the title line).

First, read the current README.md to find the title line, then add the badge on the line immediately after.

The badge should use Codecov's badge URL format:
```markdown
[![Coverage](https://codecov.io/gh/KrisGray/opencarl/branch/main/graph/badge.svg)](https://codecov.io/gh/KrisGray/opencarl)
```

Badge placement:
- Insert immediately after the main title (`# OpenCARL` or similar)
- Before any other content
- On its own line

If README.md has multiple badges already, add this one as the first badge in the row.

The badge will show:
- Live coverage percentage from Codecov
- Link to detailed coverage report on Codecov

Note: Badge will show "unknown" until first CI run uploads coverage to Codecov.
  </action>
  <verify>
    <automated>grep -q "codecov.io" README.md && echo "OK: coverage badge present" || (echo "FAIL: badge not found"; exit 1)</automated>
  </verify>
  <done>README.md contains coverage badge near the top, linking to Codecov</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify CI workflow runs successfully</name>
  <what-built>Complete CI workflow with coverage reporting</what-built>
  <how-to-verify>
**Verify GitHub Actions workflow:**

1. Commit and push these changes to trigger CI:
   ```bash
   git add .github/workflows/test.yml README.md
   git commit -m "ci: add test workflow and coverage badge"
   git push
   ```

2. Check GitHub Actions runs:
   - Go to https://github.com/KrisGray/opencarl/actions
   - Verify "Test" workflow appears and runs
   - Confirm test step passes (placeholder test from Plan 01)

3. Verify coverage artifact:
   - Click on the completed workflow run
   - Scroll to "Artifacts" section
   - Confirm "coverage-report" artifact is downloadable

4. Check README badge:
   - View README.md on GitHub
   - Confirm coverage badge is visible (may show "unknown" until Codecov processes)

**Note:** For Codecov to work, you'll need to:
1. Add `CODECOV_TOKEN` secret in GitHub repo settings (Settings → Secrets → Actions)
2. Get token from https://codecov.io/gh/KrisGray/opencarl after first upload
  </how-to-verify>
  <resume-signal>Type "approved" if CI runs successfully and artifacts are downloadable, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. GitHub Actions workflow triggers on push to main and PRs
2. Workflow runs tests with coverage
3. Coverage report uploaded as artifact
4. Coverage badge visible in README
</verification>

<success_criteria>
- [ ] .github/workflows/test.yml exists with correct triggers
- [ ] Draft PRs are skipped
- [ ] Coverage artifact upload configured
- [ ] Codecov integration configured
- [ ] README.md contains coverage badge
- [ ] CI workflow runs successfully on push
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-infrastructure-ci-setup/07-03-SUMMARY.md`
</output>
