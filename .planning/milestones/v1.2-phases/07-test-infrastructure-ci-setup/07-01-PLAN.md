---
phase: 07-test-infrastructure-ci-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - jest.config.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-03

must_haves:
  truths:
    - "Developer can run `npm test` and see Jest execute tests with ts-jest"
    - "Developer can run `npm run test:coverage` and see coverage percentages"
    - "Coverage threshold enforcement fails build when below 80%"
  artifacts:
    - path: "jest.config.ts"
      provides: "Jest configuration with ts-jest, coverage thresholds, reporters"
      contains: "preset: 'ts-jest'"
    - path: "package.json"
      provides: "Test scripts for running tests"
      contains: "test"
  key_links:
    - from: "npm test"
      to: "jest.config.ts"
      via: "jest CLI"
      pattern: "npm run test"
    - from: "jest.config.ts"
      to: "src/**/*.ts"
      via: "coverage collection"
      pattern: "collectCoverageFrom"
---

<objective>
Install and configure Jest with ts-jest for TypeScript testing, add coverage thresholds at 80%, and create all test scripts for local development.

Purpose: Establish the test framework foundation that all subsequent test phases will build upon.
Output: Working Jest configuration with coverage enforcement and npm test scripts.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>task 1: Install Jest dependencies</name>
  <files>package.json</files>
  <action>
Install Jest testing dependencies for TypeScript project:

```bash
npm install --save-dev jest ts-jest @types/jest
```

This installs:
- `jest` - Test runner
- `ts-jest` - TypeScript preprocessor for Jest
- `@types/jest` - TypeScript type definitions

Verify installation by checking package.json devDependencies contains jest, ts-jest, and @types/jest.
  </action>
  <verify>
    <automated>npm ls jest ts-jest @types/jest 2>&1 | grep -E "(jest|ts-jest|@types/jest)" | wc -l | xargs -I {} sh -c 'test {} -ge 3 && echo "OK" || (echo "FAIL: missing dependencies"; exit 1)'</automated>
  </verify>
  <done>package.json contains jest, ts-jest, and @types/jest in devDependencies</done>
</task>

<task type="auto">
  <name>task 2: Create Jest configuration</name>
  <files>jest.config.ts</files>
  <action>
Create `jest.config.ts` in the project root with the following configuration:

```typescript
import type { Config } from 'jest';

const config: Config = {
  // Use ts-jest preset for TypeScript support
  preset: 'ts-jest',
  
  // Node test environment (no browser)
  testEnvironment: 'node',
  
  // Parallel execution with 2 workers (per user decision)
  maxWorkers: 2,
  
  // Test file patterns - standard Jest patterns
  testMatch: [
    '**/tests/javascript/**/*.test.ts',
    '**/tests/javascript/**/*.spec.ts'
  ],
  
  // Coverage configuration
  collectCoverageFrom: [
    'src/**/*.ts',
    '!src/**/*.d.ts',
    '!src/**/index.ts'
  ],
  coverageDirectory: 'coverage',
  coverageReporters: ['text', 'lcov', 'html'],
  
  // Global coverage thresholds (80% per user decision)
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 80,
      lines: 80,
      statements: 80
    }
  },
  
  // Module file extensions
  moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json'],
  
  // Root directory
  rootDir: '.',
  
  // Test timeout (30 seconds default)
  testTimeout: 30000,
  
  // Verbose output
  verbose: true
};

export default config;
```

Key configuration decisions:
- `preset: 'ts-jest'` - TypeScript support (TEST-01)
- `maxWorkers: 2` - Per user decision for parallel execution
- `coverageThreshold.global.*: 80` - 80% threshold enforcement (TEST-03)
- `coverageReporters: ['text', 'lcov', 'html']` - Console + lcov + HTML reports per user decision
- `testMatch` - Uses tests/javascript/ directory structure per user decision
  </action>
  <verify>
    <automated>test -f jest.config.ts && grep -q "preset: 'ts-jest'" jest.config.ts && grep -q "branches: 80" jest.config.ts && echo "OK" || (echo "FAIL: jest.config.ts missing required fields"; exit 1)</automated>
  </verify>
  <done>jest.config.ts exists with ts-jest preset, 80% coverage thresholds, and correct reporters</done>
</task>

<task type="auto">
  <name>task 3: Add test scripts to package.json</name>
  <files>package.json</files>
  <action>
Add the following scripts to package.json under the "scripts" section:

```json
{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:unit": "jest --testPathPattern='tests/javascript/unit'",
    "test:integration": "jest --testPathPattern='tests/javascript/integration'",
    "test:e2e": "jest --testPathPattern='tests/javascript/e2e'"
  }
}
```

Scripts added (per user decision):
- `npm test` - Run all tests once
- `npm run test:watch` - Watch mode for development
- `npm run test:coverage` - Generate coverage report
- `npm run test:unit` - Run unit tests only
- `npm run test:integration` - Run integration tests only
- `npm run test:e2e` - Run e2e tests only

Preserve existing scripts (build, prepublishOnly, publish). The test:coverage script will enforce the 80% threshold defined in jest.config.ts.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); const scripts = ['test', 'test:watch', 'test:coverage', 'test:unit', 'test:integration', 'test:e2e']; const missing = scripts.filter(s => !pkg.scripts[s]); if (missing.length) { console.error('FAIL: missing scripts:', missing.join(', ')); process.exit(1); } console.log('OK: all test scripts present');"</automated>
  </verify>
  <done>package.json contains all 6 test scripts (test, test:watch, test:coverage, test:unit, test:integration, test:e2e)</done>
</task>

<task type="auto">
  <name>task 4: Create placeholder test for verification</name>
  <files>tests/javascript/unit/placeholder.test.ts</files>
  <action>
Create a minimal placeholder test to verify Jest configuration works:

1. Create directory structure: `tests/javascript/unit/`
2. Create `tests/javascript/unit/placeholder.test.ts`:

```typescript
/**
 * Placeholder test to verify Jest configuration.
 * Remove once real tests are added in Phase 8+.
 */
describe('Jest Configuration', () => {
  it('should run tests correctly', () => {
    expect(true).toBe(true);
  });
});
```

This test serves as a smoke test to confirm:
- Jest runs correctly
- ts-jest processes TypeScript
- Test discovery finds files in tests/javascript/
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "passed" && echo "OK: Jest runs successfully" || (echo "FAIL: npm test did not pass"; exit 1)</automated>
  </verify>
  <done>`npm test` executes successfully and shows 1 test passed</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm test` runs Jest and executes the placeholder test
2. `npm run test:coverage` generates coverage report in coverage/ directory
3. Jest configuration enforces 80% threshold (will fail until real code coverage exists)
</verification>

<success_criteria>
- [ ] jest, ts-jest, @types/jest installed in devDependencies
- [ ] jest.config.ts exists with ts-jest preset and 80% coverage thresholds
- [ ] All 6 test scripts added to package.json
- [ ] `npm test` executes successfully with 1 passing test
- [ ] Coverage reporters configured: text, lcov, html
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-infrastructure-ci-setup/07-01-SUMMARY.md`
</output>
