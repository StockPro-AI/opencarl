---
phase: 07-test-infrastructure-ci-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/javascript/unit/.gitkeep
  - tests/javascript/integration/.gitkeep
  - tests/javascript/e2e/.gitkeep
  - tests/fixtures/manifests/.gitkeep
  - tests/fixtures/carl-directories/.gitkeep
  - tests/fixtures/prompts/.gitkeep
  - tests/fixtures/sessions/.gitkeep
  - tests/fixtures/README.md
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "Test files can be placed in tests/javascript/unit/, tests/javascript/integration/, tests/javascript/e2e/"
    - "Test fixtures can be placed in tests/fixtures/ subdirectories"
    - "Each fixture directory has a clear purpose documented"
  artifacts:
    - path: "tests/javascript/unit/"
      provides: "Unit test directory"
      exists: true
    - path: "tests/javascript/integration/"
      provides: "Integration test directory"
      exists: true
    - path: "tests/javascript/e2e/"
      provides: "E2E test directory"
      exists: true
    - path: "tests/fixtures/manifests/"
      provides: "Sample manifest files for testing"
      exists: true
    - path: "tests/fixtures/carl-directories/"
      provides: "Sample .carl/ directory structures"
      exists: true
    - path: "tests/fixtures/prompts/"
      provides: "Sample prompt text for keyword matching tests"
      exists: true
    - path: "tests/fixtures/sessions/"
      provides: "Sample session state files"
      exists: true
    - path: "tests/fixtures/README.md"
      provides: "Documentation for fixture usage"
      contains: "fixtures"
  key_links:
    - from: "tests/javascript/"
      to: "src/"
      via: "mirrored structure"
      pattern: "tests/javascript/**/.*.test.ts"
    - from: "test files"
      to: "tests/fixtures/"
      via: "import fixtures"
      pattern: "import.*fixtures"
---

<objective>
Create the test directory structure that mirrors src/ and establish fixture directories for test data.

Purpose: Provide organized locations for unit, integration, and e2e tests plus reusable test fixtures.
Output: Complete test directory structure with .gitkeep files and fixture documentation.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>task 1: Create test directory structure</name>
  <files>
    - tests/javascript/unit/.gitkeep
    - tests/javascript/integration/.gitkeep
    - tests/javascript/e2e/.gitkeep
  </files>
  <action>
Create the test directory structure that mirrors src/ organization:

1. Create directories:
   - `tests/javascript/unit/` - For isolated unit tests
   - `tests/javascript/integration/` - For integration tests
   - `tests/javascript/e2e/` - For end-to-end tests

2. Add .gitkeep files to preserve empty directories in git:
   - `tests/javascript/unit/.gitkeep`
   - `tests/javascript/integration/.gitkeep`
   - `tests/javascript/e2e/.gitkeep`

Each .gitkeep file should be empty (just ensures directory is tracked by git).

Structure rationale (per user decision):
- tests/javascript/ mirrors src/ structure
- Subdirectories separate test types for targeted runs
- Matches test:unit, test:integration, test:e2e scripts in package.json
  </action>
  <verify>
    <automated>for dir in tests/javascript/unit tests/javascript/integration tests/javascript/e2e; do test -d "$dir" || (echo "FAIL: $dir missing"; exit 1); done && echo "OK: all test directories exist"</automated>
  </verify>
  <done>All three test directories exist with .gitkeep files</done>
</task>

<task type="auto">
  <name>task 2: Create fixtures directory structure</name>
  <files>
    - tests/fixtures/manifests/.gitkeep
    - tests/fixtures/carl-directories/.gitkeep
    - tests/fixtures/prompts/.gitkeep
    - tests/fixtures/sessions/.gitkeep
  </files>
  <action>
Create the fixtures directory structure for test data:

1. Create directories:
   - `tests/fixtures/manifests/` - Sample manifest files (.carl/manifest)
   - `tests/fixtures/carl-directories/` - Complete .carl/ directory structures
   - `tests/fixtures/prompts/` - Sample prompt text for keyword matching
   - `tests/fixtures/sessions/` - Sample session state files (.carl/sessions/*.json)

2. Add .gitkeep files:
   - `tests/fixtures/manifests/.gitkeep`
   - `tests/fixtures/carl-directories/.gitkeep`
   - `tests/fixtures/prompts/.gitkeep`
   - `tests/fixtures/sessions/.gitkeep`

Each fixture directory purpose:
- manifests/ - Valid/malformed/empty manifest files for parser tests
- carl-directories/ - Pre-configured .carl/ structures for integration tests
- prompts/ - Text files with keywords for scanner/matcher tests
- sessions/ - JSON session files for session manager tests
  </action>
  <verify>
    <automated>for dir in tests/fixtures/manifests tests/fixtures/carl-directories tests/fixtures/prompts tests/fixtures/sessions; do test -d "$dir" || (echo "FAIL: $dir missing"; exit 1); done && echo "OK: all fixture directories exist"</automated>
  </verify>
  <done>All four fixture directories exist with .gitkeep files</done>
</task>

<task type="auto">
  <name>task 3: Create fixtures README documentation</name>
  <files>tests/fixtures/README.md</files>
  <action>
Create `tests/fixtures/README.md` to document fixture usage:

```markdown
# Test Fixtures

This directory contains reusable test data for unit, integration, and e2e tests.

## Directory Structure

### manifests/
Sample CARL manifest files for testing the manifest parser.

- Valid single-domain manifests
- Valid multi-domain manifests  
- Empty manifests
- Malformed manifests (syntax errors)
- STATE/ALWAYS_ON parsing examples

### carl-directories/
Complete .carl/ directory structures for integration testing.

- Default template structure
- Multi-domain configurations
- Global rule configurations
- Command configurations

### prompts/
Sample prompt text files for keyword matching tests.

- Prompts with matching keywords
- Prompts with excluded keywords
- Prompts with multiple domain matches
- Case-insensitive matching examples
- Edge cases (empty, special characters)

### sessions/
Sample session state files (.json) for session manager tests.

- Fresh sessions (60%+ context)
- Moderate sessions (40-60% context)
- Depleted sessions (25-40% context)
- Critical sessions (<25% context)
- Domain override configurations

## Usage

Import fixtures in tests:

```typescript
import { readFileSync } from 'fs';
import { join } from 'path';

const fixturesDir = join(__dirname, '../fixtures');

// Load a manifest fixture
const manifest = readFileSync(join(fixturesDir, 'manifests', 'single-domain.txt'), 'utf-8');

// Load a prompt fixture
const prompt = readFileSync(join(fixturesDir, 'prompts', 'multi-keyword.txt'), 'utf-8');
```

## Adding Fixtures

When adding new fixtures:
1. Use descriptive filenames (e.g., `manifest-valid-multi-domain.txt`)
2. Keep fixtures minimal but realistic
3. Update this README with new fixture descriptions
```
  </action>
  <verify>
    <automated>test -f tests/fixtures/README.md && grep -q "manifests/" tests/fixtures/README.md && grep -q "prompts/" tests/fixtures/README.md && echo "OK: fixtures README exists" || (echo "FAIL: README missing or incomplete"; exit 1)</automated>
  </verify>
  <done>tests/fixtures/README.md exists with documentation for all fixture types</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All test directories exist: tests/javascript/{unit,integration,e2e}/
2. All fixture directories exist: tests/fixtures/{manifests,carl-directories,prompts,sessions}/
3. README.md documents fixture usage
4. All directories have .gitkeep to preserve them in git
</verification>

<success_criteria>
- [ ] tests/javascript/unit/, tests/javascript/integration/, tests/javascript/e2e/ directories exist
- [ ] tests/fixtures/manifests/, tests/fixtures/carl-directories/, tests/fixtures/prompts/, tests/fixtures/sessions/ directories exist
- [ ] tests/fixtures/README.md documents all fixture types
- [ ] All directories contain .gitkeep files
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-infrastructure-ci-setup/07-02-SUMMARY.md`
</output>
