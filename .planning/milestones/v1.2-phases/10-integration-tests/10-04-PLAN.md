---
phase: 10-integration-tests
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [tests/javascript/integration/file-operations.test.ts]
autonomous: true
requirements: [INTG-04]

must_haves:
  truths:
    - "Manifest changes are detected and reload rules"
    - "Session files override domain states correctly"
    - "Global rules resolve before project rules"
    - "Local rules override global rules with same name"
  artifacts:
    - path: "tests/javascript/integration/file-operations.test.ts"
      provides: "File system operations integration tests"
      min_lines: 200
  key_links:
    - from: "tests/javascript/integration/file-operations.test.ts"
      to: "src/carl/loader.ts"
      via: "loadCarlRules()"
      pattern: "loadCarlRules"
    - from: "tests/javascript/integration/file-operations.test.ts"
      to: "src/carl/rule-cache.ts"
      via: "markRulesDirty(), getCachedRules()"
      pattern: "markRulesDirty|getCachedRules"
    - from: "tests/javascript/integration/file-operations.test.ts"
      to: "src/carl/session-overrides.ts"
      via: "loadSessionOverrides()"
      pattern: "loadSessionOverrides"
---

<objective>
Create integration tests that verify file system operations including manifest changes, session persistence, and global/local rule resolution.

Purpose: Ensure manifest changes trigger reload, session files override domains correctly, and global/local rule resolution works as expected.

Output: File system operations integration test suite with comprehensive coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-tests/10-CONTEXT.md
@src/carl/loader.ts
@src/carl/rule-cache.ts
@src/carl/session-overrides.ts
@src/carl/types.ts
@tests/javascript/unit/loader.test.ts
@tests/javascript/unit/rule-cache.test.ts
@tests/javascript/unit/session-overrides.test.ts
</context>

<tasks>

<task type="auto">
  <name>Implement file system operations integration test</name>
  <files>tests/javascript/integration/file-operations.test.ts</files>
  <action>Create comprehensive file system operations integration test:

Test file: tests/javascript/integration/file-operations.test.ts

Structure:
- Import loadCarlRules from src/carl/loader.ts
- Import markRulesDirty, getCachedRules from src/carl/rule-cache.ts
- Import loadSessionOverrides from src/carl/session-overrides.ts
- Import fs, path, os for temp directory management
- Describe block: 'file system operations - integration'

Setup:
- beforeEach: Create temp directories for project, global, and fallback .carl/
- afterEach: Clean up all temp directories

Test cases:

1. 'should detect manifest changes and reload rules'
   - Setup temp .carl/ with DEVELOPMENT domain active
   - Load rules and cache result: getCachedRules({ sessionId })
   - Modify MANIFEST to change DEVELOPMENT_STATE to 'inactive'
   - Mark cache as dirty: markRulesDirty()
   - Load rules again: getCachedRules({ sessionId })
   - Assert result.domains does not include DEVELOPMENT
   - Assert projectStatus reflects updated manifest

2. 'should detect domain file changes and reload rules'
   - Setup temp .carl/ with development domain file containing rules
   - Load rules and cache result
   - Modify development domain file to add new rule
   - Mark cache as dirty and reload
   - Assert result.domainPayloads.DEVELOPMENT.rules includes new rule
   - Verify rule count increased

3. 'should apply session overrides from session files'
   - Setup temp .carl/ with multiple active domains (DEVELOPMENT, CONTENT, CONTEXT)
   - Create session file: .carl/sessions/test-session.json
   - Write session overrides: { domains: { DEVELOPMENT: "inactive", CONTENT: "active" } }
   - Load session overrides: loadSessionOverrides(path.join(tempDir, '.carl'), 'test-session')
   - Assert overrides disable DEVELOPMENT domain
   - Assert overrides preserve CONTENT domain as active

4. 'should prefer project rules over global rules with same domain name'
   - Setup global .carl/ with DEVELOPMENT domain containing global rules
   - Setup project .carl/ with DEVELOPMENT domain containing project rules
   - Load rules with both project and global paths
   - Assert result.sources includes both project and global scopes
   - Assert result.domainPayloads.DEVELOPMENT.rules contain project rules only
   - Assert global rules for DEVELOPMENT not present (project overrides)

5. 'should load fallback rules when project and global are missing'
   - Setup fallback .carl/ with minimal CONTENT domain
   - Leave project and global directories empty
   - Load rules with fallback path
   - Assert result.sources includes fallback scope
   - Assert result.domains includes CONTENT
   - Assert result.domainPayloads.CONTENT.rules contain fallback rules

6. 'should handle corrupted session file gracefully'
   - Setup temp .carl/ with domains
   - Create session file with invalid JSON: .carl/sessions/bad-session.json
   - Load session overrides
   - Assert function returns empty overrides (graceful degradation)
   - Assert no errors thrown

Pattern: Use real temp directories for project, global, and fallback .carl/. Reuse existing patterns from loader.test.ts, rule-cache.test.ts, session-overrides.test.ts. Use fs.writeFileSync() and fs.readFileSync() for direct file manipulation. Verify file system state directly by reading files. Use markRulesDirty() to simulate file change detection.</action>
  <verify>npm test -- tests/javascript/integration/file-operations.test.ts</verify>
  <done>All 6 file system operations tests pass, verifying manifest changes, session persistence, and resolution</done>
</task>

</tasks>

<verification>
1. Run integration test suite: npm test -- tests/javascript/integration/file-operations.test.ts
2. Verify all tests pass with no errors
3. Check coverage: npm run test:coverage -- tests/javascript/integration/file-operations.test.ts
4. Verify manifest change test detects and reloads correctly
5. Verify domain file change test reloads rules
6. Verify session override test applies overrides correctly
7. Verify project over global test uses correct precedence
8. Verify fallback test loads rules when others missing
9. Verify error handling for corrupted session files
</verification>

<success_criteria>
1. File system operations integration tests exist and all pass
2. Manifest changes trigger rule reload correctly
3. Domain file changes detected and reloaded
4. Session files override domain states correctly
5. Project rules override global rules with same name
6. Fallback rules load when project/global missing
7. Corrupted session files handled gracefully
8. Test coverage > 80% for loader.ts, rule-cache.ts, session-overrides.ts
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-tests/10-04-SUMMARY.md`
</output>
