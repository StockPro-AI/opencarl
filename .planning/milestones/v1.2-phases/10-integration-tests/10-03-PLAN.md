---
phase: 10-integration-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [tests/javascript/integration/setup-domain-workflow.test.ts]
autonomous: true
requirements: [INTG-03]

must_haves:
  truths:
    - "Setup command creates .carl/ directory structure"
    - "List command shows available domains and states"
    - "Toggle command changes domain state"
    - "Changes persist across subsequent operations"
  artifacts:
    - path: "tests/javascript/integration/setup-domain-workflow.test.ts"
      provides: "Setup and domain workflow integration tests"
      min_lines: 180
  key_links:
    - from: "tests/javascript/integration/setup-domain-workflow.test.ts"
      to: "src/carl/setup.ts"
      via: "runSetup()"
      pattern: "runSetup"
    - from: "tests/javascript/integration/setup-domain-workflow.test.ts"
      to: "src/carl/setup.ts"
      via: "runList()"
      pattern: "runList"
    - from: "tests/javascript/integration/setup-domain-workflow.test.ts"
      to: "src/carl/setup.ts"
      via: "runToggle()"
      pattern: "runToggle"
    - from: "tests/javascript/integration/setup-domain-workflow.test.ts"
      to: "src/carl/loader.ts"
      via: "loadCarlRules()"
      pattern: "loadCarlRules"
---

<objective>
Create integration tests that verify the setup → list → toggle workflow operates correctly end-to-end.

Purpose: Ensure setup scaffolds .carl/ directory, list displays domains correctly, and toggle persists domain state changes.

Output: Setup and domain workflow integration test suite with comprehensive coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-tests/10-CONTEXT.md
@src/carl/setup.ts
@src/carl/loader.ts
@src/carl/types.ts
@tests/javascript/unit/setup.test.ts
@tests/javascript/unit/loader.test.ts
</context>

<tasks>

<task type="auto">
  <name>Implement setup and domain workflow integration test</name>
  <files>tests/javascript/integration/setup-domain-workflow.test.ts</files>
  <action>Create comprehensive setup and domain workflow integration test:

Test file: tests/javascript/integration/setup-domain-workflow.test.ts

Structure:
- Import runSetup, runList, runToggle from src/carl/setup.ts
- Import loadCarlRules from src/carl/loader.ts
- Import fs, path, os for temp directory management
- Describe block: 'setup and domain workflow - integration'

Setup:
- beforeEach: Create temp working directory for .carl/
- afterEach: Clean up temp directory

Test cases:

1. 'should scaffold .carl/ directory with setup command'
   - Run setup: runSetup({ cwd: tempDir, homeDir })
   - Assert .carl/ directory exists: fs.existsSync(path.join(tempDir, '.carl'))
   - Assert MANIFEST file exists with domain entries
   - Assert template domain files exist (development, content, context)
   - Assert setup returns { success: true }

2. 'should list available domains and their states'
   - Run setup to create .carl/ directory
   - Run list: runList({ cwd: tempDir, homeDir })
   - Assert list output contains domain names (DEVELOPMENT, CONTENT, CONTEXT)
   - Assert list shows active/inactive state for each domain
   - Assert list format matches expected output (console output or return value)

3. 'should toggle domain state from active to inactive'
   - Run setup to create .carl/ directory
   - Run toggle for DEVELOPMENT domain: runToggle({ cwd: tempDir, domain: 'DEVELOPMENT' })
   - Assert MANIFEST file DEVELOPMENT_STATE changed to 'inactive'
   - Verify via loadCarlRules() that DEVELOPMENT domain is not in active list
   - Assert toggle returns success message

4. 'should toggle domain state from inactive back to active'
   - Run setup, then toggle DEVELOPMENT to inactive
   - Run toggle again for DEVELOPMENT domain
   - Assert MANIFEST file DEVELOPMENT_STATE changed back to 'active'
   - Verify via loadCarlRules() that DEVELOPMENT domain is in active list

5. 'should verify full workflow: setup → list → toggle → list'
   - Run setup, capture initial list output
   - Run toggle to change CONTENT domain state
   - Run list again, verify output shows new state
   - Run toggle to restore original state
   - Verify list shows original state restored

6. 'should handle toggle for non-existent domain gracefully'
   - Run setup
   - Run toggle for non-existent domain: 'NONEXISTENT'
   - Assert toggle returns error or warning message
   - Assert .carl/ directory structure unchanged
   - Verify no invalid MANIFEST entries created

Pattern: Use real temp .carl/ directories for each test. Reuse existing patterns from setup.test.ts unit tests. Use loadCarlRules() to verify domain state changes persist. Verify file system state directly by reading MANIFEST file with fs.readFileSync(). Check both console output (if any) and return values from setup functions.</action>
  <verify>npm test -- tests/javascript/integration/setup-domain-workflow.test.ts</verify>
  <done>All 6 setup and domain workflow tests pass, verifying full setup → list → toggle cycle</done>
</task>

</tasks>

<verification>
1. Run integration test suite: npm test -- tests/javascript/integration/setup-domain-workflow.test.ts
2. Verify all tests pass with no errors
3. Check coverage: npm run test:coverage -- tests/javascript/integration/setup-domain-workflow.test.ts
4. Verify setup test creates complete .carl/ directory structure
5. Verify list test displays all domains with correct states
6. Verify toggle tests change domain state in MANIFEST file
7. Verify workflow test executes complete setup → list → toggle → list cycle
8. Verify error handling for non-existent domains
</verification>

<success_criteria>
1. Setup and domain workflow integration tests exist and all pass
2. Setup scaffolds .carl/ directory with MANIFEST and template files
3. List displays domains with correct active/inactive states
4. Toggle changes domain state in MANIFEST file
5. State changes persist across subsequent operations
6. Full workflow tested: setup → list → toggle → list
7. Error handling for non-existent domains verified
8. Test coverage > 80% for setup.ts
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-tests/10-03-SUMMARY.md`
</output>
