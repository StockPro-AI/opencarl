---
phase: 10-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/helpers/opencode-api-mock.ts, tests/javascript/integration/plugin-lifecycle.test.ts]
autonomous: true
requirements: [INTG-01]

must_haves:
  truths:
    - "Plugin initializes and registers hooks correctly"
    - "chat.message hook captures prompt signals"
    - "tool.execute.before hook captures tool signals"
    - "command.execute.before hook captures command signals"
    - "experimental.chat.system.transform hook processes full pipeline"
  artifacts:
    - path: "tests/helpers/opencode-api-mock.ts"
      provides: "OpenCode API simulation helpers"
      exports: ["createMockOpenCodeClient", "createMockHooksInput", "createMockHooksOutput"]
    - path: "tests/javascript/integration/plugin-lifecycle.test.ts"
      provides: "Plugin lifecycle integration tests"
      min_lines: 150
  key_links:
    - from: "tests/javascript/integration/plugin-lifecycle.test.ts"
      to: "src/integration/plugin-hooks.ts"
      via: "createCarlPluginHooks()"
      pattern: "createCarlPluginHooks"
    - from: "tests/helpers/opencode-api-mock.ts"
      to: "tests/javascript/integration/plugin-lifecycle.test.ts"
      via: "import statements"
      pattern: "import.*opencode-api-mock"
---

<objective>
Create integration tests that verify plugin lifecycle, hook registration, and message processing work correctly end-to-end.

Purpose: Ensure plugin initializes correctly, all hooks are registered, and signals are captured and processed through the full pipeline.

Output: OpenCode API mock helpers and plugin lifecycle integration test suite with comprehensive coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-tests/10-CONTEXT.md
@src/integration/plugin-hooks.ts
@src/carl/signal-store.ts
@src/carl/types.ts
</context>

<tasks>

<task type="auto">
  <name>Create OpenCode API mock helpers</name>
  <files>tests/helpers/opencode-api-mock.ts</files>
  <action>Create OpenCode API simulation helpers for integration tests:

1. createMockOpenCodeClient(sessionId: string, messages?: any[]): Returns mock client with:
   - session.messages({ path: { id } }) returning mock messages with token telemetry
   - Methods matching @opencode-ai/plugin client interface

2. createMockHooksInput(overrides?: Partial<any>): Returns mock hook input with:
   - sessionID (string)
   - message (MessageLike with content)
   - parts (PartLike[] array)
   - tool (string)
   - args (object)
   - command (string)

3. createMockHooksOutput(overrides?: Partial<any>): Returns mock hook output with:
   - system (string[] array for injection)
   - message (MessageLike)

Pattern: Use Jest mock functions for methods, return empty arrays by default, allow overrides for specific test scenarios. Follow @opencode-ai/plugin types from src/integration/plugin-hooks.ts imports.</action>
  <verify>cat tests/helpers/opencode-api-mock.ts | grep -E "(export function|createMock)"</verify>
  <done>OpenCode API mock helpers exist with all three helper functions exported and type-safe</done>
</task>

<task type="auto">
  <name>Implement plugin lifecycle integration test</name>
  <files>tests/javascript/integration/plugin-lifecycle.test.ts</files>
  <action>Create comprehensive plugin lifecycle integration test:

Test file: tests/javascript/integration/plugin-lifecycle.test.ts

Structure:
- Import createCarlPluginHooks from plugin-hooks
- Import OpenCode API mock helpers from opencode-api-mock
- Describe block: 'plugin-hooks.ts - integration'

Test cases:

1. 'should initialize plugin and register all hooks'
   - Create hooks via createCarlPluginHooks()
   - Assert hook keys exist: 'chat.message', 'tool.execute.before', 'command.execute.before', 'experimental.chat.system.transform'
   - Assert each hook is a function

2. 'should capture prompt signals from chat.message hook'
   - Create hooks and mock input/output with user message
   - Call hooks['chat.message'](input, output)
   - Verify getSessionSignals() returns captured prompt text

3. 'should capture tool signals from tool.execute.before hook'
   - Create hooks and mock input with tool name/args
   - Call hooks['tool.execute.before'](input, output)
   - Verify getSessionSignals() includes tool signals

4. 'should capture command signals from command.execute.before hook'
   - Create hooks and mock input with command
   - Call hooks['command.execute.before'](input)
   - Verify getSessionSignals() includes command signals

5. 'should process full injection pipeline on system transform hook'
   - Setup temp .carl/ directory with test manifest
   - Create mock OpenCode client with token telemetry
   - Create hooks input with sessionID, message, client
   - Call hooks['experimental.chat.system.transform'](input, output)
   - Assert output.system contains CARL rules injection
   - Assert injection includes expected domain rules

Pattern: Use createMockOpenCodeClient for client simulation, createMockHooksInput/Output for hook I/O. Reuse existing test factories (createTestDomainPayload, createTestBracketData) from tests/helpers/. Set up temp .carl/ in beforeEach, clean up in afterEach.</action>
  <verify>npm test -- tests/javascript/integration/plugin-lifecycle.test.ts</verify>
  <done>All 5 plugin lifecycle tests pass, verifying initialization, signal capture, and full pipeline processing</done>
</task>

</tasks>

<verification>
1. Run integration test suite: npm test -- tests/javascript/integration/plugin-lifecycle.test.ts
2. Verify all tests pass with no errors
3. Check coverage: npm run test:coverage -- tests/javascript/integration/plugin-lifecycle.test.ts
4. Verify hook registration covers all 4 hooks
5. Verify signal capture tests for prompt, tool, and command signals
6. Verify full pipeline test produces correct injection output
</verification>

<success_criteria>
1. Plugin lifecycle integration tests exist and all pass
2. OpenCode API mock helpers provide realistic simulation
3. All 4 hooks are tested: chat.message, tool.execute.before, command.execute.before, experimental.chat.system.transform
4. Signal capture verified for prompt, tool, and command signals
5. Full injection pipeline tested from manifest to injection output
6. Tests use temp .carl/ directories with cleanup
7. Test coverage > 80% for plugin-hooks.ts
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-tests/10-01-SUMMARY.md`
</output>
