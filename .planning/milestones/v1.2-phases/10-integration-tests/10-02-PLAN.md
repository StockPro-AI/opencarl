---
phase: 10-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tests/javascript/integration/rule-injection-pipeline.test.ts, tests/javascript/integration/__snapshots__/rule-injection-pipeline.test.ts.snap]
autonomous: true
requirements: [INTG-02]

must_haves:
  truths:
    - "Manifest loads and parses correctly"
    - "Keywords scan and match domains"
    - "Rules load from domain files"
    - "Rules compose with matched domains"
    - "Injection output matches expected snapshot"
  artifacts:
    - path: "tests/javascript/integration/rule-injection-pipeline.test.ts"
      provides: "Rule injection pipeline integration tests"
      min_lines: 200
    - path: "tests/javascript/integration/__snapshots__/rule-injection-pipeline.test.ts.snap"
      provides: "CARL injection output snapshots"
      contains: "<carl-rules>"
  key_links:
    - from: "tests/javascript/integration/rule-injection-pipeline.test.ts"
      to: "src/carl/loader.ts"
      via: "loadCarlRules()"
      pattern: "loadCarlRules"
    - from: "tests/javascript/integration/rule-injection-pipeline.test.ts"
      to: "src/carl/matcher.ts"
      via: "matchDomainsForTurn()"
      pattern: "matchDomainsForTurn"
    - from: "tests/javascript/integration/rule-injection-pipeline.test.ts"
      to: "src/carl/injector.ts"
      via: "buildCarlInjection()"
      pattern: "buildCarlInjection"
    - from: "tests/javascript/integration/__snapshots__/rule-injection-pipeline.test.ts.snap"
      to: "tests/javascript/integration/rule-injection-pipeline.test.ts"
      via: "toMatchSnapshot()"
      pattern: "toMatchSnapshot"
---

<objective>
Create integration tests that verify the complete rule injection pipeline from manifest loading through final injection output.

Purpose: Ensure manifest → scan → load → compose → inject pipeline produces correct rules at each stage, with snapshot testing for regression detection.

Output: Rule injection pipeline integration test suite with snapshot-based verification.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-tests/10-CONTEXT.md
@src/carl/loader.ts
@src/carl/matcher.ts
@src/carl/injector.ts
@src/carl/types.ts
@tests/javascript/unit/loader.test.ts
@tests/javascript/unit/matcher.test.ts
@tests/javascript/unit/injector.test.ts
</context>

<tasks>

<task type="auto">
  <name>Implement rule injection pipeline integration test</name>
  <files>tests/javascript/integration/rule-injection-pipeline.test.ts</files>
  <action>Create comprehensive rule injection pipeline integration test:

Test file: tests/javascript/integration/rule-injection-pipeline.test.ts

Structure:
- Import loader, matcher, injector from src/carl/
- Import test factories from tests/helpers/
- Import fs, path, os for temp directory management
- Describe block: 'rule injection pipeline - integration'

Setup:
- beforeEach: Create temp directory with .carl/ structure
- Use tests/fixtures/carl-directories/full/.carl/ for realistic test data
- afterEach: Clean up temp directory

Test cases:

1. 'should load rules from manifest and domain files'
   - Call loadCarlRules() with temp directory paths
   - Assert result.domains contains expected domains (CONTENT, CONTEXT, DEVELOPMENT)
   - Assert result.domainPayloads has rules for each domain
   - Assert result.projectStatus is 'valid'

2. 'should match domains based on prompt keywords'
   - Load rules with loadCarlRules()
   - Build domain configs from loaded payloads
   - Call matchDomainsForTurn() with prompt text containing keywords
   - Assert matchedDomains includes domains with matching recall keywords
   - Assert excludedDomains filtered by exclude keywords

3. 'should build injection with matched domains'
   - Load rules and match domains
   - Call buildCarlInjection() with domainPayloads and matchedDomains
   - Assert result contains <carl-rules> opening tag
   - Assert result contains domain headers (e.g., '[DEVELOPMENT] RULES:')
   - Assert result contains numbered rules
   - Assert result contains </carl-rules> closing tag

4. 'should handle context bracket filtering in injection'
   - Load rules with CONTEXT domain having bracketRules
   - Match domains and build injection with MODERATE bracket
   - Assert injection uses MODERATE rules, not FRESH or DEPLETED
   - Test with CRITICAL bracket and verify DEPLETED rules used

5. 'should produce correct injection output (snapshot)'
   - Use full fixture with multiple domains (CONTENT, CONTEXT, DEVELOPMENT)
   - Match domains based on realistic prompt: "Help me debug this code"
   - Build injection with FRESH context bracket
   - Use expect(injection).toMatchSnapshot() for regression detection

6. 'should handle empty matched domains'
   - Load rules but match domains with no matching keywords
   - Call buildCarlInjection() with empty matchedDomains
   - Assert result is null (no injection)

Pattern: Use real temp .carl/ directories with full fixture data. Reuse existing unit test factories for helper data. Follow existing unit test patterns from loader.test.ts, matcher.test.ts, injector.test.ts for setup and assertions. Use toMatchSnapshot() for final injection output verification.</action>
  <verify>npm test -- tests/javascript/integration/rule-injection-pipeline.test.ts</verify>
  <done>All 6 rule injection pipeline tests pass, snapshot matches expected output</done>
</task>

</tasks>

<verification>
1. Run integration test suite: npm test -- tests/javascript/integration/rule-injection-pipeline.test.ts
2. Verify all tests pass with no errors
3. Check coverage: npm run test:coverage -- tests/javascript/integration/rule-injection-pipeline.test.ts
4. Verify manifest loading tests produce correct domains and payloads
5. Verify keyword matching tests match expected domains
6. Verify injection tests produce correctly formatted output
7. Verify snapshot test creates .snap file with expected format
8. Run tests with --updateSnapshot flag if snapshot needs initial creation
</verification>

<success_criteria>
1. Rule injection pipeline integration tests exist and all pass
2. All pipeline stages tested: manifest loading, keyword matching, rule loading, composition, injection
3. Snapshot test catches regressions in injection output format
4. Tests use real .carl/ directories with full fixture data
5. Context bracket filtering verified for FRESH, MODERATE, DEPLETED, CRITICAL
6. Empty matched domains handled correctly (no injection)
7. Test coverage > 80% for loader.ts, matcher.ts, injector.ts
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-tests/10-02-SUMMARY.md`
</output>
