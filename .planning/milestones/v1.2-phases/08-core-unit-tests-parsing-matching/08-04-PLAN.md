---
phase: 08-core-unit-tests-parsing-matching
plan: 04
type: tdd
wave: 1
depends_on: ["08-01"]
files_modified:
  - tests/javascript/unit/context-brackets.test.ts
autonomous: true
requirements: [UNIT-05]
must_haves:
  truths:
    - "Developer can verify context bracket logic classifies fresh sessions correctly"
    - "Developer can verify context bracket logic classifies moderate sessions correctly"
    - "Developer can verify context bracket logic classifies depleted sessions correctly"
    - "Developer can verify context bracket logic handles critical state with warning"
    - "Developer can verify bracket rule selection returns correct rules per bracket"
  artifacts:
    - path: "tests/javascript/unit/context-brackets.test.ts"
      provides: "Unit tests for context bracket logic"
      contains: "describe('context-brackets')"
      min_lines: 200
  key_links:
    - from: "tests/javascript/unit/context-brackets.test.ts"
      to: "src/carl/context-brackets.ts"
      via: "import"
      pattern: "import.*getContextBracket"
---

<objective>
Write comprehensive unit tests for context bracket logic in `context-brackets.ts`.

Purpose: Verify context bracket classification, rule selection, and CRITICAL warning behavior work correctly.
Output: Test file `context-brackets.test.ts` with full coverage of bracket functions.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-unit-tests-parsing-matching/08-CONTEXT.md

@src/carl/context-brackets.ts
@tests/helpers/bracket-factory.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create context brackets test file with describe structure</name>
  <files>tests/javascript/unit/context-brackets.test.ts</files>
  <action>
Create test file with BDD-style nested describe blocks:

```
describe('context-brackets.ts', () => {
  describe('getContextBracket', () => {
    // Tests for bracket classification from percentage
  });
  describe('computeContextBracketData', () => {
    // Tests for full bracket data computation
  });
  describe('getRulesBracket', () => {
    // Tests for rules bracket mapping (CRITICAL -> DEPLETED)
  });
  describe('selectBracketRules', () => {
    // Tests for selecting rules based on bracket
  });
  describe('parseContextFile', () => {
    // Tests for parsing bracket rules from file content
  });
  describe('formatCriticalWarning', () => {
    // Tests for critical warning formatting
  });
});
```

Import all exported functions from `'../../../src/carl/context-brackets'`
Import: `createTestBracketData` from `'../../helpers/bracket-factory'`
Import Jest globals
  </action>
  <verify>
    <automated>npx tsc --noEmit tests/javascript/unit/context-brackets.test.ts 2>&1 | head -5</automated>
  </verify>
  <done>Test file structure created with nested describe blocks</done>
</task>

<task type="auto">
  <name>task 2: Write bracket classification tests</name>
  <files>tests/javascript/unit/context-brackets.test.ts</files>
  <action>
Add tests to 'getContextBracket' and 'getRulesBracket' describe blocks:

**getContextBracket:**
1. **should return FRESH for null percentage (unknown)**
   - `getContextBracket(null)` -> 'FRESH'

2. **should return FRESH for >= 60% remaining**
   - Test: 60, 70, 80, 100 percent
   - Assert: all return 'FRESH'

3. **should return MODERATE for >= 40% and < 60%**
   - Test: 40, 50, 59 percent
   - Assert: all return 'MODERATE'

4. **should return DEPLETED for >= 25% and < 40%**
   - Test: 25, 30, 39 percent
   - Assert: all return 'DEPLETED'

5. **should return CRITICAL for < 25%**
   - Test: 0, 10, 24 percent
   - Assert: all return 'CRITICAL'

6. **should handle 0% remaining**
   - `getContextBracket(0)` -> 'CRITICAL'

**getRulesBracket:**
1. **should return DEPLETED for CRITICAL bracket**
   - CRITICAL state uses DEPLETED rules
   - Assert: `getRulesBracket('CRITICAL')` === 'DEPLETED'

2. **should return same bracket for non-CRITICAL**
   - FRESH, MODERATE, DEPLETED pass through
   - Assert: `getRulesBracket('FRESH')` === 'FRESH'
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/context-brackets.test.ts --testNamePattern="getContextBracket|getRulesBracket" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Eight bracket classification tests pass</done>
</task>

<task type="auto">
  <name>task 3: Write computeContextBracketData tests</name>
  <files>tests/javascript/unit/context-brackets.test.ts</files>
  <action>
Add tests to 'computeContextBracketData' describe block:

1. **should return FRESH for null tokensUsed**
   - `computeContextBracketData(null)`
   - Assert: bracket='FRESH', contextRemaining=null, isCritical=false

2. **should return FRESH for 0 tokensUsed**
   - `computeContextBracketData(0)`
   - Assert: bracket='FRESH', contextRemaining=null

3. **should compute percentage correctly with default max**
   - tokensUsed=80000, maxContext=200000 (default)
   - Assert: contextRemaining=60 (60%), bracket='FRESH'

4. **should compute MODERATE bracket correctly**
   - tokensUsed=100000, maxContext=200000
   - Assert: contextRemaining=50, bracket='MODERATE'

5. **should compute DEPLETED bracket correctly**
   - tokensUsed=130000, maxContext=200000
   - Assert: contextRemaining=35, bracket='DEPLETED'

6. **should compute CRITICAL bracket correctly**
   - tokensUsed=170000, maxContext=200000
   - Assert: contextRemaining=15, bracket='CRITICAL', isCritical=true

7. **should handle custom maxContext parameter**
   - tokensUsed=50000, maxContext=100000
   - Assert: contextRemaining=50

8. **should cap contextRemaining at 0 minimum**
   - tokensUsed > maxContext
   - Assert: contextRemaining >= 0

9. **should set rulesBracket to DEPLETED for CRITICAL**
   - CRITICAL state
   - Assert: rulesBracket='DEPLETED'

10. **should set rulesBracket same as bracket for non-CRITICAL**
    - FRESH, MODERATE states
    - Assert: rulesBracket === bracket
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/context-brackets.test.ts --testNamePattern="computeContextBracketData" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Ten computeContextBracketData tests pass</done>
</task>

<task type="auto">
  <name>task 4: Write rule selection tests</name>
  <files>tests/javascript/unit/context-brackets.test.ts</files>
  <action>
Add tests to 'selectBracketRules' describe block:

1. **should return rules for matching bracket**
   - bracketRules: `{ FRESH: ['rule1', 'rule2'], DEPLETED: ['rule3'] }`
   - Request FRESH bracket
   - Assert: returns ['rule1', 'rule2']

2. **should return empty array for missing bracket**
   - bracketRules: `{ FRESH: ['rule1'] }`
   - Request MODERATE bracket
   - Assert: returns []

3. **should return empty array when bracket disabled**
   - bracketFlags: `{ FRESH: false }`
   - Request FRESH bracket
   - Assert: returns []

4. **should return rules when bracket enabled**
   - bracketFlags: `{ FRESH: true }`
   - Request FRESH bracket
   - Assert: returns rules

5. **should default to enabled when flag not specified**
   - bracketFlags: `{}` (empty)
   - Request FRESH bracket
   - Assert: returns rules (enabled by default)

6. **should handle DEPLETED rules for CRITICAL request**
   - rulesBracket='DEPLETED' (mapped from CRITICAL)
   - Assert: returns DEPLETED rules

7. **should handle empty bracketRules object**
   - bracketRules: `{}`
   - Assert: returns []
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/context-brackets.test.ts --testNamePattern="selectBracketRules" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Seven selectBracketRules tests pass</done>
</task>

<task type="auto">
  <name>task 5: Write context file parsing tests</name>
  <files>tests/javascript/unit/context-brackets.test.ts</files>
  <action>
Add tests to 'parseContextFile' describe block:

1. **should parse bracket flags correctly**
   - Content: `FRESH_RULES=true\nMODERATE_RULES=false`
   - Assert: bracketFlags = { FRESH: true, MODERATE: false }

2. **should parse bracket rules correctly**
   - Content: `FRESH_RULE_1=Use detailed explanations\nFRESH_RULE_2=Include examples`
   - Assert: bracketRules.FRESH contains both rules

3. **should handle multiple brackets in same file**
   - FRESH, MODERATE, DEPLETED rules all present
   - Assert: all parsed into correct arrays

4. **should ignore comment lines**
   - Lines starting with #
   - Assert: comments ignored

5. **should ignore blank lines**
   - Empty lines in content
   - Assert: ignored

6. **should ignore lines without equals sign**
   - Invalid lines
   - Assert: ignored, no error

7. **should parse boolean variations for flags**
   - `true`, `yes`, `1` -> true
   - `false`, `no`, `0` -> false

8. **should preserve rule order within bracket**
   - RULE_1, RULE_2, RULE_3
   - Assert: order maintained in array

9. **should handle empty file**
   - Empty content string
   - Assert: returns empty objects

10. **should skip lines with empty key or value**
    - `=value` or `KEY=`
    - Assert: skipped
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/context-brackets.test.ts --testNamePattern="parseContextFile" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Ten parseContextFile tests pass</done>
</task>

<task type="auto">
  <name>task 6: Write formatting tests</name>
  <files>tests/javascript/unit/context-brackets.test.ts</files>
  <action>
Add tests to 'formatCriticalWarning' and 'formatContextBracketHeader' (if exported):

1. **should format critical warning with percentage**
   - `formatCriticalWarning(15)`
   - Assert: contains "15%", contains "CRITICAL", contains "compact"

2. **should include recommendation in warning**
   - Assert: contains "Recommend:"

3. **should handle low percentages**
   - `formatCriticalWarning(5)`
   - Assert: contains "5%"

4. **should format context bracket header for fresh session**
   - contextRemaining: null, bracket: 'FRESH'
   - Assert: contains "fresh session"

5. **should format context bracket header with percentage**
   - contextRemaining: 50, bracket: 'MODERATE'
   - Assert: contains "50%", contains "MODERATE"

6. **should include critical warning in header when isCritical**
   - isCritical: true
   - Assert: header includes warning prefix
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/context-brackets.test.ts --testNamePattern="format" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Six formatting tests pass</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
npx jest tests/javascript/unit/context-brackets.test.ts --coverage --coverageReporters=text
```

Expected: All 47+ tests pass, coverage of context-brackets.ts >90%
</verification>

<success_criteria>
- Test file `tests/javascript/unit/context-brackets.test.ts` exists
- All describe blocks populated with tests
- Tests pass: classification (8), computeContextBracketData (10), selectBracketRules (7), parseContextFile (10), formatting (6)
- Total test count >= 41
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-unit-tests-parsing-matching/08-04-SUMMARY.md`
</output>
