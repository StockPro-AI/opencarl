---
phase: 08-core-unit-tests-parsing-matching
plan: 03
type: tdd
wave: 1
depends_on: ["08-01"]
files_modified:
  - tests/javascript/unit/matcher.test.ts
autonomous: true
requirements: [UNIT-02]
must_haves:
  truths:
    - "Developer can verify keyword scanner matches RECALL keywords in prompts"
    - "Developer can verify keyword scanner respects EXCLUDE keywords"
    - "Developer can verify keyword scanner handles ALWAYS_ON domains"
    - "Developer can verify keyword scanner is case-insensitive"
    - "Developer can verify keyword scanner handles multi-domain matches"
  artifacts:
    - path: "tests/javascript/unit/matcher.test.ts"
      provides: "Unit tests for keyword scanner"
      contains: "describe('matchDomainsForTurn')"
      min_lines: 180
  key_links:
    - from: "tests/javascript/unit/matcher.test.ts"
      to: "src/carl/matcher.ts"
      via: "import"
      pattern: "import.*matchDomainsForTurn"
---

<objective>
Write comprehensive unit tests for the keyword scanner in `matcher.ts`.

Purpose: Verify matchDomainsForTurn correctly matches RECALL keywords, respects EXCLUDE keywords, handles ALWAYS_ON domains, and processes case-insensitively.
Output: Test file `matcher.test.ts` with full coverage of keyword matching logic.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-unit-tests-parsing-matching/08-CONTEXT.md

@src/carl/matcher.ts
@src/carl/types.ts
@tests/helpers/match-factory.ts
@tests/helpers/domain-factory.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create keyword scanner test file with describe structure</name>
  <files>tests/javascript/unit/matcher.test.ts</files>
  <action>
Create test file with BDD-style nested describe blocks:

```
describe('matcher.ts', () => {
  describe('matchDomainsForTurn', () => {
    describe('keyword matching', () => {
      // Tests for RECALL keyword matching
    });
    describe('exclusion logic', () => {
      // Tests for EXCLUDE and global exclude
    });
    describe('always-on domains', () => {
      // Tests for ALWAYS_ON handling
    });
    describe('case sensitivity', () => {
      // Tests for case-insensitive matching
    });
    describe('multi-domain scenarios', () => {
      // Tests for multiple domain matches
    });
    describe('edge cases', () => {
      // Empty inputs, special characters
    });
  });
});
```

Import: `matchDomainsForTurn` from `'../../../src/carl/matcher'`
Import: `createTestMatchRequest`, `createTestSignals` from `'../../helpers/match-factory'`
Import: `createTestDomainConfig` from `'../../helpers/domain-factory'`
Import Jest globals
  </action>
  <verify>
    <automated>npx tsc --noEmit tests/javascript/unit/matcher.test.ts 2>&1 | head -5</automated>
  </verify>
  <done>Test file structure created with nested describe blocks</done>
</task>

<task type="auto">
  <name>task 2: Write keyword matching tests</name>
  <files>tests/javascript/unit/matcher.test.ts</files>
  <action>
Add tests to 'keyword matching' describe block:

1. **should match single RECALL keyword in prompt**
   - Domain with `recall: ['fix bug']`
   - Prompt: "Can you help me fix bug in my app?"
   - Assert: `result.matchedDomains` contains domain name

2. **should match when keyword is substring of prompt word**
   - Domain with `recall: ['code']`
   - Prompt: "Let me code this feature"
   - Assert: matched (keyword is contained in prompt text)

3. **should match multi-word RECALL phrase**
   - Domain with `recall: ['fix bug', 'write code']`
   - Prompt: "I need to fix bug and write code"
   - Assert: domain matched

4. **should not match when keyword not in prompt**
   - Domain with `recall: ['database']`
   - Prompt: "Help me with the UI"
   - Assert: `result.matchedDomains` is empty

5. **should only match active domains**
   - Two domains: one `state: true`, one `state: false`
   - Both have matching RECALL keywords
   - Assert: only active domain in matchedDomains

6. **should return matched keywords in result**
   - Domain with multiple RECALL keywords, some match
   - Assert: `domainResults[0].matchedKeywords` contains matched entries
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/matcher.test.ts --testNamePattern="keyword matching" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Six keyword matching tests pass</done>
</task>

<task type="auto">
  <name>task 3: Write exclusion logic tests</name>
  <files>tests/javascript/unit/matcher.test.ts</files>
  <action>
Add tests to 'exclusion logic' describe block:

1. **should exclude domain when EXCLUDE keyword present**
   - Domain with `recall: ['code']`, `exclude: ['review']`
   - Prompt: "review this code change"
   - Assert: domain NOT in matchedDomains, IS in excludedDomains

2. **should include EXCLUDE matches in result**
   - Test that `domainResults[0].excludedKeywords` populated
   - Assert: `exclusionSource === 'domain'`

3. **should trigger global exclude and skip all matching**
   - Request with `globalExclude: ['secret']`
   - Prompt: "this contains secret information"
   - Assert: `result.globalExclude.triggered === true`
   - Assert: `result.matchedDomains` is empty

4. **should return global exclude matched keywords**
   - Assert: `result.globalExclude.matchedKeywords` contains 'secret'

5. **should prioritize global exclude over domain exclude**
   - Global exclude triggers, domain excludes ignored
   - Assert: early return behavior

6. **should exclude takes precedence over recall**
   - Prompt matches both RECALL and EXCLUDE
   - Assert: EXCLUDE wins, domain excluded
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/matcher.test.ts --testNamePattern="exclusion" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Six exclusion logic tests pass</done>
</task>

<task type="auto">
  <name>task 4: Write always-on domain tests</name>
  <files>tests/javascript/unit/matcher.test.ts</files>
  <action>
Add tests to 'always-on domains' describe block:

1. **should skip ALWAYS_ON domains in matching logic**
   - Domain with `alwaysOn: true`
   - Prompt with matching RECALL keyword
   - Assert: domain NOT in matchedDomains (matcher skips alwaysOn)
   - Note: ALWAYS_ON handled by injector, not matcher

2. **should not require RECALL keywords for ALWAYS_ON**
   - Domain with `alwaysOn: true`, empty recall
   - Assert: no errors, continues processing

3. **should still apply EXCLUDE to ALWAYS_ON domains**
   - ALWAYS_ON domain with exclude keyword in prompt
   - Assert: EXCLUDE logic still applies

4. **should sort matched domains alphabetically**
   - Multiple matched domains with different names
   - Assert: `result.matchedDomains` is sorted

5. **should sort excluded domains alphabetically**
   - Multiple excluded domains
   - Assert: `result.excludedDomains` is sorted
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/matcher.test.ts --testNamePattern="always-on" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Five always-on domain tests pass</done>
</task>

<task type="auto">
  <name>task 5: Write case sensitivity and multi-domain tests</name>
  <files>tests/javascript/unit/matcher.test.ts</files>
  <action>
Add tests to 'case sensitivity' and 'multi-domain scenarios' describe blocks:

**case sensitivity:**
1. **should match keywords case-insensitively**
   - RECALL: `['Fix Bug']`, Prompt: "help me fix bug"
   - Assert: matched (case-insensitive)

2. **should normalize both prompt and keywords to lowercase**
   - Mixed case in both
   - Assert: matching works

3. **should exclude case-insensitively**
   - EXCLUDE: `['REVIEW']`, Prompt: "Review this"
   - Assert: excluded

**multi-domain scenarios:**
1. **should return multiple matched domains**
   - Prompt matching DEVELOPMENT and CONTENT
   - Assert: both in matchedDomains

2. **should handle partial multi-domain match**
   - 3 domains, prompt matches 2
   - Assert: only 2 in matchedDomains

3. **should handle all domains matching**
   - All active domains have matching RECALL
   - Assert: all in matchedDomains

4. **should handle some excluded, some matched**
   - 3 domains: 1 excluded, 1 matched, 1 no match
   - Assert: correct classification

5. **should include signals from toolTokens and pathTokens**
   - Prompt doesn't match, but toolTokens do
   - Assert: domain matched via toolTokens
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/matcher.test.ts --testNamePattern="case sensitivity|multi-domain" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Eight case sensitivity and multi-domain tests pass</done>
</task>

<task type="auto">
  <name>task 6: Write edge case tests</name>
  <files>tests/javascript/unit/matcher.test.ts</files>
  <action>
Add tests to 'edge cases' describe block:

1. **should handle empty prompt gracefully**
   - Empty promptText, domains with RECALL
   - Assert: no matches (unless ALWAYS_ON, which is handled elsewhere)

2. **should handle empty domains list**
   - No domains configured
   - Assert: empty matchedDomains, no errors

3. **should handle domain with empty RECALL list**
   - Domain state=true, recall=[]
   - Assert: no match, no error

4. **should handle whitespace-only RECALL keywords**
   - `recall: ['  ', '', 'valid']`
   - Assert: empty/whitespace filtered, 'valid' used

5. **should handle special characters in keywords**
   - RECALL: `['api/v2', 'user-auth']`
   - Assert: matched correctly

6. **should handle very long prompts**
   - Prompt with 1000+ characters containing keyword
   - Assert: match still works

7. **should handle prompt with keyword repeated**
   - Prompt: "fix bug fix bug fix bug"
   - Assert: single match, not duplicated
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/matcher.test.ts --testNamePattern="edge cases" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Seven edge case tests pass</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
npx jest tests/javascript/unit/matcher.test.ts --coverage --coverageReporters=text
```

Expected: All 32+ tests pass, coverage of matchDomainsForTurn function >90%
</verification>

<success_criteria>
- Test file `tests/javascript/unit/matcher.test.ts` exists
- All describe blocks populated with tests
- Tests pass: keyword matching (6), exclusion (6), always-on (5), case sensitivity (3), multi-domain (5), edge cases (7)
- Total test count >= 32
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-unit-tests-parsing-matching/08-03-SUMMARY.md`
</output>
