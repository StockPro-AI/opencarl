---
phase: 08-core-unit-tests-parsing-matching
plan: 05
type: tdd
wave: 2
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - tests/javascript/unit/injector.test.ts
autonomous: true
requirements: [UNIT-04]
must_haves:
  truths:
    - "Developer can verify rule composer correctly combines single domain rules"
    - "Developer can verify rule composer correctly combines multiple domain rules"
    - "Developer can verify rule composer includes global rules always"
    - "Developer can verify rule composer handles context brackets correctly"
    - "Developer can verify rule composer handles command domains correctly"
  artifacts:
    - path: "tests/javascript/unit/injector.test.ts"
      provides: "Unit tests for rule composer/injector"
      contains: "describe('buildCarlInjection')"
      min_lines: 250
  key_links:
    - from: "tests/javascript/unit/injector.test.ts"
      to: "src/carl/injector.ts"
      via: "import"
      pattern: "import.*buildCarlInjection"
    - from: "tests/javascript/unit/injector.test.ts"
      to: "src/carl/context-brackets.ts"
      via: "import"
      pattern: "import.*ContextBracketData"
---

<objective>
Write comprehensive unit tests for the rule composer/injector in `injector.ts`.

Purpose: Verify buildCarlInjection correctly combines domain rules, handles GLOBAL, processes context brackets, and formats output.
Output: Test file `injector.test.ts` with full coverage of injection logic.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-unit-tests-parsing-matching/08-CONTEXT.md

@src/carl/injector.ts
@src/carl/types.ts
@src/carl/context-brackets.ts
@tests/helpers/domain-factory.ts
@tests/helpers/bracket-factory.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create injector test file with describe structure</name>
  <files>tests/javascript/unit/injector.test.ts</files>
  <action>
Create test file with BDD-style nested describe blocks:

```
describe('injector.ts', () => {
  describe('buildCarlInjection', () => {
    describe('single domain', () => {
      // Tests for single matched domain
    });
    describe('multiple domains', () => {
      // Tests for combining multiple domains
    });
    describe('global rules', () => {
      // Tests for GLOBAL domain handling
    });
    describe('always-on domains', () => {
      // Tests for ALWAYS_ON handling
    });
    describe('command domains', () => {
      // Tests for explicit command domains
    });
    describe('context brackets', () => {
      // Tests for CONTEXT with bracket filtering
    });
    describe('edge cases', () => {
      // Empty inputs, missing domains, etc.
    });
    describe('output formatting', () => {
      // Tests for output structure and format
    });
  });
});
```

Import: `buildCarlInjection` from `'../../../src/carl/injector'`
Import: `createTestDomainPayload` from `'../../helpers/domain-factory'`
Import: `createTestBracketData`, `createFreshBracketData`, etc. from `'../../helpers/bracket-factory'`
Import Jest globals
  </action>
  <verify>
    <automated>npx tsc --noEmit tests/javascript/unit/injector.test.ts 2>&1 | head -5</automated>
  </verify>
  <done>Test file structure created with nested describe blocks</done>
</task>

<task type="auto">
  <name>task 2: Write single domain tests</name>
  <files>tests/javascript/unit/injector.test.ts</files>
  <action>
Add tests to 'single domain' describe block:

1. **should inject single matched domain**
   - domainPayloads: { DEVELOPMENT: payload with rules }
   - matchedDomains: ['DEVELOPMENT']
   - Assert: output contains DEVELOPMENT rules, wrapped in carl-rules tags

2. **should include domain name in output**
   - Assert: output contains "[DEVELOPMENT] RULES:"

3. **should number rules sequentially**
   - Assert: "1. rule1", "2. rule2" format

4. **should not inject inactive domain**
   - Domain with state: false
   - matchedDomains includes it
   - Assert: domain NOT in output

5. **should not inject domain with empty rules**
   - Domain with rules: []
   - Assert: domain NOT in output

6. **should sort domains alphabetically**
   - Multiple matched domains
   - Assert: appear in alphabetical order
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/injector.test.ts --testNamePattern="single domain" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Six single domain tests pass</done>
</task>

<task type="auto">
  <name>task 3: Write multiple domain tests</name>
  <files>tests/javascript/unit/injector.test.ts</files>
  <action>
Add tests to 'multiple domains' describe block:

1. **should combine rules from multiple matched domains**
   - DEVELOPMENT and CONTENT both matched
   - Assert: output contains both domains' rules

2. **should separate domains with headers**
   - Assert: "[DEVELOPMENT] RULES:" and "[CONTENT] RULES:" present

3. **should maintain domain separation (not merge rules)**
   - Rules from different domains not combined into one list
   - Each domain has its own section

4. **should handle three or more domains**
   - 3+ domains matched
   - Assert: all present, all separated

5. **should filter matchedDomains not in domainPayloads**
   - matchedDomains includes 'UNKNOWN'
   - domainPayloads doesn't have UNKNOWN
   - Assert: UNKNOWN ignored, no error
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/injector.test.ts --testNamePattern="multiple domains" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Five multiple domain tests pass</done>
</task>

<task type="auto">
  <name>task 4: Write global and always-on tests</name>
  <files>tests/javascript/unit/injector.test.ts</files>
  <action>
Add tests to 'global rules' and 'always-on domains' describe blocks:

**global rules:**
1. **should always include GLOBAL domain**
   - No matched domains, GLOBAL in domainPayloads
   - Assert: GLOBAL rules in output

2. **should include GLOBAL even with matched domains**
   - DEVELOPMENT matched, GLOBAL present
   - Assert: both in output, GLOBAL in ALWAYS-ON section

3. **should place GLOBAL in ALWAYS-ON DOMAINS section**
   - Assert: output contains "ALWAYS-ON DOMAINS" header with GLOBAL

**always-on domains:**
1. **should include ALWAYS_ON domain even without match**
   - Domain with alwaysOn: true, not in matchedDomains
   - Assert: domain in output

2. **should not duplicate ALWAYS_ON domain in matched section**
   - Domain in matchedDomains AND alwaysOn: true
   - Assert: appears once in ALWAYS-ON section only

3. **should filter inactive ALWAYS_ON domains**
   - alwaysOn: true but state: false
   - Assert: NOT in output

4. **should separate ALWAYS-ON from MATCHED sections**
   - Both sections present
   - Assert: proper headers for each
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/injector.test.ts --testNamePattern="global rules|always-on" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Seven global/always-on tests pass</done>
</task>

<task type="auto">
  <name>task 5: Write command domain tests</name>
  <files>tests/javascript/unit/injector.test.ts</files>
  <action>
Add tests to 'command domains' describe block:

1. **should include command domains in COMMAND section**
   - commandDomains: ['BRIEF']
   - Assert: "COMMAND DOMAINS (explicit)" header present

2. **should prioritize command over matched domains**
   - Domain in both commandDomains and matchedDomains
   - Assert: appears in COMMAND section only

3. **should prioritize command over alwaysOn**
   - Domain in commandDomains AND alwaysOn: true
   - Assert: appears in COMMAND section only

4. **should handle multiple command domains**
   - commandDomains: ['BRIEF', 'DETAILED']
   - Assert: both in COMMAND section

5. **should filter inactive command domains**
   - commandDomain with state: false
   - Assert: NOT in output

6. **should normalize domain names (case-insensitive)**
   - commandDomains: ['brief']
   - domainPayloads has 'BRIEF'
   - Assert: matched correctly
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/injector.test.ts --testNamePattern="command domains" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Six command domain tests pass</done>
</task>

<task type="auto">
  <name>task 6: Write context bracket tests</name>
  <files>tests/javascript/unit/injector.test.ts</files>
  <action>
Add tests to 'context brackets' describe block:

1. **should include CONTEXT bracket header when context data provided**
   - contextBracket with bracket: 'FRESH'
   - CONTEXT domain present
   - Assert: "CONTEXT BRACKET: [FRESH]" in output

2. **should filter CONTEXT rules by bracket**
   - CONTEXT with bracketRules: { FRESH: ['f1'], DEPLETED: ['d1'] }
   - contextBracket: FRESH
   - Assert: 'f1' in output, 'd1' NOT in output

3. **should show percentage remaining in header**
   - contextRemaining: 45
   - Assert: "45% remaining" in output

4. **should show fresh session message for null contextRemaining**
   - contextRemaining: null
   - Assert: "fresh session" in output

5. **should include critical warning for CRITICAL bracket**
   - isCritical: true, contextRemaining: 15
   - Assert: "CONTEXT CRITICAL" warning in output
   - Assert: "compact session" recommendation in output

6. **should use DEPLETED rules for CRITICAL state**
   - bracket: CRITICAL, rulesBracket: DEPLETED
   - CONTEXT has DEPLETED rules
   - Assert: DEPLETED rules used, not FRESH/MODERATE

7. **should not include CONTEXT if bracket rules disabled**
   - bracketFlags: { FRESH: false }
   - bracket: FRESH
   - Assert: no CONTEXT rules in output

8. **should handle CONTEXT without bracket data (backward compat)**
   - contextBracket not provided
   - CONTEXT has rules (not bracketRules)
   - Assert: all CONTEXT rules included
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/injector.test.ts --testNamePattern="context brackets" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Eight context bracket tests pass</done>
</task>

<task type="auto">
  <name>task 7: Write edge case and formatting tests</name>
  <files>tests/javascript/unit/injector.test.ts</files>
  <action>
Add tests to 'edge cases' and 'output formatting' describe blocks:

**edge cases:**
1. **should return null for empty input**
   - Empty domainPayloads, no matched/command domains
   - Assert: `buildCarlInjection()` returns null

2. **should return null when no active domains**
   - All domains have state: false
   - Assert: returns null

3. **should return null when all domains have empty rules**
   - All rules arrays empty
   - Assert: returns null

4. **should handle null domainPayloads**
   - domainPayloads: null
   - Assert: returns null (no crash)

5. **should handle undefined matchedDomains**
   - matchedDomains not provided
   - Assert: still includes GLOBAL/ALWAYS_ON

6. **should handle empty matchedDomains array**
   - matchedDomains: []
   - GLOBAL present
   - Assert: GLOBAL included

**output formatting:**
1. **should wrap output in carl-rules tags**
   - Assert: starts with `<carl-rules>`, ends with `</carl-rules>`

2. **should include trailing newline**
   - Assert: output ends with `\n`

3. **should use section separators (=== lines)**
   - Assert: "=".repeat(48) for section dividers

4. **should handle only GLOBAL rules (no matched)**
   - GLOBAL only
   - Assert: valid output with just GLOBAL

5. **should handle only CONTEXT rules**
   - CONTEXT only with bracket data
   - Assert: valid output with context bracket header
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/injector.test.ts --testNamePattern="edge cases|output formatting" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Eleven edge case and formatting tests pass</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
npx jest tests/javascript/unit/injector.test.ts --coverage --coverageReporters=text
```

Expected: All 48+ tests pass, coverage of buildCarlInjection function >90%
</verification>

<success_criteria>
- Test file `tests/javascript/unit/injector.test.ts` exists
- All describe blocks populated with tests
- Tests pass: single domain (6), multiple domains (5), global/always-on (7), command (6), context brackets (8), edge cases (6), formatting (5)
- Total test count >= 43
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-unit-tests-parsing-matching/08-05-SUMMARY.md`
</output>
