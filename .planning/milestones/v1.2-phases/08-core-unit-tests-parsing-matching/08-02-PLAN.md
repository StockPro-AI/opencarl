---
phase: 08-core-unit-tests-parsing-matching
plan: 02
type: tdd
wave: 1
depends_on: ["08-01"]
files_modified:
  - tests/javascript/unit/validate.test.ts
autonomous: true
requirements: [UNIT-01]
must_haves:
  truths:
    - "Developer can verify manifest parser handles valid single-domain manifests"
    - "Developer can verify manifest parser handles valid multi-domain manifests"
    - "Developer can verify manifest parser handles empty manifests"
    - "Developer can verify manifest parser handles malformed manifests"
    - "Developer can verify STATE and ALWAYS_ON boolean parsing"
  artifacts:
    - path: "tests/javascript/unit/validate.test.ts"
      provides: "Unit tests for manifest parser"
      contains: "describe('parseManifest')"
      min_lines: 150
  key_links:
    - from: "tests/javascript/unit/validate.test.ts"
      to: "src/carl/validate.ts"
      via: "import"
      pattern: "import.*parseManifest"
---

<objective>
Write comprehensive unit tests for the manifest parser in `validate.ts`.

Purpose: Verify parseManifest handles all valid, edge, and error cases for CARL manifest files.
Output: Test file `validate.test.ts` with full coverage of manifest parsing logic.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-unit-tests-parsing-matching/08-CONTEXT.md

@src/carl/validate.ts
@src/carl/types.ts
@tests/helpers/manifest-factory.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Create manifest parser test file with describe structure</name>
  <files>tests/javascript/unit/validate.test.ts</files>
  <action>
Create test file with BDD-style nested describe blocks:

```
describe('validate.ts', () => {
  describe('parseManifest', () => {
    describe('valid manifests', () => {
      // Tests for valid single/multi-domain manifests
    });
    describe('edge cases', () => {
      // Empty, whitespace, comments
    });
    describe('malformed input', () => {
      // Invalid lines, missing equals, invalid booleans
    });
    describe('STATE and ALWAYS_ON parsing', () => {
      // Boolean value variations
    });
  });
});
```

Import: `parseManifest`, `ParsedManifest` from `'../../../src/carl/validate'`
Import: `createTestManifestPath` from `'../../helpers/manifest-factory'`
Import Jest globals: `describe`, `it`, `expect`, `beforeEach`, `afterEach`

Create temp directory cleanup in beforeEach/afterEach hooks for file-based tests.
  </action>
  <verify>
    <automated>npx tsc --noEmit tests/javascript/unit/validate.test.ts 2>&1 | head -5</automated>
  </verify>
  <done>Test file structure created with nested describe blocks</done>
</task>

<task type="auto">
  <name>task 2: Write valid manifest parsing tests</name>
  <files>tests/javascript/unit/validate.test.ts</files>
  <action>
Add tests to 'valid manifests' describe block:

1. **should parse valid single-domain manifest correctly**
   - Use inline content: `DEVELOPMENT_STATE=active\nDEVELOPMENT_RECALL=fix bug, write code\n...`
   - Write to temp file, call parseManifest
   - Assert: `result.isValid === true`, `result.domains.DEVELOPMENT.state === true`
   - Assert: `result.domains.DEVELOPMENT.recall` contains expected keywords

2. **should parse valid multi-domain manifest correctly**
   - Use inline content with DEVELOPMENT, CONTENT, CONTEXT
   - Assert: all domains present with correct config
   - Assert: `result.domains` has 3 keys

3. **should parse DEVMODE flag correctly**
   - Test DEVMODE=true and DEVMODE=false
   - Assert: `result.devmode` matches expected

4. **should parse GLOBAL_EXCLUDE keywords**
   - Include `GLOBAL_EXCLUDE=secret, password, token`
   - Assert: `result.globalExclude` contains parsed keywords

Use inline test data (not fixtures) for these tests per user decision.
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/validate.test.ts --testNamePattern="valid manifests" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Four tests for valid manifest parsing pass</done>
</task>

<task type="auto">
  <name>task 3: Write edge case tests</name>
  <files>tests/javascript/unit/validate.test.ts</files>
  <action>
Add tests to 'edge cases' describe block:

1. **should return invalid for empty manifest**
   - Empty file content
   - Assert: `result.isValid === false`
   - Assert: warnings include "no recognized entries"

2. **should ignore blank lines and whitespace**
   - Content with multiple blank lines between entries
   - Assert: parsing succeeds, blank lines ignored

3. **should ignore comment lines starting with hash**
   - Include `# this is a comment` lines
   - Assert: comments ignored, valid entries parsed

4. **should trim whitespace from keys and values**
   - `DEVELOPMENT_STATE  =  active  ` (extra spaces)
   - Assert: trimmed correctly

5. **should handle RECALL/EXCLUDE with extra commas**
   - `DEVELOPMENT_RECALL=fix bug, , write code, ,` (empty entries)
   - Assert: empty entries filtered out

6. **should handle file that does not exist**
   - Call parseManifest with non-existent path
   - Assert: `result.isValid === false`
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/validate.test.ts --testNamePattern="edge cases" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Six edge case tests pass</done>
</task>

<task type="auto">
  <name>task 4: Write malformed input tests</name>
  <files>tests/javascript/unit/validate.test.ts</files>
  <action>
Add tests to 'malformed input' describe block:

1. **should warn on lines without equals sign**
   - Include `this is not valid` line
   - Assert: warning added, continues parsing

2. **should warn on unknown manifest keys**
   - Include `UNKNOWN_FIELD=value` line
   - Assert: warning about unknown key

3. **should warn on invalid STATE boolean value**
   - `DEVELOPMENT_STATE=maybe` (invalid boolean)
   - Assert: warning about invalid value, state remains false

4. **should warn on invalid ALWAYS_ON boolean value**
   - `DEVELOPMENT_ALWAYS_ON=sure` (invalid boolean)
   - Assert: warning about invalid value, alwaysOn remains false

5. **should handle empty key (line starting with =)**
   - `=value` line
   - Assert: warning or graceful handling

6. **should accumulate multiple warnings**
   - Multiple malformed lines
   - Assert: all warnings captured
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/validate.test.ts --testNamePattern="malformed" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Six malformed input tests pass</done>
</task>

<task type="auto">
  <name>task 5: Write STATE and ALWAYS_ON parsing tests</name>
  <files>tests/javascript/unit/validate.test.ts</files>
  <action>
Add tests to 'STATE and ALWAYS_ON parsing' describe block:

1. **should parse STATE=true variations**
   - Test: `true`, `TRUE`, `yes`, `YES`, `1`, `active`, `on`
   - Assert: all parse to `state: true`

2. **should parse STATE=false variations**
   - Test: `false`, `FALSE`, `no`, `NO`, `0`, `inactive`, `off`
   - Assert: all parse to `state: false`

3. **should parse ALWAYS_ON=true variations**
   - Same truthy values as STATE
   - Assert: all parse to `alwaysOn: true`

4. **should parse ALWAYS_ON=false variations**
   - Same falsy values as STATE
   - Assert: all parse to `alwaysOn: false`

5. **should default domain state to false when not specified**
   - Domain with only RECALL/EXCLUDE, no STATE
   - Assert: `state: false`

6. **should default alwaysOn to false when not specified**
   - Domain without ALWAYS_ON line
   - Assert: `alwaysOn: false`
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/validate.test.ts --testNamePattern="STATE and ALWAYS_ON" --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Six boolean parsing tests pass</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
npx jest tests/javascript/unit/validate.test.ts --coverage --coverageReporters=text
```

Expected: All 22+ tests pass, coverage of parseManifest function >90%
</verification>

<success_criteria>
- Test file `tests/javascript/unit/validate.test.ts` exists
- All describe blocks populated with tests
- Tests pass: valid manifests (4), edge cases (6), malformed (6), booleans (6)
- Total test count >= 22
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-unit-tests-parsing-matching/08-02-SUMMARY.md`
</output>
