---
phase: 03-session-stability-live-updates
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/carl/context-brackets.ts
  - src/carl/injector.ts
  - src/carl/loader.ts
  - src/carl/types.ts
  - src/carl/validate.ts
  - src/integration/plugin-hooks.ts
autonomous: true
requirements:
  - CTXT-02
  - CTXT-03

must_haves:
  truths:
    - "Context bracket selection is computed from token headroom and only affects CONTEXT rules."
    - "CRITICAL bracket emits a warning while using DEPLETED context rules."
    - "After compaction, context bracket selection and rule matching continue using the remaining transcript signals with no special resets or warnings."
  artifacts:
    - path: "src/carl/context-brackets.ts"
      provides: "Context remaining + bracket resolution utilities"
    - path: "src/carl/loader.ts"
      provides: "Context domain rules organized by bracket"
    - path: "src/carl/injector.ts"
      provides: "Context bracket selection and critical warning output"
  key_links:
    - from: "src/integration/plugin-hooks.ts"
      to: "src/carl/context-brackets.ts"
      via: "session messages -> bracket"
      pattern: "session\.messages"
    - from: "src/carl/injector.ts"
      to: "CONTEXT domain payload"
      via: "selected bracket rules"
      pattern: "CONTEXT"
---

<objective>
Add context bracket selection to keep rule behavior consistent in long sessions.

Purpose: Apply CONTEXT rules based on token headroom without altering other domains.
Output: Context bracket utilities and bracket-aware injection.
</objective>

<execution_context>
@/Users/kris/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/kris/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-stability-live-updates/03-CONTEXT.md
@.planning/phases/03-session-stability-live-updates/03-RESEARCH.md
@src/carl/loader.ts
@src/carl/injector.ts
@src/carl/signal-store.ts
@src/carl/types.ts
@src/integration/plugin-hooks.ts
@hooks/carl-hook.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement context bracket selection and CONTEXT rule filtering</name>
  <files>src/carl/context-brackets.ts, src/carl/types.ts, src/carl/validate.ts, src/carl/loader.ts, src/carl/injector.ts, src/integration/plugin-hooks.ts</files>
  <action>
Add a context bracket helper that computes remaining context from session token telemetry and maps it to FRESH/MODERATE/DEPLETED/CRITICAL thresholds (≥60/≥40/≥25/<25). Parse CONTEXT domain files into bracket flags + per-bracket rule lists (mirroring hooks/carl-hook.py), and include this structured data in CarlRuleDomainPayload. Update buildCarlInjection to accept bracket inputs and select only the active bracket rules for CONTEXT, using DEPLETED rules when bracket is CRITICAL and emitting a critical warning line in the injection. In plugin hooks, read token telemetry via client.session.messages() (or plugin-provided session token data if available), compute contextRemaining + bracket, and pass them into injection. Ensure compaction continuity by not adding any compaction-specific resets, warnings, or cache clears; bracket selection and rule matching must use whatever transcript signals remain after compaction (including tool/path signals if still present).
  </action>
  <verify>bun run scripts/carl-injection-smoke.ts</verify>
  <done>CONTEXT rules are filtered by bracket and CRITICAL emits a warning while using DEPLETED rules.</done>
</task>

</tasks>

<verification>
- Run bun run scripts/carl-injection-smoke.ts to confirm no regression in injection/matching behavior.
- In an OpenCode session, exhaust context to cross bracket thresholds and confirm only CONTEXT rules change with a CRITICAL warning at <25%.
</verification>

<success_criteria>
- CONTEXT bracket selection is computed from token headroom and applied only to CONTEXT rules.
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-stability-live-updates/03-02-SUMMARY.md`
</output>
