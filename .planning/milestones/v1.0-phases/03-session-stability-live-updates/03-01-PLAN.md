---
phase: 03-session-stability-live-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/carl/loader.ts
  - src/carl/rule-cache.ts
  - src/carl/session-overrides.ts
  - src/carl/types.ts
  - src/carl/validate.ts
  - src/integration/plugin-hooks.ts
autonomous: true
requirements:
  - RULE-04
  - RULE-05

must_haves:
  truths:
    - "Project rules apply only when project opt-in is enabled in config and a project .carl/ directory exists; invalid project edits disable project rules."
    - "Editing files under .carl/ or .opencode/carl/ refreshes rule discovery on the next turn."
    - "Invalid project reload warnings appear once per session."
    - "Per-session override files can disable domains for the active session via inherit/true/false flags."
  artifacts:
    - path: "src/carl/rule-cache.ts"
      provides: "Session-scoped rule discovery cache with dirty tracking"
    - path: "src/carl/loader.ts"
      provides: "Project validity classification and filtered payloads"
    - path: "src/carl/session-overrides.ts"
      provides: "Per-session override parsing and domain gating"
    - path: "src/integration/plugin-hooks.ts"
      provides: "Reload invalidation and per-session warning guard"
  key_links:
    - from: "src/integration/plugin-hooks.ts"
      to: "src/carl/rule-cache.ts"
      via: "file.watcher.updated -> markRulesDirty()"
      pattern: "file\.watcher\.updated"
    - from: "src/integration/plugin-hooks.ts"
      to: "src/carl/session-overrides.ts"
      via: "session id + .carl/sessions/{session_id}.json"
      pattern: "sessions"
    - from: "src/integration/plugin-hooks.ts"
      to: "src/carl/loader.ts"
      via: "config opt-in passed to loadCarlRules"
      pattern: "config"
    - from: "src/integration/plugin-hooks.ts"
      to: "src/carl/loader.ts"
      via: "rule cache refresh"
      pattern: "loadCarlRules"
    - from: "src/integration/plugin-hooks.ts"
      to: "client.app.log"
      via: "warn once per session"
      pattern: "warn.*session"
---

<objective>
Implement project opt-in gating with live reload invalidation and per-session overrides so CARL rules stay stable across turns without restart.

Purpose: Apply project rules only when explicitly opted in and valid, apply per-session domain overrides, and reload cleanly on .carl edits.
Output: Rule cache module, project validity status, per-session overrides, and session-scoped warning guard.
</objective>

<execution_context>
@/Users/kris/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/kris/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-stability-live-updates/03-CONTEXT.md
@.planning/phases/03-session-stability-live-updates/03-RESEARCH.md
@src/carl/loader.ts
@src/carl/validate.ts
@src/carl/types.ts
@src/integration/plugin-hooks.ts
@hooks/carl-hook.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rule cache with live reload invalidation</name>
  <files>src/carl/rule-cache.ts, src/integration/plugin-hooks.ts</files>
  <action>
Create a session-safe rule cache that wraps loadCarlRules and exposes a dirty flag. Add API like getCachedRules(), markRulesDirty(), and resetRulesCache() (if needed) and store lastLoadedAt plus the most recent discovery result. In plugin hooks, register file.watcher.updated and mark the cache dirty when the updated path is inside a .carl/ directory or the .opencode/carl fallback. In experimental.chat.system.transform, replace direct loadCarlRules() with the cached lookup so reloads happen only when dirty, and changes apply on the next turn.
  </action>
  <verify>bun run scripts/carl-injection-smoke.ts</verify>
  <done>Rule discovery uses the cache and reloads only when .carl files change.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce project opt-in gating and warn once per session</name>
  <files>src/carl/loader.ts, src/carl/types.ts, src/carl/validate.ts, src/carl/rule-cache.ts, src/integration/plugin-hooks.ts</files>
  <action>
Extend discovery warnings to carry source scope (project/global/fallback) so project-specific invalid changes can be detected. Add a config-backed opt-in gate (e.g., a boolean flag from OpenCode config hook) and require BOTH opt-in enabled and a project .carl/ directory before project rules are eligible. In loadCarlRules, accept an opt-in flag (or options) and short-circuit project discovery when opt-in is false; also detect invalid project manifests or domain parse warnings and mark projectStatus="invalid"; when invalid, drop all project domains/payloads from the discovery result so only global/fallback rules remain. Add project status and project warnings to CarlRuleDiscoveryResult. In plugin hooks, wire the config hook to store the opt-in flag for the session/plugin and pass it into the cached loadCarlRules path. Keep a per-session warning guard (Set of session IDs) and emit a single warning via client.app.log (or console.warn if client logging is unavailable) when projectStatus is invalid; clear the guard when project rules become valid again. This enforces RULE-04 opt-in (config + directory presence) and RULE-05 reload warnings on next turn.
  </action>
  <verify>bun run scripts/carl-injection-smoke.ts</verify>
  <done>Invalid project changes disable project rules and only warn once per session; valid project rules apply normally.</done>
</task>

<task type="auto">
  <name>Task 3: Apply per-session overrides to domain activation</name>
  <files>src/carl/session-overrides.ts, src/carl/types.ts, src/carl/loader.ts, src/integration/plugin-hooks.ts, src/carl/rule-cache.ts</files>
  <action>
Add a per-session override loader that reads `.carl/sessions/{session_id}.json` (if present) and parses domain flags as inherit/true/false. Extend CarlRuleDiscoveryResult (or loader options) to include session overrides and apply them when computing which domains are active for the current session. In plugin hooks, capture the active session id and pass it into the cached loadCarlRules path so the session override file is used per session, without affecting global cache entries for other sessions. Ensure per-session overrides can only disable domains, not enable missing domains, and default to inherit when no override file is present.
  </action>
  <verify>bun run scripts/carl-injection-smoke.ts</verify>
  <done>Domains marked false in .carl/sessions/{session_id}.json are excluded only for that session; other sessions remain unaffected.</done>
</task>

</tasks>

<verification>
- Run bun run scripts/carl-injection-smoke.ts to ensure injection flow still executes.
- In an OpenCode session, edit a project .carl/manifest and confirm the warning appears once and project rules stop loading until fixed.
- Create a .carl/sessions/{session_id}.json override for one domain and confirm only that session disables the domain.
</verification>

<success_criteria>
- Project rules are excluded when project .carl manifests or domain files are invalid.
- .carl edits trigger a cache refresh and apply on the next turn without restart.
- Per-session overrides disable domains only for the active session.
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-stability-live-updates/03-01-SUMMARY.md`
</output>
