---
phase: 02-matching-injection-command-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/carl/types.ts
  - src/carl/matcher.ts
  - src/carl/signal-store.ts
  - scripts/carl-matching-parity-smoke.ts
autonomous: true
requirements: [MATCH-01, MATCH-02, CTXT-01, SAFE-01]

must_haves:
  truths:
    - "Recall keyword matching follows existing CARL/Claude behavior for the same prompt text."
    - "EXCLUDE handling blocks rule selection with the same precedence as current CARL semantics."
    - "Tool usage and file path context become searchable matching signals for subsequent turns."
  artifacts:
    - path: "src/carl/matcher.ts"
      provides: "Parity matcher with exclusion-first selection"
      exports: ["matchDomainsForTurn"]
    - path: "src/carl/signal-store.ts"
      provides: "Per-session signal capture for prompt/tool/path tokens"
      exports: ["recordPromptSignals", "recordToolSignals", "getSessionSignals"]
    - path: "src/carl/types.ts"
      provides: "Typed contracts for matching inputs and results"
    - path: "scripts/carl-matching-parity-smoke.ts"
      provides: "Executable parity checks for matcher behavior"
  key_links:
    - from: "src/carl/matcher.ts"
      to: "src/carl/signal-store.ts"
      via: "consumes normalized session signals"
      pattern: "getSessionSignals"
    - from: "src/carl/matcher.ts"
      to: "src/carl/types.ts"
      via: "typed match request/result contracts"
      pattern: "Match"
    - from: "scripts/carl-matching-parity-smoke.ts"
      to: "src/carl/matcher.ts"
      via: "fixture-driven parity verification"
      pattern: "matchDomainsForTurn"
---

<objective>
Build a dedicated parity matcher and session signal store that replicate current CARL selection semantics.

Purpose: Prevent semantic drift by implementing exclusion-first matching and context-signal capture exactly where Phase 2 requires parity.
Output: Typed matcher and signal-store modules plus a parity smoke script.
</objective>

<execution_context>
@/Users/kris/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/kris/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-matching-injection-command-parity/02-CONTEXT.md
@.planning/phases/02-matching-injection-command-parity/02-RESEARCH.md
@hooks/carl-hook.py
@src/carl/validate.ts
@src/carl/loader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CARL types for parity matching inputs and outputs</name>
  <files>src/carl/types.ts</files>
  <action>
Define explicit TypeScript contracts for parity matching so later injection and command plans can reuse one source of truth.

Add input/output types covering:
- Prompt text tokens used for recall/exclude matching.
- Session tool and file-path tokens (normalized, lowercased).
- Per-domain match results (matched keywords, excluded keywords, exclusion source).
- Global exclusion outcomes that short-circuit matching.

Keep naming aligned with existing CARL language (`recall`, `exclude`, `alwaysOn`, `globalExclude`) to reduce parity ambiguity.
  </action>
  <verify>node -e "const fs=require('fs'); const t=fs.readFileSync('src/carl/types.ts','utf8'); console.log(t.includes('globalExclude') && t.includes('matchedKeywords'));"</verify>
  <done>`src/carl/types.ts` exposes stable typed contracts for parity matching and exclusion outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement per-session prompt/tool/path signal capture</name>
  <files>src/carl/signal-store.ts</files>
  <action>
Create a small in-memory signal store keyed by session id that records and normalizes matching inputs across turns.

Implementation requirements:
- Record prompt-derived tokens for each turn without mutating original prompt text.
- Record tool usage signals from hook payloads, including tool names and argument-derived path fragments.
- Normalize paths into searchable tokens (full path, basename, and path segments).
- Provide deterministic read APIs for current session signals consumed by matcher logic.
- Include bounded retention to avoid unbounded growth (e.g., keep last N entries per signal class).

Do not add persistence or compaction behavior in this phase; keep scope to CTXT-01 capture only.
  </action>
  <verify>node -e "const fs=require('fs'); const t=fs.readFileSync('src/carl/signal-store.ts','utf8'); console.log(t.includes('recordToolSignals') && t.includes('getSessionSignals'));"</verify>
  <done>Signal store captures prompt, tool, and file-path signals per session with deterministic retrieval for matching.</done>
</task>

<task type="auto">
  <name>Task 3: Implement exclusion-first parity matcher and smoke verification</name>
  <files>src/carl/matcher.ts, scripts/carl-matching-parity-smoke.ts</files>
  <action>
Implement `matchDomainsForTurn` using existing CARL behavior from `hooks/carl-hook.py` as the parity fixture source.

Required behavior:
- Enforce global exclusion first; when matched, return no domain matches.
- Enforce per-domain exclusion before checking recall keywords.
- Support recall matching against combined prompt + tool + file-path signal tokens.
- Preserve current CARL matching permissiveness (substring-compatible behavior unless a fixture proves otherwise).
- Produce deterministic output ordering for matched/excluded domains (stable alphabetical order).

Add `scripts/carl-matching-parity-smoke.ts` that executes representative parity fixtures (recall-only, recall+exclude overlap, global exclude short-circuit, tool/path-triggered match) and exits non-zero on failure.
  </action>
  <verify>bun run scripts/carl-matching-parity-smoke.ts</verify>
  <done>Parity smoke script passes and confirms exclusion precedence plus prompt/tool/path matching behavior.</done>
</task>

</tasks>

<verification>
- Matcher applies global and domain exclusions before recall selection.
- Tool/path signals influence matching decisions in parity checks.
- Smoke script exits successfully for all parity fixtures.
</verification>

<success_criteria>
- Matching semantics for recall and exclusion align with current CARL outcomes for equivalent inputs.
- Session signal capture supports Phase 2 matching requirements without introducing new behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-matching-injection-command-parity/02-01-SUMMARY.md`
</output>
