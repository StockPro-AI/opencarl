---
phase: 02-matching-injection-command-parity
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/carl/loader.ts
  - src/carl/injector.ts
  - src/integration/plugin-hooks.ts
  - .opencode/plugins/carl.ts
  - scripts/carl-injection-smoke.ts
autonomous: true
requirements: [MATCH-03, MATCH-04, MATCH-05]

must_haves:
  truths:
    - "Matched CARL rules are injected into system context before the model turn."
    - "Always-on domains are merged with matched domains every turn."
    - "Injected output order is deterministic for equivalent inputs."
  artifacts:
    - path: "src/carl/injector.ts"
      provides: "Deterministic CARL injection composition and rendering"
      exports: ["buildCarlInjection"]
    - path: "src/carl/loader.ts"
      provides: "Rule payloads for always-on and matched-domain rendering"
      exports: ["loadCarlRules"]
    - path: "src/integration/plugin-hooks.ts"
      provides: "OpenCode system-transform hook wiring"
      exports: ["createCarlPluginHooks"]
    - path: ".opencode/plugins/carl.ts"
      provides: "Plugin entrypoint delegating to hook wiring"
    - path: "scripts/carl-injection-smoke.ts"
      provides: "Pre-turn injection parity smoke checks"
  key_links:
    - from: "src/integration/plugin-hooks.ts"
      to: "src/carl/injector.ts"
      via: "experimental.chat.system.transform"
      pattern: "experimental.chat.system.transform"
    - from: "src/carl/injector.ts"
      to: "src/carl/loader.ts"
      via: "domain rules + always-on metadata"
      pattern: "loadCarlRules"
    - from: ".opencode/plugins/carl.ts"
      to: "src/integration/plugin-hooks.ts"
      via: "plugin hook registration"
      pattern: "createCarlPluginHooks"
---

<objective>
Wire deterministic CARL system prompt injection into OpenCode's pre-turn transform hook.

Purpose: Ensure matched and always-on rules are injected at the correct lifecycle point with stable ordering.
Output: Injection engine, OpenCode hook wiring, and pre-turn smoke verification.
</objective>

<execution_context>
@/Users/kris/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/kris/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-matching-injection-command-parity/02-CONTEXT.md
@.planning/phases/02-matching-injection-command-parity/02-RESEARCH.md
@.planning/phases/02-matching-injection-command-parity/02-01-SUMMARY.md
@hooks/carl-hook.py
@.opencode/plugins/carl.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend loader output to supply injection-ready rule payloads</name>
  <files>src/carl/loader.ts</files>
  <action>
Update `loadCarlRules` return data so injection can operate without re-parsing files in downstream modules.

Required changes:
- Include per-domain rule lists alongside source metadata.
- Surface per-domain `alwaysOn` and state metadata derived from manifest parsing.
- Keep project-over-global precedence and fallback behavior from Phase 1 unchanged.
- Preserve warning-first behavior; invalid rules remain non-fatal.

Do not implement matching or command logic in loader; keep loader as source-resolution + parsed payload provider.
  </action>
  <verify>node -e "const fs=require('fs'); const t=fs.readFileSync('src/carl/loader.ts','utf8'); console.log(t.includes('alwaysOn') && t.includes('rules'));"</verify>
  <done>Loader returns injection-ready domain payloads while preserving existing discovery semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic CARL injection composer</name>
  <files>src/carl/injector.ts</files>
  <action>
Create `buildCarlInjection` to compose and render CARL output from selected domains.

Parity requirements:
- Input includes matched domains (from matcher), explicit command domains (if present), and always-on domains.
- Dedupe domains while preserving deterministic class ordering.
- Render output as a stable CARL block suitable for system-level insertion.
- Keep ordering deterministic and testable: explicit command selections first, then always-on domains, then matched recall domains; stable alphabetical order within each class unless a parity fixture requires otherwise.

Keep rendering and selection composition in this module; hook wiring should call it as a pure function.
  </action>
  <verify>node -e "const fs=require('fs'); const t=fs.readFileSync('src/carl/injector.ts','utf8'); console.log(t.includes('buildCarlInjection') && t.includes('always-on'));"</verify>
  <done>Injector produces deterministic CARL output for equivalent selected-domain inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pre-turn system transform hook and add injection smoke test</name>
  <files>src/integration/plugin-hooks.ts, .opencode/plugins/carl.ts, scripts/carl-injection-smoke.ts</files>
  <action>
Implement OpenCode hook integration so injection is applied before each model turn.

Implementation steps:
- Add `src/integration/plugin-hooks.ts` that registers `experimental.chat.system.transform` and appends CARL injection to system content.
- Update `.opencode/plugins/carl.ts` to delegate hook creation/registration through `plugin-hooks.ts` (keep startup discovery summary logging).
- Add `scripts/carl-injection-smoke.ts` that runs representative fixtures and verifies generated CARL blocks are deterministic and non-empty when expected.

Honor locked parity decisions: this hook path must preserve observable CARL behavior and avoid injecting as user content.
  </action>
  <verify>bun run scripts/carl-injection-smoke.ts</verify>
  <done>Plugin applies CARL injection via `experimental.chat.system.transform` before model turns with deterministic output.</done>
</task>

</tasks>

<verification>
- System-transform hook appends CARL content before provider payload generation.
- Always-on and matched domain sets are composed together and deduped.
- Injection smoke script confirms stable ordering across repeated runs.
</verification>

<success_criteria>
- Pre-turn system injection is active and deterministic for matched + always-on rules.
- Rule composition logic stays parity-aligned and isolated from hook plumbing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-matching-injection-command-parity/02-02-SUMMARY.md`
</output>
