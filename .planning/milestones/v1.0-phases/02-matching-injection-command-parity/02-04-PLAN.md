---
phase: 02-matching-injection-command-parity
plan: 04
type: execute
wave: 4
depends_on: ["02", "03"]
files_modified:
  - scripts/carl-phase2-parity.ts
  - scripts/fixtures/phase2/matching.json
  - scripts/fixtures/phase2/injection.json
  - scripts/fixtures/phase2/commands.json
autonomous: true
requirements: [MATCH-01, MATCH-03, MATCH-04, CMD-01, CMD-04, SAFE-01]

must_haves:
  truths:
    - "Phase 2 parity cases can be executed repeatedly with stable pass/fail outcomes."
    - "Critical parity behaviors (exclude precedence, deterministic ordering, command parity) are covered by executable fixtures."
    - "Regression checks fail loudly when behavior drifts from CARL baseline semantics."
  artifacts:
    - path: "scripts/carl-phase2-parity.ts"
      provides: "End-to-end phase parity runner"
    - path: "scripts/fixtures/phase2/matching.json"
      provides: "Matching and exclusion parity fixtures"
    - path: "scripts/fixtures/phase2/injection.json"
      provides: "Injection composition and ordering fixtures"
    - path: "scripts/fixtures/phase2/commands.json"
      provides: "Star/slash command parity fixtures"
  key_links:
    - from: "scripts/carl-phase2-parity.ts"
      to: "src/carl/matcher.ts"
      via: "matching fixture assertions"
      pattern: "matchDomainsForTurn"
    - from: "scripts/carl-phase2-parity.ts"
      to: "src/carl/injector.ts"
      via: "injection ordering assertions"
      pattern: "buildCarlInjection"
    - from: "scripts/carl-phase2-parity.ts"
      to: "src/carl/command-parity.ts"
      via: "command parity assertions"
      pattern: "resolveCarlCommandSignals"
---

<objective>
Create a single Phase 2 parity regression harness that verifies matching, injection, and command behavior together.

Purpose: Lock parity expectations into executable fixtures so future changes cannot silently drift from CARL semantics.
Output: Fixture set and one command to validate all Phase 2 parity guarantees.
</objective>

<execution_context>
@/Users/kris/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/kris/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-matching-injection-command-parity/02-CONTEXT.md
@.planning/phases/02-matching-injection-command-parity/02-RESEARCH.md
@.planning/phases/02-matching-injection-command-parity/02-02-SUMMARY.md
@.planning/phases/02-matching-injection-command-parity/02-03-SUMMARY.md
@hooks/carl-hook.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author canonical Phase 2 parity fixture sets</name>
  <files>scripts/fixtures/phase2/matching.json, scripts/fixtures/phase2/injection.json, scripts/fixtures/phase2/commands.json</files>
  <action>
Create explicit fixture files encoding expected parity outcomes using current CARL behavior as baseline.

Fixture scope:
- `matching.json`: recall matches, per-domain exclusions, global exclusion short-circuit, tool/path-triggered matches.
- `injection.json`: matched+always-on composition, deterministic ordering checks, dedupe cases.
- `commands.json`: `*carl`, custom `*domain` commands, multi-command prompts, `/carl` fallback equivalence.

Keep fixtures compact and human-readable; each case should include input, expected selection/composition, and expected ordering.
  </action>
  <verify>node -e "const fs=require('fs'); const a=JSON.parse(fs.readFileSync('scripts/fixtures/phase2/matching.json','utf8')); const b=JSON.parse(fs.readFileSync('scripts/fixtures/phase2/injection.json','utf8')); const c=JSON.parse(fs.readFileSync('scripts/fixtures/phase2/commands.json','utf8')); console.log(Boolean(a.cases?.length && b.cases?.length && c.cases?.length));"</verify>
  <done>Fixture files exist with executable parity expectations for matching, injection, and commands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement unified Phase 2 parity runner</name>
  <files>scripts/carl-phase2-parity.ts</files>
  <action>
Implement a single script that loads Phase 2 fixtures and validates outputs from matcher, injector, and command resolver modules.

Runner requirements:
- Execute all fixture groups in one run with deterministic ordering.
- Print concise pass/fail summary by category and case id.
- Exit non-zero on first failure category summary (CI-friendly behavior).
- Include minimal diagnostics showing actual vs expected for failed cases.

Use existing Phase 2 module exports; do not duplicate matching/injection/command logic in the runner.
  </action>
  <verify>bun run scripts/carl-phase2-parity.ts</verify>
  <done>Unified parity script validates all fixture categories and fails clearly on semantic drift.</done>
</task>

<task type="auto">
  <name>Task 3: Add guard checks for key parity links</name>
  <files>scripts/carl-phase2-parity.ts</files>
  <action>
Augment parity runner output with explicit checks for the highest-risk regressions identified in research.

Must include guard assertions for:
- Exclusion precedence remains global-first then domain-level.
- Injection order remains deterministic for repeated runs and identical inputs.
- Star/slash command parity does not diverge for CARL help mode.

Report these guard checks in a dedicated "critical links" section at the end of script output.
  </action>
  <verify>bun run scripts/carl-phase2-parity.ts</verify>
  <done>Parity output explicitly confirms critical links and blocks release when those links regress.</done>
</task>

</tasks>

<verification>
- Fixture files cover Phase 2 requirement-critical behaviors.
- One script command validates matcher, injector, and command parity end-to-end.
- Critical-link guard checks are present and enforced.
</verification>

<success_criteria>
- Phase 2 can be re-verified with a single deterministic parity command.
- Regressions in matching, injection order, or command parity are caught immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/02-matching-injection-command-parity/02-04-SUMMARY.md`
</output>
