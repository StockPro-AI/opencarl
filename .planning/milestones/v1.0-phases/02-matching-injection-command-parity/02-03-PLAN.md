---
phase: 02-matching-injection-command-parity
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/carl/command-parity.ts
  - src/carl/help-text.ts
  - src/integration/plugin-hooks.ts
  - scripts/carl-command-parity-smoke.ts
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03, CMD-04]

must_haves:
  truths:
    - "`*carl` and `*domain` star-command triggers map to CARL command-mode rules when present in prompt text."
    - "`/carl` command fallback routes through the same command resolver behavior as star commands."
    - "CARL help/manager guidance content matches the existing project docs sources."
  artifacts:
    - path: "src/carl/command-parity.ts"
      provides: "Shared resolver for star and slash command parity"
      exports: ["resolveCarlCommandSignals"]
    - path: "src/carl/help-text.ts"
      provides: "Help/manager guidance payload sourced from CARL docs"
      exports: ["buildCarlHelpGuidance"]
    - path: "src/integration/plugin-hooks.ts"
      provides: "Command and slash fallback hook integration"
    - path: "scripts/carl-command-parity-smoke.ts"
      provides: "Parity smoke checks for command behaviors"
  key_links:
    - from: "src/integration/plugin-hooks.ts"
      to: "src/carl/command-parity.ts"
      via: "command.execute.before and prompt parsing"
      pattern: "command.execute.before"
    - from: "src/carl/command-parity.ts"
      to: "src/carl/help-text.ts"
      via: "help/manager command mapping"
      pattern: "carl"
    - from: "scripts/carl-command-parity-smoke.ts"
      to: "src/carl/command-parity.ts"
      via: "fixture-based command parity assertions"
      pattern: "resolveCarlCommandSignals"
---

<objective>
Implement command-mode parity for `*carl`, `*domain`, and `/carl` fallback using a single resolver path.

Purpose: Preserve CARL command UX semantics across OpenCode input surfaces without split-brain behavior.
Output: Shared command resolver, docs-backed help guidance builder, and command parity smoke checks.
</objective>

<execution_context>
@/Users/kris/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/kris/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-matching-injection-command-parity/02-CONTEXT.md
@.planning/phases/02-matching-injection-command-parity/02-RESEARCH.md
@.planning/phases/02-matching-injection-command-parity/02-02-SUMMARY.md
@.carl-template/commands
@resources/skills/carl-help/CARL-OVERVIEW.md
@resources/commands/carl/manager.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared star/slash command resolver</name>
  <files>src/carl/command-parity.ts</files>
  <action>
Create `resolveCarlCommandSignals` as the single command-parity resolver used by both prompt-star detection and slash fallback.

Resolver requirements:
- Parse one or more `*token` commands from user text and normalize to command keys.
- Treat `*carl` as the CARL help/manager trigger with priority-preserving behavior.
- Support custom `*domain` command tokens using COMMANDS domain rule groups.
- Return deterministic command selections and unresolved-command diagnostics for smoke tests.

Preserve existing CARL semantics from `hooks/carl-hook.py`; do not introduce new command syntax in this phase.
  </action>
  <verify>node -e "const fs=require('fs'); const t=fs.readFileSync('src/carl/command-parity.ts','utf8'); console.log(t.includes('resolveCarlCommandSignals') && t.includes('*carl'));"</verify>
  <done>Single resolver handles star-command parsing for CARL and custom command domains deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Add `/carl` fallback hook integration using shared resolver</name>
  <files>src/integration/plugin-hooks.ts</files>
  <action>
Extend plugin hook wiring so slash commands and star commands converge on the same parity path.

Implementation requirements:
- Use `command.execute.before` to intercept `/carl` invocations and map them to CARL command-mode behavior.
- Ensure slash fallback injects equivalent command rules/help guidance as `*carl` when star interception is unavailable.
- Reuse `resolveCarlCommandSignals` for both pathways; no duplicate command parsing logic.
- Preserve existing pre-turn injection behavior added in Plan 02.

Keep fallback scoped to `/carl`; do not add extra slash command surface area in this phase.
  </action>
  <verify>node -e "const fs=require('fs'); const t=fs.readFileSync('src/integration/plugin-hooks.ts','utf8'); console.log(t.includes('command.execute.before') && t.includes('/carl'));"</verify>
  <done>`/carl` fallback routes through the same resolver and yields parity-equivalent command behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Source help guidance from docs and validate command parity</name>
  <files>src/carl/help-text.ts, scripts/carl-command-parity-smoke.ts</files>
  <action>
Add docs-backed help text assembly and verify command behavior across star/slash flows.

Implementation steps:
- Create `src/carl/help-text.ts` that derives CARL help/manager guidance from `resources/skills/carl-help/CARL-OVERVIEW.md` and `resources/commands/carl/manager.md` (source-of-truth, no duplicate prose constants).
- Add `scripts/carl-command-parity-smoke.ts` fixtures for `*carl`, `*brief`, multi-command prompts, and `/carl` fallback equivalence.
- Fail fast on mismatched command selection, missing guidance payload, or drift between star/slash outputs.
  </action>
  <verify>bun run scripts/carl-command-parity-smoke.ts</verify>
  <done>Command smoke checks prove parity for `*carl`, `*domain`, and `/carl` fallback with docs-consistent guidance output.</done>
</task>

</tasks>

<verification>
- Star commands and slash fallback share one resolver path.
- `*carl` and `/carl` produce equivalent command-mode outcomes.
- Help/manager guidance is sourced from existing CARL docs files.
</verification>

<success_criteria>
- Command parity behavior is preserved for CARL help mode and custom star-command domains.
- Users get consistent CARL guidance regardless of trigger surface.
</success_criteria>

<output>
After completion, create `.planning/phases/02-matching-injection-command-parity/02-03-SUMMARY.md`
</output>
