---
phase: 17-package-metadata-cicd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/test.yml
  - .github/workflows/e2e-tests.yml
  - tests/e2e/run-e2e-tests.sh
autonomous: true
requirements:
  - PKG-02
  - PKG-03

must_haves:
  truths:
    - "GitHub Actions workflow and job names show OpenCARL branding"
    - "Workflow artifacts are labeled with OpenCARL names"
    - "No CARL path references remain in workflows after rebranding"
    - "Docker image references use opencode-opencarl:e2e with an override option"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "OpenCARL-branded test workflow and artifacts"
      contains: "OpenCARL"
    - path: ".github/workflows/e2e-tests.yml"
      provides: "OpenCARL-branded E2E workflow and artifacts"
      contains: "opencode-opencarl:e2e"
    - path: "tests/e2e/run-e2e-tests.sh"
      provides: "E2E image name default with env override"
      contains: "opencode-opencarl:e2e"
  key_links:
    - from: ".github/workflows/e2e-tests.yml"
      to: "tests/e2e/run-e2e-tests.sh"
      via: "shared image naming"
      pattern: "opencode-opencarl:e2e"
---

<objective>
Rebrand CI workflows and ensure Docker image naming is consistent across CI and E2E tooling.

Purpose: CI/CD labels and artifacts reflect OpenCARL and the canonical image name is used everywhere.
Output: Updated GitHub Actions workflow names/artifacts and E2E runner image override support.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/test.yml
@.github/workflows/e2e-tests.yml
@tests/e2e/run-e2e-tests.sh
</context>

<tasks>

<task type="auto">
  <name>task 1: rebrand test workflow labels and artifacts</name>
  <files>.github/workflows/test.yml</files>
  <action>
Update workflow name, job name/labels, step names, and artifact names to include OpenCARL branding. While editing, scan for CARL path references (e.g., /carl, *carl, opencode-carl) and replace them with OpenCARL equivalents; if none exist, note in the summary. Keep triggers, conditions, and test commands unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.github/workflows/test.yml','utf8'); if(!/OpenCARL/.test(t) || /\/carl\b|\*carl\b|opencode-carl/.test(t)) process.exit(1);"</automated>
  </verify>
  <done>Test workflow labels and artifact names display OpenCARL branding.</done>
</task>

<task type="auto">
  <name>task 2: rebrand e2e workflow labels and artifacts</name>
  <files>.github/workflows/e2e-tests.yml</files>
  <action>
Update workflow name, job name/labels, step names, and artifact names to include OpenCARL branding. While editing, scan for CARL path references (e.g., /carl, *carl, opencode-carl) and replace them with OpenCARL equivalents; if none exist, note in the summary. Keep the Docker build and test commands unchanged, ensuring the image name remains opencode-opencarl:e2e.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.github/workflows/e2e-tests.yml','utf8'); if(!/OpenCARL/.test(t) || !/opencode-opencarl:e2e/.test(t) || /\/carl\b|\*carl\b|opencode-carl/.test(t)) process.exit(1);"</automated>
  </verify>
  <done>E2E workflow labels and artifact names display OpenCARL branding with the canonical image name.</done>
</task>

<task type="auto">
  <name>task 3: add E2E image override support</name>
  <files>tests/e2e/run-e2e-tests.sh</files>
  <action>
Introduce an env override for the Docker image name (defaulting to opencode-opencarl:e2e) and align the container name with OpenCARL branding. Do not change test flow or Docker build/run behavior beyond the name default.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('tests/e2e/run-e2e-tests.sh','utf8'); if(!/opencode-opencarl:e2e/.test(t)) process.exit(1);"</automated>
  </verify>
  <done>The E2E runner defaults to opencode-opencarl:e2e and can be overridden by env var.</done>
</task>

</tasks>

<verification>
CI workflows are branded as OpenCARL, artifacts carry OpenCARL names, and E2E tooling uses the canonical Docker image name with override support.
</verification>

<success_criteria>
- Workflow names and artifact names in .github/workflows/*.yml include OpenCARL
- E2E workflow and runner reference opencode-opencarl:e2e consistently
- E2E runner supports env-based image override
</success_criteria>

<output>
After completion, create `.planning/phases/17-package-metadata-cicd/17-02-SUMMARY.md`
</output>
