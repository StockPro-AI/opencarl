---
phase: 17-package-metadata-cicd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - scripts/publish-opencode.ts
autonomous: true
requirements:
  - PKG-01

must_haves:
  truths:
    - "Installer help and guidance reference @krisgray/opencarl and *opencarl / /opencarl commands"
    - "Publish guard validates @krisgray/opencarl as the package name"
  artifacts:
    - path: "bin/install.js"
      provides: "OpenCARL installer messaging and AGENTS integration text"
      contains: "@krisgray/opencarl"
    - path: "scripts/publish-opencode.ts"
      provides: "Publish guard for OpenCARL package name"
      contains: "@krisgray/opencarl"
  key_links:
    - from: "bin/install.js"
      to: "package.json"
      via: "version + package name references"
      pattern: "pkg\.version"
    - from: "scripts/publish-opencode.ts"
      to: "package.json"
      via: "name validation"
      pattern: "pkg\.name"
---

<objective>
Align installer messaging and publish guards with the OpenCARL package identity.

Purpose: Ensure installer output and publish checks consistently reference @krisgray/opencarl.
Output: Updated installer help text and publish checks.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bin/install.js
@scripts/publish-opencode.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: rebrand installer messaging and AGENTS section</name>
  <files>bin/install.js</files>
  <action>
Update installer help text, examples, and guidance to use @krisgray/opencarl and *opencarl / /opencarl commands. Replace CARL marker labels and integration text with OpenCARL equivalents, including the AGENTS section markers and prompts, while keeping the install flow logic unchanged. Update opencode.json plugin hints to the new package name.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('bin/install.js','utf8'); if(/@krisgray\/opencode-carl-plugin|\*carl|\/carl/.test(t)) process.exit(1);"</automated>
  </verify>
  <done>Installer output and AGENTS section content reference OpenCARL branding and @krisgray/opencarl.</done>
</task>

<task type="auto">
  <name>task 2: update publish guard to OpenCARL package name</name>
  <files>scripts/publish-opencode.ts</files>
  <action>
Replace the hardcoded package name validation and publish messages to use @krisgray/opencarl. Keep the verification flow and dist checks intact.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('scripts/publish-opencode.ts','utf8'); if(/@krisgray\/opencode-carl-plugin/.test(t)) process.exit(1);"</automated>
  </verify>
  <done>Publish script enforces @krisgray/opencarl and no longer references the legacy package name.</done>
</task>

</tasks>

<verification>
Installer strings and publish guard reflect @krisgray/opencarl with no legacy package name references in touched files.
</verification>

<success_criteria>
- bin/install.js help output shows @krisgray/opencarl and *opencarl / /opencarl
- scripts/publish-opencode.ts validates the OpenCARL package name
</success_criteria>

<output>
After completion, create `.planning/phases/17-package-metadata-cicd/17-01-SUMMARY.md`
</output>
