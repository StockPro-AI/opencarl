---
phase: 17-package-metadata-cicd
plan: 03
type: execute
wave: 2
depends_on:
  - "17-01"
  - "17-02"
  - "17-04"
files_modified:
  - .planning/phases/17-package-metadata-cicd/17-03-VERIFICATION.md
autonomous: true
requirements:
  - PKG-05

must_haves:
  truths:
    - "Repo-wide search finds no legacy package or Docker names outside documented exceptions"
    - "E2E suite passes using the opencode-opencarl:e2e Docker image"
  artifacts:
    - path: ".planning/phases/17-package-metadata-cicd/17-03-VERIFICATION.md"
      provides: "Legacy-string sweep results and E2E run evidence"
      contains: "opencode-opencarl:e2e"
  key_links:
    - from: ".planning/phases/17-package-metadata-cicd/17-03-VERIFICATION.md"
      to: "tests/e2e/run-e2e-tests.sh"
      via: "E2E command evidence"
      pattern: "npm run test:e2e:ci"
---

<objective>
Verify package and Docker naming consistency and confirm E2E tests pass with the canonical image.

Purpose: Ensure no legacy package or Docker references remain and E2E validation passes once after updates.
Output: Verification record for legacy-string sweep and E2E test run.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/e2e/run-e2e-tests.sh
</context>

<tasks>

<task type="auto">
  <name>task 1: record legacy string sweep results</name>
  <files>.planning/phases/17-package-metadata-cicd/17-03-VERIFICATION.md</files>
  <action>
Run a repo-wide search for legacy package and Docker names (e.g., @krisgray/opencode-carl-plugin, opencode-carl). Document matches and explicitly list any allowed exceptions (such as .planning history) in the verification file, including the exact command used and a short summary.
  </action>
  <verify>
    <automated>rg -n "@krisgray/opencode-carl-plugin|opencode-carl" || true</automated>
  </verify>
  <done>Verification file documents search results and any explicit exceptions.</done>
</task>

<task type="auto">
  <name>task 2: run E2E suite once and capture results</name>
  <files>.planning/phases/17-package-metadata-cicd/17-03-VERIFICATION.md</files>
  <action>
Run the full E2E suite once using npm run test:e2e:ci. Append the command and pass/fail outcome to the verification file, noting that the run uses the opencode-opencarl:e2e image.
  </action>
  <verify>
    <automated>npm run test:e2e:ci</automated>
  </verify>
  <done>E2E tests complete successfully and results are recorded in the verification file.</done>
</task>

</tasks>

<verification>
Legacy-string sweep results are recorded with explicit exceptions, and a successful E2E run is documented.
</verification>

<success_criteria>
- Verification file lists legacy string search results and exceptions
- npm run test:e2e:ci passes using opencode-opencarl:e2e
</success_criteria>

<output>
After completion, create `.planning/phases/17-package-metadata-cicd/17-03-SUMMARY.md`
</output>
