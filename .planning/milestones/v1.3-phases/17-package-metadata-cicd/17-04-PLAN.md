---
phase: 17-package-metadata-cicd
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
autonomous: true
requirements:
  - PKG-04

must_haves:
  truths:
    - "package.json metadata reflects OpenCARL branding and repository details"
    - "README badges and links point to the npm and GitHub locations for @krisgray/opencarl"
  artifacts:
    - path: "package.json"
      provides: "Canonical package metadata"
      contains: "\"name\": \"@krisgray/opencarl\""
    - path: "README.md"
      provides: "OpenCARL badges and npm/GitHub links"
      contains: "https://www.npmjs.com/package/@krisgray/opencarl"
  key_links:
    - from: "README.md"
      to: "https://www.npmjs.com/package/@krisgray/opencarl"
      via: "npm badge/link"
      pattern: "npmjs\.com/package/@krisgray/opencarl"
---

<objective>
Align package.json metadata and README links with the OpenCARL package identity.

Purpose: Ensure public-facing metadata and documentation links consistently reference @krisgray/opencarl.
Output: Updated package.json metadata and README badges/links.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>task 1: align package.json metadata with OpenCARL</name>
  <files>package.json</files>
  <action>
Confirm core metadata fields match OpenCARL branding (name, description, keywords, repository, bugs, homepage). Update keywords to include OpenCARL terms and remove legacy CARL-only keywords where they conflict with branding.
  </action>
  <verify>
    <automated>node -e "const pkg=require('./package.json'); const ok=pkg.name==='@krisgray/opencarl' && String(pkg.description).includes('OpenCARL') && String(pkg.repository?.url||'').includes('opencarl') && String(pkg.bugs?.url||'').includes('opencarl') && String(pkg.homepage||'').includes('opencarl'); if(!ok) process.exit(1);"</automated>
  </verify>
  <done>package.json metadata reflects OpenCARL branding and repository links.</done>
</task>

<task type="auto">
  <name>task 2: update README badges and npm/github links</name>
  <files>README.md</files>
  <action>
Update existing badges and links so they reference @krisgray/opencarl on npm and the KrisGray/opencarl GitHub repo. Ensure any badge URL parameters or link targets use the new package name and repo; keep the README structure and sections unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('README.md','utf8'); if(!t.includes('https://www.npmjs.com/package/@krisgray/opencarl')||!t.includes('https://github.com/KrisGray/opencarl')) process.exit(1);"</automated>
  </verify>
  <done>README badges and links point to the npm and GitHub locations for @krisgray/opencarl.</done>
</task>

</tasks>

<verification>
package.json metadata reflects OpenCARL branding, and README badges/links point to the OpenCARL npm and GitHub locations.
</verification>

<success_criteria>
- package.json metadata fields match OpenCARL branding
- README badges and links point to npm and GitHub for @krisgray/opencarl
</success_criteria>

<output>
After completion, create `.planning/phases/17-package-metadata-cicd/17-04-SUMMARY.md`
</output>
