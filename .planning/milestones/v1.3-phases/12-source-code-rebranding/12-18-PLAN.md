---
phase: 12-source-code-rebranding
plan: 18
type: execute
wave: 4
depends_on:
  - 12-03
files_modified:
  - src/opencarl/session-overrides.ts
  - src/opencarl/validate.ts
  - src/integration/opencode-config.ts
  - src/integration/agents-writer.ts
  - src/opencarl/setup.ts
  - src/integration/plugin-hooks.ts
  - src/opencarl/errors.ts
autonomous: true
requirements: [SOURCE-02, SOURCE-04]
gap_closure: true

must_haves:
  truths:
    - "Function and variable identifiers in session overrides, validation, and integration helpers use the opencarl prefix"
    - "Integration-layer comments describe OpenCARL branding without CARL references"
    - "Error docs links point to the KrisGray/opencarl docs URL"
  artifacts:
    - path: "src/opencarl/session-overrides.ts"
      provides: "Opencarl-prefixed session override parameter names"
      contains: "opencarlDir"
    - path: "src/opencarl/validate.ts"
      provides: "Opencarl-prefixed validation parameter names"
      contains: "opencarlDir"
    - path: "src/integration/opencode-config.ts"
      provides: "Opencarl-prefixed merge instructions helper"
      exports: ["mergeOpencarlInstructions"]
    - path: "src/integration/agents-writer.ts"
      provides: "Opencarl-prefixed agent integration helpers"
      exports: ["integrateOpencarl", "removeOpencarlIntegration"]
    - path: "src/opencarl/errors.ts"
      provides: "OpenCARL docs base URL"
      contains: "https://github.com/KrisGray/opencarl/blob/main/docs"
  key_links:
    - from: "src/opencarl/setup.ts"
      to: "src/integration/opencode-config.ts"
      via: "mergeOpencarlInstructions import"
      pattern: "mergeOpencarlInstructions"
    - from: "src/opencarl/setup.ts"
      to: "src/integration/agents-writer.ts"
      via: "integrateOpencarl/removeOpencarlIntegration imports"
      pattern: "integrateOpencarl|removeOpencarlIntegration"
---

<objective>
Close Phase 12 rebranding gaps by renaming remaining carl-prefixed identifiers, aligning integration comments to OpenCARL, and fixing the docs base URL.

Purpose: Complete SOURCE-02 and SOURCE-04 verification gaps and address the UAT-reported docs URL mismatch.
Output: Updated integration helpers, comments, and error docs link with opencarl naming.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-source-code-rebranding/12-CONTEXT.md
@.planning/phases/12-source-code-rebranding/12-VERIFICATION.md
@.planning/phases/12-source-code-rebranding/12-UAT.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename remaining carl-prefixed identifiers in source and integration helpers</name>
  <files>
    src/opencarl/session-overrides.ts
    src/opencarl/validate.ts
    src/integration/opencode-config.ts
    src/integration/agents-writer.ts
    src/opencarl/setup.ts
  </files>
  <action>
    Rename remaining carl-prefixed identifiers to opencarl-prefixed names and update all references:
    - session-overrides.ts: rename the `carlDir` parameter to `opencarlDir` and update all uses.
    - validate.ts: rename the `carlDir` parameter to `opencarlDir` and update all uses.
    - opencode-config.ts: rename `mergeCarlInstructions` to `mergeOpencarlInstructions` and `carlDocPaths` to `opencarlDocPaths` (including loop variables).
    - agents-writer.ts: rename `integrateCarl` to `integrateOpencarl`, `removeCarlIntegration` to `removeOpencarlIntegration`, and `carlDocsPath` to `opencarlDocsPath`.
    - setup.ts: update imports and call sites to use the renamed integration helpers.
    Keep behavior unchanged; only rename identifiers and adjust imports/exports.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All listed files use opencarl-prefixed identifiers and TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>task 2: Update remaining CARL references in integration comments</name>
  <files>
    src/integration/plugin-hooks.ts
    src/integration/opencode-config.ts
    src/integration/agents-writer.ts
  </files>
  <action>
    Update substantive comments and JSDoc to OpenCARL branding:
    - Replace CARL references with OpenCARL in doc comments.
    - Replace /carl mention with /opencarl in comments describing commands.
    - Keep runtime strings and behavior unchanged; comment-only updates.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>No CARL branding remains in comments within these integration files.</done>
</task>

<task type="auto">
  <name>task 3: Fix DOCS_BASE_URL to OpenCARL repository</name>
  <files>src/opencarl/errors.ts</files>
  <action>
    Update DOCS_BASE_URL to `https://github.com/KrisGray/opencarl/blob/main/docs` and ensure DOCS_LINKS continues to build from the base URL.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>DOCS_BASE_URL matches the KrisGray/opencarl docs path and builds valid docs links.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes after each task
- Grep for remaining `mergeCarlInstructions`, `integrateCarl`, `removeCarlIntegration`, `carlDocsPath`, `carlDocPaths`, and `carlDir` in the listed source/integration files returns no matches
- DOCS_BASE_URL in `src/opencarl/errors.ts` matches the KrisGray/opencarl URL
</verification>

<success_criteria>
- SOURCE-02 gaps closed: no remaining carl-prefixed identifiers in the listed source/integration helpers
- SOURCE-04 gaps closed: integration comments reference OpenCARL branding only
- UAT docs URL gap closed: DOCS_BASE_URL points to KrisGray/opencarl docs
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-code-rebranding/12-18-SUMMARY.md`
</output>
