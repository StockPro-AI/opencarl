---
phase: 12-source-code-rebranding
plan: 21
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/javascript/unit/injector.test.ts
  - tests/javascript/integration/plugin-lifecycle.test.ts
  - tests/javascript/integration/rule-injection-pipeline.test.ts
autonomous: true
requirements: [SOURCE-04]
gap_closure: true

must_haves:
  truths:
    - "Test assertions expect the new <opencarl-rules> output format"
    - "All tests in injector.test.ts, plugin-lifecycle.test.ts, and rule-injection-pipeline.test.ts pass"
  artifacts:
    - path: "tests/javascript/unit/injector.test.ts"
      provides: "Updated assertions for injection output"
      contains: "<opencarl-rules>"
    - path: "tests/javascript/integration/plugin-lifecycle.test.ts"
      provides: "Updated mock and assertions for injection output"
      contains: "<opencarl-rules>"
    - path: "tests/javascript/integration/rule-injection-pipeline.test.ts"
      provides: "Updated assertions for injection output"
      contains: "<opencarl-rules>"
  key_links:
    - from: "tests/javascript/unit/injector.test.ts"
      to: "src/opencarl/injector.ts"
      via: "test assertions"
      pattern: "toContain.*opencarl-rules"
---

<objective>
Fix critical test assertion failures where tests expect old `<carl-rules>` format but code produces `<opencarl-rules>`.

Purpose: Tests are currently failing because the source code was updated to produce `<opencarl-rules>` output, but test assertions still expect the old `<carl-rules>` format. This gap blocks phase completion.

Output: All affected tests pass with updated assertions matching the new output format.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-source-code-rebranding/12-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Update injector.test.ts assertions</name>
  <files>tests/javascript/unit/injector.test.ts</files>
  <action>
Update the following lines in injector.test.ts to expect `<opencarl-rules>` instead of `<carl-rules>`:

1. Line 34: Change `expect(result).toContain('<carl-rules>');` to `expect(result).toContain('<opencarl-rules>');`
2. Line 37: Change `expect(result).toContain('</' + 'carl-rules>');` to `expect(result).toContain('</opencarl-rules>');`

These assertions verify the output format from buildOpencarlInjection, which now produces `<opencarl-rules>` tags.
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/injector.test.ts --passWithNoTests 2>&1 | head -50</automated>
    <manual>All tests in injector.test.ts pass</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>injector.test.ts assertions updated to expect `<opencarl-rules>`, tests pass</done>
</task>

<task type="auto">
  <name>task 2: Update plugin-lifecycle.test.ts assertions and mock</name>
  <files>tests/javascript/integration/plugin-lifecycle.test.ts</files>
  <action>
Update the following lines in plugin-lifecycle.test.ts:

1. Line 128: Change mock return value from:
   `mockBuildOpencarlInjection.mockReturnValue('<carl-rules>\n[DEVELOPMENT] RULES:\n1. Use early returns\n2. Prefer small functions\n</carl-rules>');`
   to:
   `mockBuildOpencarlInjection.mockReturnValue('<opencarl-rules>\n[DEVELOPMENT] RULES:\n1. Use early returns\n2. Prefer small functions\n</opencarl-rules>');`

2. Line 314: Change `expect(output.system[0]).toContain('<carl-rules>');` to `expect(output.system[0]).toContain('<opencarl-rules>');`

3. Line 416: Change `expect(output.system[0]).toContain('<carl-rules>');` to `expect(output.system[0]).toContain('<opencarl-rules>');`

These match the new output format from the injector module.
  </action>
  <verify>
    <automated>npx jest tests/javascript/integration/plugin-lifecycle.test.ts --passWithNoTests 2>&1 | head -50</automated>
    <manual>All tests in plugin-lifecycle.test.ts pass</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>plugin-lifecycle.test.ts mock and assertions updated, tests pass</done>
</task>

<task type="auto">
  <name>task 3: Update rule-injection-pipeline.test.ts assertions</name>
  <files>tests/javascript/integration/rule-injection-pipeline.test.ts</files>
  <action>
Update the following lines in rule-injection-pipeline.test.ts:

1. Line 213: Change `expect(injection).toContain('<carl-rules>');` to `expect(injection).toContain('<opencarl-rules>');`

2. Line 216: Change `expect(injection).toContain('</carl-rules>');` to `expect(injection).toContain('</opencarl-rules>');`

These assertions verify the injection output format which now uses `<opencarl-rules>` tags.

Note: The snapshot test (line 347) may also need to be updated. After changing the assertions, run tests with -u flag to update snapshots if needed:
`npx jest tests/javascript/integration/rule-injection-pipeline.test.ts -u`
  </action>
  <verify>
    <automated>npx jest tests/javascript/integration/rule-injection-pipeline.test.ts --passWithNoTests 2>&1 | head -50</automated>
    <manual>All tests in rule-injection-pipeline.test.ts pass</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>rule-injection-pipeline.test.ts assertions updated, tests pass</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run full test suite: `npm test`
2. Verify no tests fail with "expected `<carl-rules>`" errors
3. Verify all three modified test files pass
</verification>

<success_criteria>
- All three test files pass with updated assertions
- No test failures related to `<carl-rules>` vs `<opencarl-rules>` format
- `npm test` shows all tests passing (except pre-existing setup.test.ts compilation error)
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-code-rebranding/12-21-SUMMARY.md`
</output>
