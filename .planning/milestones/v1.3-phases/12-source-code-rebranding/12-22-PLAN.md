---
phase: 12-source-code-rebranding
plan: 22
type: execute
wave: 1
depends_on: []
files_modified:
  - src/integration/agents-writer.ts
  - tests/javascript/unit/session-overrides.test.ts
  - tests/javascript/integration/plugin-lifecycle.test.ts
  - tests/javascript/integration/rule-injection-pipeline.test.ts
autonomous: true
requirements: [SOURCE-02, SOURCE-04]
gap_closure: true

must_haves:
  truths:
    - "Doc comments reference OPENCARL-START marker consistently"
    - "Test variable names use opencarl prefix for consistency"
  artifacts:
    - path: "src/integration/agents-writer.ts"
      provides: "Updated doc comment"
      contains: "OPENCARL-START"
    - path: "tests/javascript/unit/session-overrides.test.ts"
      provides: "Consistent variable naming"
      contains: "opencarlDir"
    - path: "tests/javascript/integration/plugin-lifecycle.test.ts"
      provides: "Consistent variable naming"
      contains: "tempOpencarlDir"
    - path: "tests/javascript/integration/rule-injection-pipeline.test.ts"
      provides: "Consistent parameter naming"
      contains: "targetOpencarlDir"
  key_links: []
---

<objective>
Fix doc comment inconsistency and update test variable names for SOURCE-02/SOURCE-04 compliance.

Purpose: The agents-writer.ts doc comment still references "CARL-START marker" instead of "OPENCARL-START marker". Additionally, test files use `carlDir`, `tempCarlDir`, and `targetCarlDir` variable names which is inconsistent with SOURCE-02 (all functions/variables use opencarl prefix).

Output: Doc comments and variable names updated to use OpenCARL branding consistently.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-source-code-rebranding/12-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Update agents-writer.ts doc comment</name>
  <files>src/integration/agents-writer.ts</files>
  <action>
Update line 16 in agents-writer.ts:

Change: `* - If AGENTS.md already contains CARL-START marker, returns success with "already integrated" message`
To: `* - If AGENTS.md already contains OPENCARL-START marker, returns success with "already integrated" message`

This aligns the doc comment with the actual marker constant which is now `<!-- OPENCARL-START - DO NOT EDIT -->`.
  </action>
  <verify>
    <automated>grep -n "OPENCARL-START marker" src/integration/agents-writer.ts</automated>
    <manual>Line 16 contains "OPENCARL-START marker"</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Doc comment references OPENCARL-START marker</done>
</task>

<task type="auto">
  <name>task 2: Rename carlDir to opencarlDir in session-overrides.test.ts</name>
  <files>tests/javascript/unit/session-overrides.test.ts</files>
  <action>
Rename the `carlDir` variable to `opencarlDir` throughout the file:

1. Line 15: Change `let carlDir: string;` to `let opencarlDir: string;`
2. Line 19: Change `carlDir = path.join(tempDir, '.opencarl');` to `opencarlDir = path.join(tempDir, '.opencarl');`
3. Line 20: Change `fs.mkdirSync(carlDir, { recursive: true });` to `fs.mkdirSync(opencarlDir, { recursive: true });`

Then find and replace all remaining occurrences of `carlDir` with `opencarlDir`:
- Approximately 48 occurrences throughout the file in beforeEach blocks, test bodies, and helper function calls

Use a global find-and-replace for efficiency: `carlDir` → `opencarlDir`

Also update the edge case section (lines 423-430):
- Line 424: `let edgeCarlDir: string;` → `let edgeOpencarlDir: string;`
- Line 428: `edgeCarlDir = path.join(edgeTempDir, '.opencarl');` → `edgeOpencarlDir = path.join(edgeTempDir, '.opencarl');`
- Line 429: `fs.mkdirSync(edgeCarlDir, { recursive: true });` → `fs.mkdirSync(edgeOpencarlDir, { recursive: true });`
- Replace all `edgeCarlDir` references with `edgeOpencarlDir`
  </action>
  <verify>
    <automated>npx jest tests/javascript/unit/session-overrides.test.ts --passWithNoTests 2>&1 | head -30</automated>
    <manual>No `carlDir` occurrences remain, tests still pass</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All `carlDir` variables renamed to `opencarlDir`, tests pass</done>
</task>

<task type="auto">
  <name>task 3: Rename tempCarlDir to tempOpencarlDir in plugin-lifecycle.test.ts</name>
  <files>tests/javascript/integration/plugin-lifecycle.test.ts</files>
  <action>
Rename the `tempCarlDir` variable to `tempOpencarlDir`:

1. Line 31: Change `let tempCarlDir: string;` to `let tempOpencarlDir: string;`
2. Line 143: Change `tempCarlDir = fs.mkdtempSync(path.join(os.tmpdir(), 'carl-test-'));` to `tempOpencarlDir = fs.mkdtempSync(path.join(os.tmpdir(), 'opencarl-test-'));`
3. Line 151: Change `if (fs.existsSync(tempCarlDir)) {` to `if (fs.existsSync(tempOpencarlDir)) {`
4. Line 152: Change `fs.rmSync(tempCarlDir, { recursive: true, force: true });` to `fs.rmSync(tempOpencarlDir, { recursive: true, force: true });`

Note: Also update the temp directory prefix from 'carl-test-' to 'opencarl-test-' for consistency.
  </action>
  <verify>
    <automated>npx jest tests/javascript/integration/plugin-lifecycle.test.ts --passWithNoTests 2>&1 | head -30</automated>
    <manual>No `tempCarlDir` occurrences remain, tests still pass</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>`tempCarlDir` renamed to `tempOpencarlDir`, tests pass</done>
</task>

<task type="auto">
  <name>task 4: Rename targetCarlDir to targetOpencarlDir in rule-injection-pipeline.test.ts</name>
  <files>tests/javascript/integration/rule-injection-pipeline.test.ts</files>
  <action>
Rename the `targetCarlDir` parameter to `targetOpencarlDir`:

1. Line 25: Change `function copyFixture(fixtureName: 'minimal' | 'full', targetCarlDir: string): void {` to `function copyFixture(fixtureName: 'minimal' | 'full', targetOpencarlDir: string): void {`
2. Line 26: Change `const sourceCarlDir = path.join(fixturesRoot, fixtureName, '.opencarl');` to `const sourceOpencarlDir = path.join(fixturesRoot, fixtureName, '.opencarl');`
3. Line 27: Change `fs.mkdirSync(path.dirname(targetCarlDir), { recursive: true });` to `fs.mkdirSync(path.dirname(targetOpencarlDir), { recursive: true });`
4. Line 28: Change `fs.cpSync(sourceCarlDir, targetCarlDir, { recursive: true });` to `fs.cpSync(sourceOpencarlDir, targetOpencarlDir, { recursive: true });`

Also update the temp directory prefix on line 56:
- Change `'carl-pipeline-integration-'` to `'opencarl-pipeline-integration-'`
  </action>
  <verify>
    <automated>npx jest tests/javascript/integration/rule-injection-pipeline.test.ts --passWithNoTests 2>&1 | head -30</automated>
    <manual>No `targetCarlDir` or `sourceCarlDir` occurrences remain, tests still pass</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>`targetCarlDir`/`sourceCarlDir` renamed to `targetOpencarlDir`/`sourceOpencarlDir`, tests pass</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify no `carlDir`, `tempCarlDir`, `sourceCarlDir`, or `targetCarlDir` remain in test files:
   ```bash
   grep -r "carlDir\|tempCarlDir\|sourceCarlDir\|targetCarlDir" tests/javascript/ || echo "No old variable names found"
   ```
2. Run full test suite: `npm test`
3. Verify tests still pass
</verification>

<success_criteria>
- agents-writer.ts doc comment references "OPENCARL-START marker"
- No `carl*` variable names in test files (except when testing old behavior)
- All modified test files pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-code-rebranding/12-22-SUMMARY.md`
</output>
