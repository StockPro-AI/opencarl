---
phase: 12-source-code-rebranding
plan: 19
type: execute
wave: 1
depends_on: []
files_modified:
  - src/opencarl/session-overrides.ts
  - src/integration/paths.ts
  - tests/javascript/unit/validate.test.ts
autonomous: true
gap_closure: true
requirements:
  - SOURCE-02

must_haves:
  truths:
    - "All functions and variables use 'opencarl' prefix"
    - "Console messages use [opencarl] prefix"
    - "Path strings reference opencarl, not carl"
  artifacts:
    - path: "src/opencarl/session-overrides.ts"
      provides: "Session override loading with consistent branding"
      contains: "\\[opencarl\\]"
    - path: "src/integration/paths.ts"
      provides: "Path resolution with opencarl paths"
      contains: ".opencode/opencarl"
    - path: "tests/javascript/unit/validate.test.ts"
      provides: "Validation tests with opencarlDir variable names"
      contains: "opencarlDir"
  key_links:
    - from: "src/opencarl/session-overrides.ts"
      to: "console.warn"
      via: "error logging"
      pattern: "\\[opencarl\\]"
    - from: "src/integration/paths.ts"
      to: "findFallbackOpencarl"
      via: "fallback directory path"
      pattern: ".opencode/opencarl"
---

<objective>
Fix remaining SOURCE-02 function/variable naming gaps identified in verification.

Purpose: Complete SOURCE-02 requirement by renaming remaining carl-prefixed identifiers to opencarl.
Output: Three files with consistent opencarl branding in console logs, paths, and test variables.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-source-code-rebranding/12-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Fix console log prefix in session-overrides.ts</name>
  <files>src/opencarl/session-overrides.ts</files>
  <action>
    Change line 96 from:
    ```
    console.warn(
      `[carl] Failed to parse session overrides for ${sessionId}: ${error}`
    );
    ```
    To:
    ```
    console.warn(
      `[opencarl] Failed to parse session overrides for ${sessionId}: ${error}`
    );
    ```
    This completes the SOURCE-02 requirement for consistent opencarl branding in console messages.
  </action>
  <verify>
    <automated>grep -n "\[carl\]" src/opencarl/session-overrides.ts && exit 1 || grep -n "\[opencarl\]" src/opencarl/session-overrides.ts</automated>
    <manual>Verify no [carl] prefix exists, only [opencarl]</manual>
  </verify>
  <done>Console messages use [opencarl] prefix consistently</done>
</task>

<task type="auto">
  <name>Fix fallback path in paths.ts</name>
  <files>src/integration/paths.ts</files>
  <action>
    Change line 61 from:
    ```
    path.join(resolvedRoot, ".opencode/carl"),
    ```
    To:
    ```
    path.join(resolvedRoot, ".opencode/opencarl"),
    ```
    This updates the fallback directory path to use consistent opencarl naming.
  </action>
  <verify>
    <automated>grep -n ".opencode/carl" src/integration/paths.ts && exit 1 || grep -n ".opencode/opencarl" src/integration/paths.ts</automated>
    <manual>Verify no .opencode/carl path exists, only .opencode/opencarl</manual>
  </verify>
  <done>Fallback path uses .opencode/opencarl</done>
</task>

<task type="auto">
  <name>Rename test variables from carlDir to opencarlDir</name>
  <files>tests/javascript/unit/validate.test.ts</files>
  <action>
    In the test file `tests/javascript/unit/validate.test.ts`, rename all occurrences of variable `carlDir` to `opencarlDir` in the "domain file resolution" and "edge cases" describe blocks (approximately lines 608-664).

    The variable is declared and used like:
    ```typescript
    const carlDir = path.join(tempDir, '.opencarl');
    ```

    Change to:
    ```typescript
    const opencarlDir = path.join(tempDir, '.opencarl');
    ```

    Update all references to this variable within those test blocks. The variable appears in multiple test cases within these describe blocks.
  </action>
  <verify>
    <automated>grep -n "const carlDir" tests/javascript/unit/validate.test.ts && exit 1 || echo "No carlDir variables found"</automated>
    <manual>Verify all test variables use opencarlDir naming</manual>
  </verify>
  <done>Test variables use opencarlDir consistently</done>
</task>

</tasks>

<verification>
Run TypeScript compilation and tests to verify changes:
```bash
npx tsc --noEmit && npm test -- tests/javascript/unit/validate.test.ts
```
</verification>

<success_criteria>
- Console messages in session-overrides.ts use [opencarl] prefix
- Fallback path in paths.ts uses .opencode/opencarl
- Test variables in validate.test.ts use opencarlDir naming
- TypeScript compilation passes
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-code-rebranding/12-19-SUMMARY.md`
</output>
