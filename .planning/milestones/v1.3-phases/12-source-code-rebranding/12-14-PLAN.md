---
phase: 12-source-code-rebranding
plan: 14
type: execute
wave: 4
depends_on:
  - 12-03
files_modified:
  - src/opencarl/context-brackets.ts
  - src/opencarl/command-parity.ts
  - src/opencarl/session-overrides.ts
  - src/opencarl/duplicate-detector.ts
autonomous: true
requirements:
  - SOURCE-03

must_haves:
  truths:
    - Import statements in remaining source files reference ./opencarl/* paths instead of ./carl/*
    - Type-only imports in remaining source files follow the same opencarl path pattern
  artifacts:
    - path: "src/opencarl/session-overrides.ts"
      provides: "Import statements with opencarl paths"
      contains: "from.*opencarl/"
  key_links:
    - from: "src/opencarl/session-overrides.ts"
      to: "src/opencarl/loader.ts"
      via: "import statement"
      pattern: "from.*opencarl/"
---

<objective>
Update import statements in remaining source files to use ./opencarl/* paths

Purpose: Finish the source import path rebrand after directory rename
Output: Updated import statements in remaining source files using opencarl paths
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-source-code-rebranding/12-CONTEXT.md
@.planning/phases/12-source-code-rebranding/12-RESEARCH.md
@.planning/phases/12-source-code-rebranding/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Update import statements in remaining source files</name>
  <files>
    src/opencarl/context-brackets.ts
    src/opencarl/command-parity.ts
    src/opencarl/session-overrides.ts
    src/opencarl/duplicate-detector.ts
  </files>
  <action>
    Update import statements that explicitly reference a /carl/ segment to /opencarl/ in the listed files:
    - Replace "from './carl/" with "from './opencarl/"
    - Replace "from '../carl/" with "from '../opencarl/"
    - Replace "from '../../carl/" with "from '../../opencarl/" (if present)
    - Update both regular imports and type-only imports

    Do NOT rewrite intra-folder imports that do not contain /carl/ (e.g., "./types", "./errors").
  </action>
  <verify>
    rg "from.*carl/" src/opencarl/context-brackets.ts src/opencarl/command-parity.ts src/opencarl/session-overrides.ts src/opencarl/duplicate-detector.ts
  </verify>
  <done>
    All listed files use opencarl paths with no remaining /carl/ import segments
  </done>
</task>

</tasks>

<verification>
1. No remaining "/carl/" import paths in listed files (rg "from.*carl/" src/opencarl/context-brackets.ts src/opencarl/command-parity.ts src/opencarl/session-overrides.ts src/opencarl/duplicate-detector.ts)
</verification>

<success_criteria>
All listed source files reference opencarl import paths with no remaining /carl/ imports
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-code-rebranding/12-14-SUMMARY.md`
</output>
