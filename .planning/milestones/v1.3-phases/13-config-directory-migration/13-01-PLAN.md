---
phase: 13-config-directory-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .carl-template/
  - .opencarl-template/
  - package.json
autonomous: true
requirements:
  - CONFIG-03
  - CONFIG-05

must_haves:
  truths:
    - ".carl-template/ directory no longer exists on disk"
    - ".opencarl-template/ directory exists with all template files"
    - "package.json files array includes .opencarl-template"
  artifacts:
    - path: ".opencarl-template/"
      provides: "Template files for new OpenCARL installations"
      contains: "manifest, global, context, commands, example-custom-domain, sessions/"
    - path: "package.json"
      provides: "NPM package file list"
      contains: '".opencarl-template"'
  key_links:
    - from: "package.json"
      to: ".opencarl-template/"
      via: "files array"
      pattern: '".opencarl-template"'
---

<objective>
Rename .carl-template/ directory to .opencarl-template/ and update package.json to reference the new name.

Purpose: Establish the renamed template directory as the source for new OpenCARL installations.
Output: Renamed .opencarl-template/ directory and updated package.json.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-config-directory-migration/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Rename template directory and update package.json</name>
  <files>.carl-template/, .opencarl-template/, package.json</files>
  <action>
Rename the .carl-template/ directory to .opencarl-template/ using git mv to preserve history:
  git mv .carl-template .opencarl-template

Update package.json line 14 to reference the new directory name:
  Change: ".carl-template"
  To:     ".opencarl-template"

This ensures npm packages include the renamed template directory.
  </action>
  <verify>
    <automated>test -d .opencarl-template && test ! -d .carl-template && grep -q '".opencarl-template"' package.json && echo "PASS: Directory renamed and package.json updated"</automated>
  </verify>
  <done>.opencarl-template/ exists, .carl-template/ does not exist, package.json files array contains .opencarl-template</done>
</task>

</tasks>

<verification>
- Directory .carl-template/ does not exist
- Directory .opencarl-template/ exists with all files (manifest, global, context, commands, example-custom-domain, sessions/)
- package.json files array contains ".opencarl-template"
- Git history preserved for template files
</verification>

<success_criteria>
1. .carl-template/ directory is renamed to .opencarl-template/
2. All template files exist in .opencarl-template/
3. package.json files array references .opencarl-template
</success_criteria>

<output>
After completion, create `.planning/phases/13-config-directory-migration/13-01-SUMMARY.md`
</output>
