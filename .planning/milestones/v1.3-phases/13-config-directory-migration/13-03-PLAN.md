---
phase: 13-config-directory-migration
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - tests/javascript/unit/loader.test.ts
  - tests/javascript/unit/setup.test.ts
  - tests/javascript/unit/validate.test.ts
  - tests/javascript/unit/session-overrides.test.ts
  - tests/javascript/integration/file-operations.test.ts
  - tests/javascript/integration/rule-injection-pipeline.test.ts
  - tests/javascript/integration/setup-domain-workflow.test.ts
  - tests/javascript/e2e/setup-flow.test.ts
  - tests/javascript/e2e/keyword-matching.test.ts
  - tests/javascript/e2e/star-command.test.ts
  - tests/helpers/domain-factory.ts
autonomous: true
requirements:
  - CONFIG-04

must_haves:
  truths:
    - "Tests create temp directories named .opencarl instead of .carl"
    - "Tests verify .opencarl/manifest path instead of .carl/manifest"
    - "All test assertions reference updated directory names"
  artifacts:
    - path: "tests/javascript/unit/*.test.ts"
      provides: "Unit test coverage"
      contains: '".opencarl"'
    - path: "tests/javascript/integration/*.test.ts"
      provides: "Integration test coverage"
      contains: '".opencarl"'
    - path: "tests/javascript/e2e/*.test.ts"
      provides: "E2E test coverage"
      contains: '".opencarl"'
  key_links:
    - from: "tests/**"
      to: "temp directories"
      via: "path.join(tempDir, '.opencarl')"
      pattern: '\.opencarl'
---

<objective>
Update all test files to use .opencarl directory paths instead of .carl.

Purpose: Ensure tests create and verify the correct renamed directories.
Output: Updated test files with new directory names.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-config-directory-migration/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Update unit test path references</name>
  <files>
    - tests/javascript/unit/loader.test.ts
    - tests/javascript/unit/setup.test.ts
    - tests/javascript/unit/validate.test.ts
    - tests/javascript/unit/session-overrides.test.ts
    - tests/helpers/domain-factory.ts
  </files>
  <action>
Replace all occurrences of `.carl` with `.opencarl` in unit test files:

In tests/javascript/unit/loader.test.ts (9 occurrences):
- Replace `.carl` with `.opencarl` in all path strings

In tests/javascript/unit/setup.test.ts (2 occurrences):
- Replace `.carl` with `.opencarl` in all path strings

In tests/javascript/unit/validate.test.ts (6 occurrences):
- Replace `.carl` with `.opencarl` in all path strings

In tests/javascript/unit/session-overrides.test.ts (5 occurrences):
- Replace `.carl` with `.opencarl` in all path strings

In tests/helpers/domain-factory.ts (1 occurrence):
- Replace `.carl` with `.opencarl` in path strings

Use sed or editor find-and-replace for efficiency. Preserve all other test logic.
  </action>
  <verify>
    <automated>grep -r "\.carl" tests/javascript/unit/ tests/helpers/domain-factory.ts 2>/dev/null | grep -v "node_modules" | wc -l | xargs -I {} sh -c 'if [ {} -eq 0 ]; then echo "PASS: No .carl references in unit tests"; else echo "FAIL: {} .carl references remain"; exit 1; fi'</automated>
  </verify>
  <done>Unit test files contain no .carl path references, only .opencarl</done>
</task>

<task type="auto">
  <name>task 2: Update integration test path references</name>
  <files>
    - tests/javascript/integration/file-operations.test.ts
    - tests/javascript/integration/rule-injection-pipeline.test.ts
    - tests/javascript/integration/setup-domain-workflow.test.ts
  </files>
  <action>
Replace all occurrences of `.carl` with `.opencarl` in integration test files:

In tests/javascript/integration/file-operations.test.ts (6 occurrences):
- Replace `.carl` with `.opencarl` in all path strings

In tests/javascript/integration/rule-injection-pipeline.test.ts (4 occurrences):
- Replace `.carl` with `.opencarl` in all path strings

In tests/javascript/integration/setup-domain-workflow.test.ts (4 occurrences):
- Replace `.carl` with `.opencarl` in all path strings
- Replace `.carl-template` with `.opencarl-template` in template references

Use sed or editor find-and-replace for efficiency. Preserve all other test logic.
  </action>
  <verify>
    <automated>grep -r "\.carl" tests/javascript/integration/ 2>/dev/null | grep -v "node_modules" | wc -l | xargs -I {} sh -c 'if [ {} -eq 0 ]; then echo "PASS: No .carl references in integration tests"; else echo "FAIL: {} .carl references remain"; exit 1; fi'</automated>
  </verify>
  <done>Integration test files contain no .carl path references, only .opencarl</done>
</task>

<task type="auto">
  <name>task 3: Update e2e test path references</name>
  <files>
    - tests/javascript/e2e/setup-flow.test.ts
    - tests/javascript/e2e/keyword-matching.test.ts
    - tests/javascript/e2e/star-command.test.ts
  </files>
  <action>
Replace all occurrences of `.carl` with `.opencarl` in e2e test files:

In tests/javascript/e2e/setup-flow.test.ts (20 occurrences):
- Replace `.carl` with `.opencarl` in all path strings
- Update dockerExec commands that reference .carl paths

In tests/javascript/e2e/keyword-matching.test.ts (12 occurrences):
- Replace `.carl` with `.opencarl` in all path strings
- Update dockerExec commands that reference .carl paths

In tests/javascript/e2e/star-command.test.ts (18 occurrences):
- Replace `.carl` with `.opencarl` in all path strings
- Update dockerExec commands that reference .carl paths

Use sed or editor find-and-replace for efficiency. Preserve all other test logic.
  </action>
  <verify>
    <automated>grep -r "\.carl" tests/javascript/e2e/ 2>/dev/null | grep -v "node_modules" | wc -l | xargs -I {} sh -c 'if [ {} -eq 0 ]; then echo "PASS: No .carl references in e2e tests"; else echo "FAIL: {} .carl references remain"; exit 1; fi'</automated>
  </verify>
  <done>E2E test files contain no .carl path references, only .opencarl</done>
</task>

<task type="auto">
  <name>task 4: Run unit tests to verify changes</name>
  <files>N/A</files>
  <action>
Run unit tests to verify all path updates are correct:
  npm run test:unit

All tests should pass. If any test fails due to path issues, fix the specific test file.
  </action>
  <verify>
    <automated>npm run test:unit -- --passWithNoTests</automated>
  </verify>
  <done>All unit tests pass with updated .opencarl paths</done>
</task>

</tasks>

<verification>
- No .carl path references remain in tests/javascript/ directory
- All .opencarl path references are correct
- Unit tests pass
- Integration tests pass (npm run test:integration)
</verification>

<success_criteria>
1. All test files use .opencarl directory paths
2. No .carl path references remain in test files
3. Unit tests pass with updated paths
4. Integration tests pass with updated paths
</success_criteria>

<output>
After completion, create `.planning/phases/13-config-directory-migration/13-03-SUMMARY.md`
</output>
