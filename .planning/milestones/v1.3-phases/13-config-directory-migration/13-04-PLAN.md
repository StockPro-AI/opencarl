---
phase: 13-config-directory-migration
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - scripts/carl-discovery-smoke.ts
  - scripts/carl-injection-smoke.ts
  - scripts/carl-phase2-parity.ts
  - scripts/carl-command-parity-smoke.ts
  - scripts/verify-dist.ts
  - scripts/fixtures/phase2/injection.json
autonomous: true
requirements:
  - CONFIG-04

must_haves:
  truths:
    - "Smoke test scripts reference .opencarl paths"
    - "Verification scripts check .opencarl-template directory"
    - "Fixture files contain .opencarl paths"
  artifacts:
    - path: "scripts/carl-*.ts"
      provides: "Smoke test scripts"
      contains: '".opencarl"'
    - path: "scripts/verify-dist.ts"
      provides: "Distribution verification"
      contains: '".opencarl-template"'
    - path: "scripts/fixtures/phase2/injection.json"
      provides: "Test fixture data"
      contains: '".opencarl"'
  key_links:
    - from: "scripts/*.ts"
      to: "config directories"
      via: "hardcoded paths"
      pattern: '\.opencarl'
---

<objective>
Update all script files to use .opencarl directory paths instead of .carl.

Purpose: Ensure smoke tests and verification scripts reference correct directories.
Output: Updated script files with new directory names.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-config-directory-migration/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Update smoke test script paths</name>
  <files>
    - scripts/carl-discovery-smoke.ts
    - scripts/carl-injection-smoke.ts
    - scripts/carl-phase2-parity.ts
    - scripts/carl-command-parity-smoke.ts
  </files>
  <action>
Replace all occurrences of `.carl` with `.opencarl` in smoke test scripts:

In scripts/carl-discovery-smoke.ts (2 occurrences):
- Replace `.carl` with `.opencarl` in path strings

In scripts/carl-injection-smoke.ts (6 occurrences):
- Replace `.carl` with `.opencarl` in path strings

In scripts/carl-phase2-parity.ts (2 occurrences):
- Replace `.carl` with `.opencarl` in path strings

In scripts/carl-command-parity-smoke.ts (1 occurrence):
- Replace `.carl-template` with `.opencarl-template` in template references

Use sed or editor find-and-replace for efficiency.
  </action>
  <verify>
    <automated>grep -l "\.carl" scripts/carl-*.ts 2>/dev/null | wc -l | xargs -I {} sh -c 'if [ {} -eq 0 ]; then echo "PASS: No .carl references in smoke scripts"; else echo "FAIL: {} files still have .carl references"; exit 1; fi'</automated>
  </verify>
  <done>Smoke test scripts contain no .carl path references</done>
</task>

<task type="auto">
  <name>task 2: Update verify-dist.ts and fixtures</name>
  <files>
    - scripts/verify-dist.ts
    - scripts/fixtures/phase2/injection.json
  </files>
  <action>
Replace all occurrences of `.carl` with `.opencarl`:

In scripts/verify-dist.ts (1 occurrence):
- Replace `.carl-template` with `.opencarl-template`

In scripts/fixtures/phase2/injection.json (4 occurrences):
- Replace `.carl` with `.opencarl` in all path strings in the JSON fixture

Use sed or editor find-and-replace for efficiency.
  </action>
  <verify>
    <automated>grep "\.carl" scripts/verify-dist.ts scripts/fixtures/phase2/injection.json 2>/dev/null | wc -l | xargs -I {} sh -c 'if [ {} -eq 0 ]; then echo "PASS: No .carl references in verify-dist or fixtures"; else echo "FAIL: {} .carl references remain"; exit 1; fi'</automated>
  </verify>
  <done>verify-dist.ts and fixtures contain no .carl path references</done>
</task>

<task type="auto">
  <name>task 3: Run full test suite to verify all changes</name>
  <files>N/A</files>
  <action>
Run the full test suite to verify all path updates are correct:
  npm test

This validates that all source, test, and script changes work together correctly.
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests</automated>
  </verify>
  <done>All tests pass with updated .opencarl paths</done>
</task>

</tasks>

<verification>
- No .carl path references remain in scripts/ directory
- verify-dist.ts checks .opencarl-template
- Full test suite passes
- TypeScript compiles without errors
</verification>

<success_criteria>
1. All script files use .opencarl directory paths
2. No .carl path references remain in script files
3. Full test suite passes
4. Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-config-directory-migration/13-04-SUMMARY.md`
</output>
