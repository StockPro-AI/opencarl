---
phase: 13-config-directory-migration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
autonomous: true
gap_closure: true
requirements: [CONFIG-02, CONFIG-04, CONFIG-05]

must_haves:
  truths:
    - "Help text shows .opencarl instead of .carl"
    - "CARL_AGENTS_SECTION references .opencarl directory"
    - "All user-facing documentation in install.js is consistent"
  artifacts:
    - path: "bin/install.js"
      provides: "CLI installer with updated documentation"
      contains: ".opencarl"
  key_links:
    - from: "bin/install.js"
      to: "User's terminal"
      via: "console.log help text"
      pattern: "\\.opencarl"
---

<objective>
Update all .carl references in bin/install.js documentation strings to .opencarl.

Purpose: Functional code already scaffolds .opencarl/ correctly, but 8 documentation/help text strings still reference .carl, causing user confusion.
Output: Consistent .opencarl branding in all user-facing text.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-config-directory-migration/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Update .carl references to .opencarl in documentation strings</name>
  <files>bin/install.js</files>
  <action>
    Update the following lines in bin/install.js to replace .carl with .opencarl:
    
    1. Line 7: `* - Seeds .carl/ directory` → `* - Seeds .opencarl/ directory`
    2. Line 60: `|.carl/` directory |` → `|.opencarl/` directory |`
    3. Line 65: `.carl/` in code block → `.opencarl/`
    4. Line 90: `~/.carl` → `~/.opencarl`
    5. Line 91: `./.carl` → `./.opencarl`
    6. Line 113: `.carl/` → `.opencarl/`
    7. Lines 360-361: `~/.carl` and `./.carl` → `~/.opencarl` and `./.opencarl`
    
    Also update CARL_AGENTS_SECTION constant (lines 42-73):
    - Line 60: `|.carl/` directory |` → `|.opencarl/` directory |`
    - Lines 65-69: Structure showing `.carl/` → `.opencarl/`
    
    Do NOT update command triggers (*carl, /carl) - those are Phase 14 scope.
  </action>
  <verify>
    <automated>grep -n '\.carl' bin/install.js | grep -v '\.opencarl' || echo "No .carl references remaining"</automated>
  </verify>
  <done>All .carl directory references replaced with .opencarl in documentation strings (excluding command triggers which are Phase 14)</done>
</task>

</tasks>

<verification>
- Run: `grep -n '\.carl' bin/install.js | grep -v '\.opencarl'`
- Expect: No matches (or only command triggers *carl, /carl which are Phase 14)
- Run: `grep -c '\.opencarl' bin/install.js`
- Expect: Multiple matches confirming updates
</verification>

<success_criteria>
- All directory path references in help text use .opencarl
- CARL_AGENTS_SECTION uses .opencarl directory name
- Command triggers (*carl, /carl) remain unchanged (Phase 14 scope)
</success_criteria>

<output>
After completion, create `.planning/phases/13-config-directory-migration/13-06-SUMMARY.md`
</output>
