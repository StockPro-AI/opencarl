---
phase: 13-config-directory-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/integration/paths.ts
  - bin/install.js
autonomous: true
requirements:
  - CONFIG-02
  - CONFIG-04

must_haves:
  truths:
    - "Path resolution searches for .opencarl directory instead of .carl"
    - "Setup creates .opencarl directory instead of .carl"
    - "bin/install.js scaffolds .opencarl/ and uses .opencarl-template"
  artifacts:
    - path: "src/integration/paths.ts"
      provides: "Directory discovery functions"
      contains: '".opencarl"'
    - path: "bin/install.js"
      provides: "CLI installer"
      contains: "'.opencarl'"
  key_links:
    - from: "src/integration/paths.ts:31"
      to: ".opencarl directory"
      via: "findProjectOpencarl search"
      pattern: 'path\.join\(searchPath, "\.opencarl"\)'
    - from: "src/integration/paths.ts:51"
      to: ".opencarl directory"
      via: "findGlobalOpencarl search"
      pattern: 'path\.join\(resolvedHome, "\.opencarl"\)'
    - from: "bin/install.js:251-253"
      to: ".opencarl directory"
      via: "carlDir assignment"
      pattern: "opencarlDir"
---

<objective>
Update path resolution in paths.ts and bin/install.js to use .opencarl instead of .carl.

Purpose: Change runtime behavior to search for and create .opencarl directories.
Output: Updated source files with new directory names.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-config-directory-migration/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Update paths.ts directory search</name>
  <files>src/integration/paths.ts</files>
  <action>
Update src/integration/paths.ts to search for .opencarl instead of .carl:

Line 31 - Change:
  const opencarlDir = path.join(searchPath, ".carl");
To:
  const opencarlDir = path.join(searchPath, ".opencarl");

Line 51 - Change:
  path.join(resolvedHome, ".carl"),
To:
  path.join(resolvedHome, ".opencarl"),

These changes affect findProjectOpencarl() and findGlobalOpencarl() functions.
  </action>
  <verify>
    <automated>grep -q 'path.join(searchPath, ".opencarl")' src/integration/paths.ts && grep -q 'path.join(resolvedHome, ".opencarl")' src/integration/paths.ts && echo "PASS: paths.ts updated"</automated>
  </verify>
  <done>paths.ts searches for .opencarl directory in both project and global locations</done>
</task>

<task type="auto">
  <name>task 2: Update bin/install.js scaffolding</name>
  <files>bin/install.js</files>
  <action>
Update bin/install.js to create .opencarl directory and use .opencarl-template:

Lines 251-253 - Rename variable and update paths:
Change:
  const carlDir = isGlobal
    ? path.join(os.homedir(), '.carl')
    : path.join(process.cwd(), '.carl');
To:
  const opencarlDir = isGlobal
    ? path.join(os.homedir(), '.opencarl')
    : path.join(process.cwd(), '.opencarl');

Lines 259-260 - Update label variable:
Change:
  const carlLabel = carlDir.replace(os.homedir(), '~');
To:
  const opencarlLabel = opencarlDir.replace(os.homedir(), '~');

Line 262 - Update console.log:
Change:
  console.log(`  Installing to ${amber}${opencodeLabel}${reset} and ${amber}${carlLabel}${reset}\n`);
To:
  console.log(`  Installing to ${amber}${opencodeLabel}${reset} and ${amber}${opencarlLabel}${reset}\n`);

Line 286 - Update template source:
Change:
  const carlTemplateSrc = path.join(src, '.carl-template');
To:
  const opencarlTemplateSrc = path.join(src, '.opencarl-template');

Lines 287-291 - Update variable references:
Change all occurrences of carlDir to opencarlDir and carlLabel to opencarlLabel.
  </action>
  <verify>
    <automated>grep -q "opencarlDir" bin/install.js && grep -q "'.opencarl'" bin/install.js && grep -q "'.opencarl-template'" bin/install.js && echo "PASS: bin/install.js updated"</automated>
  </verify>
  <done>bin/install.js creates .opencarl directory and copies from .opencarl-template</done>
</task>

</tasks>

<verification>
- src/integration/paths.ts line 31 contains ".opencarl"
- src/integration/paths.ts line 51 contains ".opencarl"
- bin/install.js uses opencarlDir variable
- bin/install.js references .opencarl-template
- TypeScript compiles without errors
</verification>

<success_criteria>
1. findProjectOpencarl() searches for .opencarl directory
2. findGlobalOpencarl() searches for .opencarl directory
3. bin/install.js scaffolds .opencarl/ directory
4. bin/install.js copies from .opencarl-template/
</success_criteria>

<output>
After completion, create `.planning/phases/13-config-directory-migration/13-02-SUMMARY.md`
</output>
