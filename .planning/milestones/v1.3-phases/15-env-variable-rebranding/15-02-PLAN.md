---
phase: 15-env-variable-rebranding
plan: 02
type: execute
wave: 2
depends_on: ["15-03"]
files_modified:
  - .github/workflows/e2e-tests.yml
autonomous: true
requirements: [ENV-02]
must_haves:
  truths:
    - "All CI configuration files use OPENCARL_DEBUG instead of CARL_DEBUG"
    - "All Docker image references use opencode-opencarl:e2e instead of opencode-carl:e2e"
    - "Zero CARL_DEBUG references remain in CI configurations"
  artifacts:
    - path: ".github/workflows/e2e-tests.yml"
      provides: "CI workflow configuration with updated Docker image and env var"
      contains: "opencode-opencarl:e2e"
  key_links:
    - from: ".github/workflows/e2e-tests.yml"
      to: "tests/e2e/run-e2e-tests.sh"
      via: "Docker image reference in workflow"
      pattern: "IMAGE_NAME.*opencode-opencarl:e2e"
---

<objective>
Update CI configuration files to use OPENCARL_DEBUG and updated Docker image naming convention.

Purpose: Ensure GitHub Actions CI workflows use the new environment variable name and Docker image, maintaining consistent naming across infrastructure.

Output:
- .github/workflows/e2e-tests.yml updated with OPENCARL_DEBUG and opencode-opencarl:e2e
- All CARL_DEBUG references removed from CI configs
- CI workflow verified to compile and run without errors
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-env-variable-rebranding/15-CONTEXT.md
@.planning/phases/15-env-variable-rebranding/15-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Context from prior phases:
- Phase 13 (config): .carl/ renamed to .opencarl/
- Phase 15 CONTEXT.md: CI configurations updated, include documentation and code comments
- Phase 15 RESEARCH.md: CI workflows use Docker image naming pattern opencode-{product}:e2e
</context>

<tasks>

<task type="auto">
  <name>Update CI workflow with OPENCARL_DEBUG and opencode-opencarl:e2e</name>
  <files>.github/workflows/e2e-tests.yml</files>
  <action>
    Update .github/workflows/e2e-tests.yml to use new environment variable and Docker image:

    1. Find all references to "CARL_DEBUG" and replace with "OPENCARL_DEBUG"
    2. Find all references to "opencode-carl:e2e" and replace with "opencode-opencarl:e2e"
    3. **CRITICAL: Rename CARL_DEBUG strings in inline code comments in CI YAML**
       - Find all comments containing "CARL_DEBUG" and replace with "OPENCARL_DEBUG"
       - Examples: "# CARL_DEBUG=true", "# Debug mode: CARL_DEBUG"
    4. Update environment variable definitions if present
    5. Verify YAML syntax is correct
    6. Check for any other CARL_DEBUG references in configuration values

    Use targeted edits for specific patterns:
    - Environment variable declarations: CARL_DEBUG → OPENCARL_DEBUG
    - Docker image references: opencode-carl:e2e → opencode-opencarl:e2e
    - YAML comments: CARL_DEBUG → OPENCARL_DEBUG
    - String literals: CARL_DEBUG → OPENCARL_DEBUG
  </action>
  <verify>
    <automated>rg "CARL_DEBUG" .github/workflows/e2e-tests.yml && echo "ERROR: CARL_DEBUG still exists" || echo "SUCCESS: Zero CARL_DEBUG in CI workflow"</automated>
    <automated>rg "opencode-carl:e2e" .github/workflows/e2e-tests.yml && echo "ERROR: Old Docker image still exists" || echo "SUCCESS: Old Docker image removed"</automated>
    <automated>rg "opencode-opencarl:e2e" .github/workflows/e2e-tests.yml && echo "SUCCESS: OPENCARL_DEBUG Docker image present"</automated>
    <manual>Open .github/workflows/e2e-tests.yml: Verify environment variables, Docker image references, check YAML syntax</manual>
    <sampling_rate>After file edit, before next task</sampling_rate>
  </verify>
  <done>.github/workflows/e2e-tests.yml updated with OPENCARL_DEBUG and opencode-opencarl:e2e, zero CARL_DEBUG references, YAML syntax valid</done>
</task>

<task type="auto">
  <name>Verify CI configuration syntax and references</name>
  <files>.github/workflows/e2e-tests.yml</files>
  <action>
    Verify CI workflow configuration is valid and complete:

    1. Check YAML syntax:
       ```bash
       yamllint .github/workflows/e2e-tests.yml || true
       ```

    2. Verify required keys are present:
       - name
       - on (event triggers)
       - jobs
       - steps

    3. Check Docker image references are correct throughout:
       - Build steps use opencode-opencarl:e2e
       - Container references use opencode-opencarl:e2e

    4. Check environment variable references are correct:
       - All references to CARL_DEBUG replaced with OPENCARL_DEBUG

    5. Ensure consistency across the workflow file
  </action>
  <verify>
    <automated>yamllint .github/workflows/e2e-tests.yml && echo "SUCCESS: YAML syntax valid" || echo "WARN: YAML lint warnings (may be pre-existing)"</automated>
    <automated>rg "opencode-opencarl:e2e" .github/workflows/e2e-tests.yml tests/e2e/run-e2e-tests.sh</automated>
    <manual>Open .github/workflows/e2e-tests.yml: Verify all steps use correct Docker image and env var, check no broken references</manual>
    <sampling_rate>After verification, before wave 2 begins</sampling_rate>
  </verify>
  <done>CI workflow configuration verified valid, all Docker image references use opencode-opencarl:e2e, all environment variable references use OPENCARL_DEBUG</done>
</task>

</tasks>

<verification>
## Verification Checklist

1. [ ] .github/workflows/e2e-tests.yml uses OPENCARL_DEBUG
2. [ ] Inline code comments in CI workflow contain OPENCARL_DEBUG
3. [ ] Docker image references use opencode-opencarl:e2e
4. [ ] Zero CARL_DEBUG references in CI configuration
5. [ ] Zero opencode-carl:e2e references
6. [ ] YAML syntax is valid
7. [ ] All required workflow keys present
8. [ ] Consistent naming throughout workflow
</verification>

<success_criteria>
1. All CI configuration files (.github/workflows/*.yml) use OPENCARL_DEBUG instead of CARL_DEBUG
2. All Docker image references use opencode-opencarl:e2e instead of opencode-carl:e2e
3. Zero CARL_DEBUG references remain in CI configurations
4. YAML syntax is valid and workflow can be parsed
5. All required workflow elements present and configured
</success_criteria>

<output>
After completion, create `.planning/phases/15-env-variable-rebranding/15-02-SUMMARY.md`
</output>
