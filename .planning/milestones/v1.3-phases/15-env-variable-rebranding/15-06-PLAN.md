---
phase: 15-env-variable-rebranding
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/e2e-tests.yml
  - .github/workflows/test.yml
  - tests/javascript/e2e/keyword-matching.test.ts
  - tests/javascript/e2e/setup-flow.test.ts
  - tests/javascript/e2e/star-command.test.ts
  - tests/e2e/run-e2e-tests.sh.backup
autonomous: true
requirements: [ENV-02, ENV-03]
gap_closure: true

must_haves:
  truths:
    - "CI workflows define OPENCARL_DEBUG where debug env is expected"
    - "All test scripts use opencode-opencarl:e2e Docker image"
  artifacts:
    - path: ".github/workflows/e2e-tests.yml"
      provides: "CI workflow env includes OPENCARL_DEBUG"
    - path: ".github/workflows/test.yml"
      provides: "CI workflow env includes OPENCARL_DEBUG"
    - path: "tests/javascript/e2e/keyword-matching.test.ts"
      provides: "E2E test image name uses opencode-opencarl:e2e"
    - path: "tests/e2e/run-e2e-tests.sh.backup"
      provides: "Backup script uses opencode-opencarl:e2e image name"
  key_links:
    - from: ".github/workflows/e2e-tests.yml"
      to: "OPENCARL_DEBUG"
      via: "env definition"
      pattern: "OPENCARL_DEBUG"
    - from: "tests/javascript/e2e/*.test.ts"
      to: "opencode-opencarl:e2e"
      via: "IMAGE_NAME literal"
      pattern: "opencode-opencarl:e2e"
---

<objective>
Align CI workflows and test scripts with OPENCARL_DEBUG and the opencode-opencarl:e2e image.

Purpose: Close verification gaps in CI env usage and test image references.
Output: Updated workflows and tests with consistent env and image names.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-env-variable-rebranding/15-CONTEXT.md
@.planning/phases/15-env-variable-rebranding/15-RESEARCH.md
@.planning/phases/15-env-variable-rebranding/15-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: add OPENCARL_DEBUG to CI workflows</name>
  <files>.github/workflows/e2e-tests.yml, .github/workflows/test.yml</files>
  <action>Add OPENCARL_DEBUG environment variable usage in CI workflows where debug env vars are defined or expected. Keep existing workflow structure intact and ensure no CARL_DEBUG references are introduced.</action>
  <verify>
    <automated>rg -n "OPENCARL_DEBUG" .github/workflows/e2e-tests.yml .github/workflows/test.yml</automated>
  </verify>
  <done>CI workflows include OPENCARL_DEBUG in their environment configuration.</done>
</task>

<task type="auto">
  <name>task 2: update E2E test image references</name>
  <files>tests/javascript/e2e/keyword-matching.test.ts, tests/javascript/e2e/setup-flow.test.ts, tests/javascript/e2e/star-command.test.ts, tests/e2e/run-e2e-tests.sh.backup</files>
  <action>Replace opencode-carl:e2e with opencode-opencarl:e2e in E2E test files and the backup runner script. Preserve existing constants/variables and ensure no old image references remain.</action>
  <verify>
    <automated>rg -n "opencode-opencarl:e2e" tests/javascript/e2e tests/e2e/run-e2e-tests.sh.backup && ! rg -n "opencode-carl:e2e" tests/javascript/e2e tests/e2e/run-e2e-tests.sh.backup</automated>
  </verify>
  <done>All E2E test references use opencode-opencarl:e2e and no opencode-carl:e2e references remain.</done>
</task>

</tasks>

<verification>
CI workflows reference OPENCARL_DEBUG and all E2E test files use opencode-opencarl:e2e.
</verification>

<success_criteria>
CI and test scripts are aligned with OPENCARL_DEBUG and opencode-opencarl:e2e naming.
</success_criteria>

<output>
After completion, create `.planning/phases/15-env-variable-rebranding/15-06-SUMMARY.md`
</output>
