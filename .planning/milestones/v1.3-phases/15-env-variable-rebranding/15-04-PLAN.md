---
phase: 15-env-variable-rebranding
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - TROUBLESHOOTING.md
  - INSTALL.md
  - resources/docs/CARL-DOCS.md
autonomous: true
requirements: [ENV-04]
must_haves:
  truths:
    - "All documentation references OPENCARL_DEBUG instead of CARL_DEBUG"
    - "Documentation includes prominent breaking change notice"
    - "Zero CARL_DEBUG references remain in documentation"
  artifacts:
    - path: "README.md"
      provides: "User-facing documentation with OPENCARL_DEBUG examples"
      contains: "OPENCARL_DEBUG=true"
    - path: "TROUBLESHOOTING.md"
      provides: "Troubleshooting guide referencing OPENCARL_DEBUG"
      contains: "OPENCARL_DEBUG"
    - path: "INSTALL.md"
      provides: "Install guide with OPENCARL_DEBUG examples"
      contains: "OPENCARL_DEBUG"
    - path: "resources/docs/CARL-DOCS.md"
      provides: "Technical docs aligned with OPENCARL_DEBUG"
      contains: "OPENCARL_DEBUG"
  key_links:
    - from: "README.md"
      to: "TROUBLESHOOTING.md"
      via: "Debug mode documentation cross-reference"
      pattern: "See TROUBLESHOOTING.md for debugging options"
---

<objective>
Update documentation files to use OPENCARL_DEBUG and include prominent breaking change notices.

Purpose: Ensure user-facing documentation reflects the environment variable rename, with clear guidance for users to update their configurations.

Output:
- README.md updated with OPENCARL_DEBUG examples and breaking change notice
- TROUBLESHOOTING.md updated with OPENCARL_DEBUG references
- Zero CARL_DEBUG references remain in documentation
- Documentation clearly communicates the breaking change
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-env-variable-rebranding/15-CONTEXT.md
@.planning/phases/15-env-variable-rebranding/15-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Context from prior phases:
- Phase 13 (config): .carl/ renamed to .opencarl/
- Phase 15 CONTEXT.md: Documentation updated, include documentation and code comments
- Phase 15 RESEARCH.md: Documentation uses code blocks with VAR_NAME=true syntax
</context>

<tasks>

<task type="auto">
  <name>Update README.md with OPENCARL_DEBUG and breaking change notice</name>
  <files>README.md</files>
  <action>
    Update README.md to use OPENCARL_DEBUG and communicate breaking change:

    1. **Add prominent breaking change notice at the top of README.md**:
       ```markdown
       # 🚨 Breaking Change Notice

       Environment variable renamed: **CARL_DEBUG → OPENCARL_DEBUG** (immediate, no deprecation)

       If you have CARL_DEBUG set in your environment or CI configuration, you must update to OPENCARL_DEBUG immediately.

       See: TROUBLESHOOTING.md for migration instructions
       ```

    2. Find all code blocks showing CARL_DEBUG and replace with OPENCARL_DEBUG:
       ```bash
       CARL_DEBUG=true  →  OPENCARL_DEBUG=true
       ```

    3. Find all descriptions mentioning "CARL_DEBUG environment variable" and replace with "OPENCARL_DEBUG environment variable"

    4. Update section headers if they reference CARL_DEBUG

    5. Ensure all code examples show OPENCARL_DEBUG=true

    6. Check for any other CARL_DEBUG references in text (not just code blocks)
  </action>
  <verify>
    <automated>rg "CARL_DEBUG" README.md && echo "ERROR: CARL_DEBUG still exists" || echo "SUCCESS: Zero CARL_DEBUG in README.md"</automated>
    <automated>rg "OPENCARL_DEBUG" README.md && echo "SUCCESS: OPENCARL_DEBUG present"</automated>
    <automated>rg "Environment variable renamed: CARL_DEBUG" README.md && echo "SUCCESS: Breaking change notice present"</automated>
    <manual>Open README.md: Verify breaking change notice at top, check all code blocks show OPENCARL_DEBUG=true, review text for consistency</manual>
    <sampling_rate>After file edit, before next task</sampling_rate>
  </verify>
  <done>README.md updated with OPENCARL_DEBUG examples, prominent breaking change notice at top, zero CARL_DEBUG references</done>
</task>

<task type="auto">
  <name>Update TROUBLESHOOTING.md with OPENCARL_DEBUG</name>
  <files>TROUBLESHOOTING.md</files>
  <action>
    Update TROUBLESHOOTING.md to use OPENCARL_DEBUG:

    1. Find all debug mode examples using CARL_DEBUG and replace with OPENCARL_DEBUG:
       ```bash
       CARL_DEBUG=true  →  OPENCARL_DEBUG=true
       ```

    2. Update all section headers if they reference CARL_DEBUG

    3. Ensure all troubleshooting steps mention OPENCARL_DEBUG consistently

    4. Check for any other CARL_DEBUG references in code blocks or text

    5. Add cross-reference to README.md's breaking change notice if not already present

    6. Ensure all code examples show OPENCARL_DEBUG=true
  </action>
  <verify>
    <automated>rg "CARL_DEBUG" TROUBLESHOOTING.md && echo "ERROR: CARL_DEBUG still exists" || echo "SUCCESS: Zero CARL_DEBUG in TROUBLESHOOTING.md"</automated>
    <automated>rg "OPENCARL_DEBUG" TROUBLESHOOTING.md && echo "SUCCESS: OPENCARL_DEBUG present"</automated>
    <automated>rg "OPENCARL_DEBUG=true" TROUBLESHOOTING.md && echo "SUCCESS: OPENCARL_DEBUG examples present"</automated>
    <manual>Open TROUBLESHOOTING.md: Verify all debug mode examples show OPENCARL_DEBUG=true, check all sections consistent, review for any missed references</manual>
    <sampling_rate>After file edit, before verification task</sampling_rate>
  </verify>
  <done>TROUBLESHOOTING.md updated with OPENCARL_DEBUG examples, zero CARL_DEBUG references, consistent with README.md</done>
</task>

<task type="auto">
  <name>Comprehensive documentation verification</name>
  <files>README.md TROUBLESHOOTING.md INSTALL.md resources/docs/CARL-DOCS.md</files>
  <action>
    Verify all documentation is updated and consistent:

    1. Run comprehensive grep search:
       ```bash
        rg -i "CARL_DEBUG" README.md TROUBLESHOOTING.md INSTALL.md resources/docs/CARL-DOCS.md && echo "ERROR: CARL_DEBUG found" || echo "SUCCESS: Zero CARL_DEBUG in docs"
        ```

    2. Verify README.md contains breaking change notice at top

    3. Verify README.md has OPENCARL_DEBUG examples in code blocks

    4. Verify TROUBLESHOOTING.md has OPENCARL_DEBUG examples

    5. Check cross-references between README.md and TROUBLESHOOTING.md

    6. Ensure consistent naming throughout both documents

    7. If CARL_DEBUG appears in INSTALL.md or resources/docs/CARL-DOCS.md, update those files to use OPENCARL_DEBUG examples and language.
    8. Verify no other documentation files need updates after fixes.
  </action>
  <verify>
    <automated>rg -i "CARL_DEBUG" README.md TROUBLESHOOTING.md INSTALL.md resources/docs/CARL-DOCS.md && echo "ERROR: CARL_DEBUG found in docs" || echo "SUCCESS: Zero CARL_DEBUG in documentation files"</automated>
    <automated>rg "Environment variable renamed" README.md && echo "SUCCESS: Breaking change notice present"</automated>
    <automated>rg "OPENCARL_DEBUG=true" README.md TROUBLESHOOTING.md INSTALL.md resources/docs/CARL-DOCS.md && echo "SUCCESS: OPENCARL_DEBUG examples present"</automated>
    <manual>Review documentation consistency: Open both files, check all sections, verify cross-references, ensure consistent language</manual>
    <sampling_rate>After verification, before wave 2 begins</sampling_rate>
  </verify>
  <done>README.md, TROUBLESHOOTING.md, INSTALL.md, and resources/docs/CARL-DOCS.md are updated with OPENCARL_DEBUG and verified to contain no CARL_DEBUG references.</done>
</task>

</tasks>

<verification>
## Verification Checklist

1. [ ] README.md updated with OPENCARL_DEBUG examples
2. [ ] README.md has prominent breaking change notice at top
3. [ ] README.md has zero CARL_DEBUG references
4. [ ] TROUBLESHOOTING.md updated with OPENCARL_DEBUG examples
5. [ ] TROUBLESHOOTING.md has zero CARL_DEBUG references
6. [ ] All code blocks show OPENCARL_DEBUG=true
7. [ ] Descriptions consistently use OPENCARL_DEBUG environment variable
8. [ ] Cross-references between README.md and TROUBLESHOOTING.md correct
9. [ ] Other documentation files reviewed for CARL_DEBUG references
10. [ ] Consistent naming throughout all documentation
</verification>

<success_criteria>
1. All documentation files (README.md, TROUBLESHOOTING.md) use OPENCARL_DEBUG instead of CARL_DEBUG
2. README.md has prominent breaking change notice at the top
3. Zero CARL_DEBUG references remain in documentation files
4. All code examples show OPENCARL_DEBUG=true
5. Descriptions consistently reference OPENCARL_DEBUG environment variable
6. Cross-references between documentation files are correct
7. Consistent naming throughout all documentation
</success_criteria>

<output>
After completion, create `.planning/phases/15-env-variable-rebranding/15-04-SUMMARY.md`
</output>
