---
phase: 15-env-variable-rebranding
plan: 01
type: execute
wave: 2
depends_on: ["15-07"]
files_modified:
  - src/opencarl/debug.ts
  - dist/opencarl/debug.js
autonomous: true
requirements: [ENV-01]
must_haves:
  truths:
    - "All source code uses OPENCARL_DEBUG instead of CARL_DEBUG"
    - "All generated distribution files (dist/) use OPENCARL_DEBUG"
    - "Inline code comments rename CARL_DEBUG strings"
    - "Zero CARL_DEBUG references remain in source code and dist files (except the explicit deprecation warning)"
  artifacts:
    - path: "src/opencarl/debug.ts"
      provides: "Source file with OPENCARL_DEBUG implementation"
      contains: "process.env.OPENCARL_DEBUG === \"true\""
    - path: "dist/opencarl/debug.js"
      provides: "Compiled distribution file with OPENCARL_DEBUG"
      contains: "process.env.OPENCARL_DEBUG === \"true\""
  key_links:
    - from: "src/opencarl/debug.ts"
      to: "dist/opencarl/debug.js"
      via: "Build output from TypeScript compilation"
      pattern: "dist.*\.js"
---

<objective>
Rename environment variable references from CARL_DEBUG to OPENCARL_DEBUG in source code and generated distribution files.

Purpose: Ensure all TypeScript source files and compiled JavaScript distribution files use the new environment variable name, maintaining consistency across the codebase.

Output:
- src/opencarl/debug.ts updated with OPENCARL_DEBUG
- dist/opencarl/debug.js updated with OPENCARL_DEBUG
- All inline code comments updated with OPENCARL_DEBUG references
- Build verified to compile successfully with zero errors
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-env-variable-rebranding/15-CONTEXT.md
@.planning/phases/15-env-variable-rebranding/15-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Context from prior phases:
- Phase 12 (source code): All TypeScript type names, function/variable names, and imports updated
- Phase 13 (config): .carl/ renamed to .opencarl/, .carl-template/ renamed to .opencarl-template/
- Phase 14 (commands): *carl renamed to *opencarl, /carl renamed to /opencarl
- Phase 15 CONTEXT.md: Include documentation and code comments, comprehensive grep verification required
</context>

<tasks>

<task type="auto">
  <name>Update source code with OPENCARL_DEBUG</name>
  <files>
    src/opencarl/debug.ts
  </files>
  <action>
    Update all references to CARL_DEBUG to OPENCARL_DEBUG in source code:

    src/opencarl/debug.ts:
    - Find all `process.env.CARL_DEBUG` patterns → replace with `process.env.OPENCARL_DEBUG`
    - Update any string literals like `"CARL_DEBUG"` → `"OPENCARL_DEBUG"` in comments or docstrings
    - CRITICAL: Rename CARL_DEBUG strings in inline code comments (e.g., "// CARL_DEBUG environment variable")
    - Verify all references use consistent SCREAMING_SNAKE_CASE naming
    - Ensure type safety is maintained (TypeScript will catch type mismatches)

    Use precise find-and-replace for each CARL_DEBUG occurrence. Verify no old references remain.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const text=fs.readFileSync('src/opencarl/debug.ts','utf8'); if(!text.includes('CARL_DEBUG is deprecated, please use OPENCARL_DEBUG')){console.error('Missing warning');process.exit(1)}; const matches=(text.match(/\\bCARL_DEBUG\\b/g)||[]).length; if(matches>1){console.error('Unexpected CARL_DEBUG references');process.exit(1)}"</automated>
    <manual>Manual review: Open src/opencarl/debug.ts, verify all process.env references use OPENCARL_DEBUG, check comments for renamed strings, ensure type safety</manual>
    <sampling_rate>After source file write, before building distribution</sampling_rate>
  </verify>
  <done>All references to CARL_DEBUG in source code replaced with OPENCARL_DEBUG, including inline code comments and process.env access patterns</done>
</task>

<task type="auto">
  <name>Build and update distribution files</name>
  <files>
    dist/opencarl/debug.js
  </files>
  <action>
    Rebuild the project to generate updated distribution files:

    1. Run TypeScript compiler to generate new JavaScript files:
       ```bash
       npm run build
       # or
       npm run tsc
       ```

    2. Verify dist/opencarl/debug.js was generated/updated with OPENCARL_DEBUG:
       - Check that process.env.OPENCARL_DEBUG appears in compiled output
       - Ensure no CARL_DEBUG references remain in dist/ directory

    3. If TypeScript compilation produces errors, fix them before proceeding:
       - Check src/opencarl/debug.ts for any syntax errors introduced by the rename
       - Verify all type annotations are correct after string changes

    4. Verify build output integrity:
       - Check that dist/opencarl/debug.js is not empty
       - Verify the file is syntactically valid JavaScript
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const text=fs.readFileSync('dist/opencarl/debug.js','utf8'); if(!text.includes('CARL_DEBUG is deprecated, please use OPENCARL_DEBUG')){console.error('Missing warning');process.exit(1)}; const matches=(text.match(/\\bCARL_DEBUG\\b/g)||[]).length; if(matches>1){console.error('Unexpected CARL_DEBUG references');process.exit(1)}"</automated>
    <automated>npm run build 2>&1 | grep -E "(error|Error)" || echo "BUILD SUCCESSFUL: No compilation errors"</automated>
    <manual>Manual review: Open dist/opencarl/debug.js, verify process.env.OPENCARL_DEBUG is present, check file integrity (not empty, valid syntax)</manual>
    <sampling_rate>After build, before next task wave</sampling_rate>
  </verify>
  <done>Distribution files (dist/opencarl/debug.js) generated successfully with OPENCARL_DEBUG, no CARL_DEBUG references remain, zero compilation errors</done>
</task>

<task type="auto">
  <name>Verify complete source code coverage</name>
  <files>
    src/**/*.{ts,js}
    dist/**/*.{js,ts}
  </files>
  <action>
    Comprehensive verification that CARL_DEBUG has been fully removed from source and distribution:

    Run grep search across all source and dist files:
    ```bash
    rg "CARL_DEBUG" \
      src/**/*.{ts,js} \
      dist/**/*.{js,ts} \
      --glob '!node_modules' \
      --glob '!dist/**/*.map'
    ```

    Expected result: Zero matches (exit code 1 or no output)

    If matches found:
    - Identify all files with CARL_DEBUG references
    - Manually review each file
    - Update CARL_DEBUG → OPENCARL_DEBUG in each file
    - Re-run grep until zero matches

    Do NOT include dist/**/*.map (source maps are automatically regenerated)
  </action>
  <verify>
    <automated>rg "\\bCARL_DEBUG\\b" src/**/*.{ts,js} dist/**/*.{js,ts} --glob '!node_modules' --glob '!dist/**/*.map' --glob '!src/opencarl/debug.ts' && echo "ERROR: CARL_DEBUG references still exist in source/distribution" || echo "SUCCESS: Zero CARL_DEBUG references outside the deprecation warning"</automated>
    <manual>Manual review: Review grep output (should be empty). Check that comments were updated, no inline strings remain with old name</manual>
    <sampling_rate>After verification task, before wave 2 begins</sampling_rate>
  </verify>
  <done>Comprehensive grep search confirms zero CARL_DEBUG references across all source code (src/ and dist/), including comments and inline strings</done>
</task>

</tasks>

<verification>
## Verification Checklist

1. [ ] src/opencarl/debug.ts uses process.env.OPENCARL_DEBUG
2. [ ] Inline code comments in source code renamed CARL_DEBUG → OPENCARL_DEBUG
3. [ ] dist/opencarl/debug.js uses process.env.OPENCARL_DEBUG
4. [ ] CARL_DEBUG appears only in the deprecation warning in src/opencarl/debug.ts
5. [ ] CARL_DEBUG appears only in the deprecation warning in dist/opencarl/debug.js
6. [ ] TypeScript compilation succeeds with zero errors
7. [ ] dist/ directory is not empty after build
8. [ ] All grep searches for CARL_DEBUG return zero results
</verification>

<success_criteria>
1. All source code files (src/) use OPENCARL_DEBUG instead of CARL_DEBUG
2. All distribution files (dist/) use OPENCARL_DEBUG instead of CARL_DEBUG
3. Inline code comments and strings are updated with new name
4. TypeScript compilation succeeds with zero errors
5. Comprehensive grep verification confirms zero CARL_DEBUG references outside the deprecation warning
</success_criteria>

<output>
After completion, create `.planning/phases/15-env-variable-rebranding/15-01-SUMMARY.md`
</output>
