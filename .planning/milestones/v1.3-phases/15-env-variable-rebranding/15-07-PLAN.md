---
phase: 15-env-variable-rebranding
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/opencarl/debug.ts
  - package.json
autonomous: true
requirements: [ENV-01]
gap_closure: true

must_haves:
  truths:
    - "CARL_DEBUG usage emits a deprecation warning to stderr"
    - "Package version reflects a breaking change bump"
  artifacts:
    - path: "src/opencarl/debug.ts"
      provides: "Deprecation warning for CARL_DEBUG"
    - path: "package.json"
      provides: "Semver bump for breaking change"
  key_links:
    - from: "process.env.CARL_DEBUG"
      to: "console.error"
      via: "deprecation warning"
      pattern: "CARL_DEBUG is deprecated, please use OPENCARL_DEBUG"
---

<objective>
Add the required deprecation warning and bump the package version for the breaking env var rename.

Purpose: Honor locked decisions for immediate deprecation messaging and semver bump.
Output: Updated debug warning logic and package.json version.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-env-variable-rebranding/15-CONTEXT.md
@.planning/phases/15-env-variable-rebranding/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: emit CARL_DEBUG deprecation warning</name>
  <files>src/opencarl/debug.ts</files>
  <action>Add a warning when CARL_DEBUG is detected: if process.env.CARL_DEBUG is set, emit `console.error("CARL_DEBUG is deprecated, please use OPENCARL_DEBUG")` once at module initialization. Do not reintroduce CARL_DEBUG support for behavior; only warn and continue to use OPENCARL_DEBUG for gating.</action>
  <verify>
    <automated>rg -n "CARL_DEBUG is deprecated, please use OPENCARL_DEBUG" src/opencarl/debug.ts</automated>
  </verify>
  <done>CARL_DEBUG triggers a one-time stderr warning and OPENCARL_DEBUG remains the only functional flag.</done>
</task>

<task type="auto">
  <name>task 2: bump version for breaking change</name>
  <files>package.json</files>
  <action>Bump package.json version to a breaking-change semver (use MAJOR bump to 2.0.0). Do not change other metadata.</action>
  <verify>
    <automated>node -e "const pkg=require('./package.json'); if(pkg.version !== '2.0.0') {console.error('Expected version 2.0.0'); process.exit(1)}"</automated>
  </verify>
  <done>package.json version is 2.0.0.</done>
</task>

</tasks>

<verification>
Deprecation warning message exists and package.json reflects a MAJOR bump.
</verification>

<success_criteria>
The deprecated env var warning is in place and the package version signals the breaking change.
</success_criteria>

<output>
After completion, create `.planning/phases/15-env-variable-rebranding/15-07-SUMMARY.md`
</output>
