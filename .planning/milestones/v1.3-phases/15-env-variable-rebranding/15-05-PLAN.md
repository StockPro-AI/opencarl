---
phase: 15-env-variable-rebranding
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/carl/debug.js
  - dist/carl/debug.d.ts
  - dist/opencarl/debug.js
  - dist/opencarl/debug.d.ts
  - dist/plugin.js
  - dist/plugin.d.ts
autonomous: true
requirements: [ENV-01]
gap_closure: true

must_haves:
  truths:
    - "All generated dist/ outputs use OPENCARL_DEBUG, with any CARL_DEBUG limited to the deprecation warning"
    - "Legacy dist/carl artifacts are removed or regenerated without legacy CARL_DEBUG usage"
  artifacts:
    - path: "dist/carl/debug.js"
      provides: "Legacy debug artifact removed or updated"
    - path: "dist/carl/debug.d.ts"
      provides: "Legacy debug type artifact removed or updated"
    - path: "dist/opencarl/debug.js"
      provides: "Compiled debug output with OPENCARL_DEBUG"
  key_links:
    - from: "npm run build"
      to: "dist/opencarl/debug.js"
      via: "TypeScript build output"
      pattern: "OPENCARL_DEBUG"
---

<objective>
Remove legacy dist/carl artifacts and regenerate dist outputs without CARL_DEBUG.

Purpose: Close verification gaps where dist/ still contains CARL_DEBUG references.
Output: Updated dist outputs with only OPENCARL_DEBUG references.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-env-variable-rebranding/15-CONTEXT.md
@.planning/phases/15-env-variable-rebranding/15-RESEARCH.md
@.planning/phases/15-env-variable-rebranding/15-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: remove legacy dist/carl artifacts</name>
  <files>dist/carl/debug.js, dist/carl/debug.d.ts</files>
  <action>Delete the legacy dist/carl artifacts so stale CARL_DEBUG references cannot ship. If dist/carl is regenerated by build tooling, ensure any regenerated output uses OPENCARL_DEBUG only (no CARL_DEBUG tokens in code or comments).</action>
  <verify>
    <automated>test ! -d dist/carl</automated>
  </verify>
  <done>dist/carl directory no longer exists or contains no CARL_DEBUG references.</done>
</task>

<task type="auto">
  <name>task 2: rebuild dist outputs and verify no CARL_DEBUG</name>
  <files>dist/opencarl/debug.js, dist/opencarl/debug.d.ts, dist/plugin.js, dist/plugin.d.ts</files>
  <action>Run the build to regenerate distribution outputs. Confirm dist files reference OPENCARL_DEBUG only and that no CARL_DEBUG tokens remain anywhere under dist/.</action>
  <verify>
    <automated>npm run build && node -e "const {execSync}=require('child_process'); const out=execSync('rg -n \\\"\\\\bCARL_DEBUG\\\\b\\\" dist/ || true',{stdio:'pipe'}).toString().trim(); if(!out){process.exit(0)}; const bad=out.split('\\n').filter(line=>!line.includes('CARL_DEBUG is deprecated, please use OPENCARL_DEBUG')); if(bad.length){console.error(bad.join('\\n')); process.exit(1)}"</automated>
  </verify>
  <done>Build succeeds and any CARL_DEBUG references in dist/ are limited to the deprecation warning string.</done>
</task>

</tasks>

<verification>
npm run build completes successfully and dist/ contains no CARL_DEBUG references.
</verification>

<success_criteria>
dist/ contains only OPENCARL_DEBUG references and no dist/carl artifacts remain with CARL_DEBUG.
</success_criteria>

<output>
After completion, create `.planning/phases/15-env-variable-rebranding/15-05-SUMMARY.md`
</output>
