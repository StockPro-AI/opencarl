---
phase: 15-env-variable-rebranding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/run-e2e-tests.sh
autonomous: true
requirements: [ENV-03]
must_haves:
  truths:
    - "All test scripts use OPENCARL_DEBUG instead of CARL_DEBUG"
    - "Test scripts use opencode-opencarl:e2e Docker image"
    - "Zero CARL_DEBUG references remain in test scripts"
  artifacts:
    - path: "tests/e2e/run-e2e-tests.sh"
      provides: "E2E test runner with updated Docker image and env var"
      contains: "IMAGE_NAME='opencode-opencarl:e2e'"
      contains: "OPENCARL_DEBUG"
  key_links:
    - from: "tests/e2e/run-e2e-tests.sh"
      to: ".github/workflows/e2e-tests.yml"
      via: "Docker image reference shared between files"
      pattern: "IMAGE_NAME.*opencode-opencarl:e2e"
---

<objective>
Update test scripts to use OPENCARL_DEBUG and updated Docker image naming convention.

Purpose: Ensure E2E test scripts use the new environment variable name and Docker image, maintaining consistent naming across testing infrastructure.

Output:
- tests/e2e/run-e2e-tests.sh updated with OPENCARL_DEBUG and opencode-opencarl:e2e
- All CARL_DEBUG references removed from test scripts
- Test script verified to be syntactically correct and functional
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-env-variable-rebranding/15-CONTEXT.md
@.planning/phases/15-env-variable-rebranding/15-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Context from prior phases:
- Phase 13 (config): .carl/ renamed to .opencarl/
- Phase 15 CONTEXT.md: Test scripts updated, include documentation and code comments
- Phase 15 RESEARCH.md: Test scripts use Docker image naming pattern opencode-{product}:e2e
</context>

<tasks>

<task type="auto">
  <name>Update test script with OPENCARL_DEBUG and opencode-opencarl:e2e</name>
  <files>tests/e2e/run-e2e-tests.sh</files>
  <action>
    Update tests/e2e/run-e2e-tests.sh to use new environment variable and Docker image:

    1. Find all references to "CARL_DEBUG" and replace with "OPENCARL_DEBUG"
    2. Find IMAGE_NAME variable and update from "opencode-carl:e2e" to "opencode-opencarl:e2e"
    3. **CRITICAL: Rename CARL_DEBUG strings in inline code comments in shell script**
       - Find all comments containing "CARL_DEBUG" and replace with "OPENCARL_DEBUG"
       - Examples: "# CARL_DEBUG=true", "# Debug mode: CARL_DEBUG"
    4. Check for any environment variable references to CARL_DEBUG in command lines
    5. Verify shell script syntax is correct
    6. Ensure Docker commands use the updated IMAGE_NAME variable

    Use targeted edits for specific patterns:
    - Variable definitions: IMAGE_NAME='...' → 'opencode-opencarl:e2e'
    - Environment variable references: CARL_DEBUG → OPENCARL_DEBUG
    - Shell comments: CARL_DEBUG → OPENCARL_DEBUG
    - String literals: CARL_DEBUG → OPENCARL_DEBUG
  </action>
  <verify>
    <automated>rg "CARL_DEBUG" tests/e2e/run-e2e-tests.sh && echo "ERROR: CARL_DEBUG still exists" || echo "SUCCESS: Zero CARL_DEBUG in test script"</automated>
    <automated>rg "opencode-carl:e2e" tests/e2e/run-e2e-tests.sh && echo "ERROR: Old Docker image still exists" || echo "SUCCESS: Old Docker image removed"</automated>
    <automated>rg "IMAGE_NAME='opencode-opencarl:e2e'" tests/e2e/run-e2e-tests.sh && echo "SUCCESS: OPENCARL_DEBUG image name present"</automated>
    <automated>sh -n tests/e2e/run-e2e-tests.sh && echo "SUCCESS: Shell script syntax valid" || echo "ERROR: Shell syntax error"</automated>
    <manual>Open tests/e2e/run-e2e-tests.sh: Verify IMAGE_NAME variable, check Docker commands, ensure all env var references correct</manual>
    <sampling_rate>After file edit, before next task</sampling_rate>
  </verify>
  <done>tests/e2e/run-e2e-tests.sh updated with OPENCARL_DEBUG and opencode-opencarl:e2e, zero CARL_DEBUG references, shell syntax valid</done>
</task>

<task type="auto">
  <name>Verify test script functionality and references</name>
  <files>tests/e2e/run-e2e-tests.sh</files>
  <action>
    Verify test script is correct and consistent:

    1. Check all Docker commands use IMAGE_NAME variable:
       - Verify docker build uses "$IMAGE_NAME"
       - Verify docker run uses "$IMAGE_NAME"

    2. Check all environment variable references are correct:
       - Verify all CARL_DEBUG replaced with OPENCARL_DEBUG
       - Check for any remaining hardcoded env var names

    3. Verify script flow is correct:
       - Script initializes variables
       - Docker image is built
       - Tests run
       - Cleanup happens

    4. Check for any documentation or help text in script that might need updating

    5. Ensure consistency with .github/workflows/e2e-tests.yml
  </action>
  <verify>
    <automated>grep -n "IMAGE_NAME" tests/e2e/run-e2e-tests.sh | grep -v "^#" && echo "SUCCESS: IMAGE_NAME variable used"</automated>
    <automated>grep -n "docker build.*\$IMAGE_NAME" tests/e2e/run-e2e-tests.sh && echo "SUCCESS: docker build uses IMAGE_NAME"</automated>
    <automated>grep -n "docker run.*\$IMAGE_NAME" tests/e2e/run-e2e-tests.sh && echo "SUCCESS: docker run uses IMAGE_NAME"</automated>
    <manual>Review test script logic: Verify Docker commands, env var usage, script flow, consistency with CI workflow</manual>
    <sampling_rate>After verification, before wave 2 begins</sampling_rate>
  </verify>
  <done>Test script verified to use correct Docker image and environment variable, consistent with CI workflow, script logic intact</done>
</task>

</tasks>

<verification>
## Verification Checklist

1. [ ] tests/e2e/run-e2e-tests.sh uses OPENCARL_DEBUG
2. [ ] Inline code comments in test script contain OPENCARL_DEBUG
3. [ ] IMAGE_NAME variable uses opencode-opencarl:e2e
4. [ ] Docker commands use $IMAGE_NAME variable
5. [ ] Zero CARL_DEBUG references in test script
6. [ ] Zero opencode-carl:e2e references
7. [ ] Shell script syntax valid
8. [ ] Consistent with CI workflow configuration
9. [ ] All Docker commands correct
10. [ ] Environment variable usage consistent
</verification>

<success_criteria>
1. All test scripts use OPENCARL_DEBUG instead of CARL_DEBUG
2. All Docker image references use opencode-opencarl:e2e instead of opencode-carl:e2e
3. Zero CARL_DEBUG references remain in test scripts
4. Shell script syntax is valid and executable
5. All Docker commands correctly use the IMAGE_NAME variable
6. Consistent naming between test scripts and CI configurations
</success_criteria>

<output>
After completion, create `.planning/phases/15-env-variable-rebranding/15-03-SUMMARY.md`
</output>
