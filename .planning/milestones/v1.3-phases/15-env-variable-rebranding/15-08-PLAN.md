---
phase: 15-env-variable-rebranding
plan: 08
type: execute
wave: 3
depends_on: ["15-01", "15-02", "15-03", "15-04", "15-05", "15-06", "15-07"]
files_modified: []
autonomous: true
requirements: [ENV-01, ENV-02, ENV-03, ENV-04]
gap_closure: true

must_haves:
  truths:
    - "Repo-wide search finds zero CARL_DEBUG references outside the deprecation warning"
    - "Full test suite passes with OPENCARL_DEBUG set"
  artifacts:
    - path: "."
      provides: "Repository-wide verification across required file types"
  key_links:
    - from: "rg -n \\"\\bCARL_DEBUG\\b\\""
      to: "repo root"
      via: "file-type scoped search"
      pattern: "\\bCARL_DEBUG\\b"
---

<objective>
Verify zero CARL_DEBUG references remain and confirm tests pass under OPENCARL_DEBUG.

Purpose: Satisfy verification requirements for repo-wide checks and test suite validation.
Output: Verified search results and passing test suite.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-env-variable-rebranding/15-CONTEXT.md
@.planning/phases/15-env-variable-rebranding/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: repo-wide CARL_DEBUG search (filtered)</name>
  <files>.</files>
  <action>Run a word-boundary search for CARL_DEBUG across required file types, excluding generated dist/ and node_modules/, and excluding src/opencarl/debug.ts (allowed deprecation warning). Confirm the warning exists in src/opencarl/debug.ts and that no other matches remain.</action>
  <verify>
    <automated>rg -n "CARL_DEBUG is deprecated, please use OPENCARL_DEBUG" src/opencarl/debug.ts</automated>
    <automated>rg -n "\\bCARL_DEBUG\\b" -g "*.ts" -g "*.js" -g "*.json" -g "*.yaml" -g "*.yml" -g "*.sh" -g "*.md" --glob "!dist/**" --glob "!node_modules/**" --glob "!src/opencarl/debug.ts"</automated>
    <manual>Use IDE “Find in Files” for CARL_DEBUG across the repo; confirm only the deprecation warning remains.</manual>
  </verify>
  <done>Warning exists in src/opencarl/debug.ts and word-boundary search finds no other CARL_DEBUG matches outside dist/ and node_modules/.</done>
</task>

<task type="auto">
  <name>task 2: run full test suite with OPENCARL_DEBUG</name>
  <files>.</files>
  <action>Run the full test suite with zero failures, including a run with OPENCARL_DEBUG enabled.</action>
  <verify>
    <automated>npm run test:coverage && OPENCARL_DEBUG=true npm test</automated>
  </verify>
  <done>All tests pass in both normal and OPENCARL_DEBUG-enabled runs.</done>
</task>

</tasks>

<verification>
Repo-wide search (scoped) returns zero CARL_DEBUG and full test suite passes with OPENCARL_DEBUG enabled.
</verification>

<success_criteria>
All verification steps complete with no CARL_DEBUG findings and zero test failures.
</success_criteria>

<output>
After completion, create `.planning/phases/15-env-variable-rebranding/15-08-SUMMARY.md`
</output>
