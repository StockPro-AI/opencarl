---
phase: 16-documentation-rebranding
plan: 04
type: execute
wave: 2
depends_on: ["16-03"]
files_modified:
  - tests/fixtures/README.md
autonomous: true
requirements: [DOCS-05]
gap_closure: true
must_haves:
  truths:
    - "Supplemental docs and fixture README examples use the OpenCARL naming conventions."
  artifacts:
    - path: "tests/fixtures/README.md"
      provides: "Fixture README uses OpenCARL naming in examples"
      contains: "*opencarl"
  key_links:
    - from: "tests/fixtures/README.md"
      to: "*opencarl"
      via: "command example code block"
      pattern: "\\*opencarl"
---

<objective>
Ensure the fixture README examples use OpenCARL naming without expanding content.

Purpose: Close the Phase 16 verification gap for DOCS-05 while respecting the minimal rename scope.
Output: Updated `tests/fixtures/README.md` with legacy terms replaced in existing examples.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation-rebranding/16-VERIFICATION.md
@tests/fixtures/README.md
</context>

<tasks>

<task type="auto">
  <name>task 1: update fixture README examples to OpenCARL naming</name>
  <files>tests/fixtures/README.md</files>
  <action>
Replace legacy CARL references in existing examples with OpenCARL naming conventions. Update any command examples to use `*opencarl` or `/opencarl`, paths to `.opencarl/` and `.opencarl-template/`, env vars to `OPENCARL_DEBUG`, and package name to `@krisgray/opencarl` where present. Do not add new sections or examples; keep changes limited to rename replacements.
  </action>
  <verify>
    <automated>rg -n "CARL|\\*carl|/carl|\\.carl|CARL_DEBUG|opencode-carl" tests/fixtures/README.md || true</automated>
  </verify>
  <done>`tests/fixtures/README.md` contains only OpenCARL naming in existing examples and no legacy CARL terms.</done>
</task>

</tasks>

<verification>
rg -n "CARL|\\*carl|/carl|\\.carl|CARL_DEBUG|opencode-carl" tests/fixtures/README.md || true
</verification>

<success_criteria>
- The fixture README includes a visible `*opencarl` command example.
- Phase 16 verification gap for DOCS-05 is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/16-documentation-rebranding/16-04-SUMMARY.md`
</output>
