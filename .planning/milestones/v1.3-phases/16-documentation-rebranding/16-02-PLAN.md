---
phase: 16-documentation-rebranding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - INSTALL.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "README uses OpenCARL branding with updated commands, paths, and package name."
    - "INSTALL instructions use .opencarl/ and *opencarl consistently."
  artifacts:
    - path: "README.md"
      provides: "OpenCARL overview and usage"
      contains: "OpenCARL"
    - path: "INSTALL.md"
      provides: "OpenCARL installation steps"
      contains: "*opencarl"
  key_links:
    - from: "README.md"
      to: "resources/docs/OPENCARL-DOCS.md"
      via: "Markdown link"
      pattern: "resources/docs/OPENCARL-DOCS\\.md"
---

<objective>
Rebrand README and INSTALL documentation to OpenCARL naming conventions.

Purpose: Ensure the primary user-facing docs match the current command, path, and package naming.
Output: Updated README.md and INSTALL.md with consistent OpenCARL examples.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation-rebranding/16-CONTEXT.md
@.planning/phases/16-documentation-rebranding/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>task 1: update README.md branding and examples</name>
  <files>README.md</files>
  <action>
Replace CARL branding in prose, headings, tables, and code blocks with OpenCARL. Update command examples to `*opencarl` and `/opencarl`, path examples to `.opencarl/` and `.opencarl-template/`, environment examples to `OPENCARL_DEBUG`, and package name to `@krisgray/opencarl`. Update links to point at `resources/docs/OPENCARL-DOCS.md` where relevant. Make no other copy edits beyond minimal rename adjustments.
  </action>
  <verify>
    <automated>rg -n "CARL|\\*carl|/carl|\\.carl|CARL_DEBUG|opencode-carl" README.md || true</automated>
  </verify>
  <done>README.md contains only OpenCARL branding and updated examples.</done>
</task>

<task type="auto">
  <name>task 2: update INSTALL.md naming conventions</name>
  <files>INSTALL.md</files>
  <action>
Replace CARL references in INSTALL.md with OpenCARL naming. Ensure all commands use `*opencarl` or `/opencarl`, all paths use `.opencarl/` and `.opencarl-template/`, all env var examples use `OPENCARL_DEBUG`, and package references use `@krisgray/opencarl`. Keep edits limited to rename replacements.
  </action>
  <verify>
    <automated>rg -n "CARL|\\*carl|/carl|\\.carl|CARL_DEBUG|opencode-carl" INSTALL.md || true</automated>
  </verify>
  <done>INSTALL.md uses OpenCARL naming conventions throughout.</done>
</task>

</tasks>

<verification>
rg -n "CARL|\\*carl|/carl|\\.carl|CARL_DEBUG|opencode-carl" README.md INSTALL.md || true
</verification>

<success_criteria>
- README.md and INSTALL.md show OpenCARL branding in prose, commands, and paths.
- Links referencing docs point to resources/docs/OPENCARL-DOCS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/16-documentation-rebranding/16-02-SUMMARY.md`.
</output>
