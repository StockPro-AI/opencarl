---
phase: 14-command-rebranding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/opencarl/command-parity.ts]
autonomous: true
requirements: [CMND-01]

must_haves:
  truths:
    - "User can type *opencarl to trigger OpenCARL command mode"
    - "*opencarl token is normalized to OPENCARL for matching"
  artifacts:
    - path: "src/opencarl/command-parity.ts"
      provides: "Star command token matching"
      contains: 'token === "OPENCARL"'
  key_links:
    - from: "src/opencarl/command-parity.ts:172"
      to: "STAR_COMMAND_PATTERN"
      via: "token comparison"
      pattern: 'token === "OPENCARL"'
---

<objective>
Rename *carl star command trigger to *opencarl in the command detection logic.

Purpose: Users type *opencarl instead of *carl to trigger command-mode guidance.
Output: Updated token comparison in command-parity.ts
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-command-rebranding/14-RESEARCH.md

@src/opencarl/command-parity.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Update star command token from CARL to OPENCARL</name>
  <files>src/opencarl/command-parity.ts</files>
  <action>
Update the token comparison at line 172 from `token === "CARL"` to `token === "OPENCARL"`.

The change enables *opencarl (normalized to OPENCARL) to trigger the help/docs guidance injection logic. The STAR_COMMAND_PATTERN regex already supports any alphanumeric command name, so only the token comparison needs updating.

Do NOT modify:
- The regex pattern (STAR_COMMAND_PATTERN) - already supports *opencarl
- Any other token comparisons
- The help guidance generation logic
  </action>
  <verify>
    <automated>rg 'token === "OPENCARL"' src/opencarl/command-parity.ts && echo "PASS: OPENCARL token found"</automated>
    <manual>Verify line 172 shows `token === "OPENCARL"`</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>Line 172 in command-parity.ts uses `token === "OPENCARL"` instead of `token === "CARL"`</done>
</task>

</tasks>

<verification>
- [ ] Source code uses OPENCARL token for star command matching
- [ ] No references to `token === "CARL"` remain in command-parity.ts
- [ ] Regex pattern unchanged (already supports *opencarl)
</verification>

<success_criteria>
Star command *opencarl is recognized and triggers help/docs guidance injection.
</success_criteria>

<output>
After completion, create `.planning/phases/14-command-rebranding/14-01-SUMMARY.md`
</output>
