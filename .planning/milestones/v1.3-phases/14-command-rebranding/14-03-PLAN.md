---
phase: 14-command-rebranding
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - tests/javascript/unit/command-parity.test.ts
  - tests/javascript/integration/plugin-lifecycle.test.ts
  - tests/javascript/e2e/star-command.test.ts
  - scripts/carl-command-parity-smoke.ts
autonomous: true
requirements: [CMND-03]

must_haves:
  truths:
    - "Test fixtures use *opencarl instead of *carl in prompts"
    - "Test assertions expect OPENCARL token instead of CARL"
    - "Test fixtures use /opencarl instead of /carl in command strings"
  artifacts:
    - path: "tests/javascript/unit/command-parity.test.ts"
      provides: "Command parity unit tests"
      contains: "*opencarl"
    - path: "tests/javascript/integration/plugin-lifecycle.test.ts"
      provides: "Plugin lifecycle integration tests"
      contains: "/opencarl"
    - path: "tests/javascript/e2e/star-command.test.ts"
      provides: "Star command E2E tests"
      contains: "*opencarl"
  key_links:
    - from: "tests/javascript/unit/command-parity.test.ts"
      to: "src/opencarl/command-parity.ts"
      via: "test prompts and assertions"
      pattern: "\\*opencarl|OPENCARL"
---

<objective>
Update all test fixtures to use *opencarl and /opencarl instead of *carl and /carl.

Purpose: Tests verify the new command triggers work correctly.
Output: Updated test files with new command strings and assertions
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-command-rebranding/14-RESEARCH.md

@src/opencarl/command-parity.ts
@tests/javascript/unit/command-parity.test.ts
@tests/javascript/integration/plugin-lifecycle.test.ts
@tests/javascript/e2e/star-command.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Update command-parity.test.ts fixtures</name>
  <files>tests/javascript/unit/command-parity.test.ts</files>
  <action>
Update all occurrences of old command references:

1. Replace all `*carl` in test prompt strings with `*opencarl`
2. Replace all `CARL` in assertions with `OPENCARL`
3. Ensure test descriptions reference the new command name where applicable

The file has ~40+ occurrences across multiple test cases. Use sed or search-replace to update systematically.

Verify changes:
- All prompt strings use `*opencarl`
- All `toContain('CARL')` assertions now use `toContain('OPENCARL')`
- All `toContain('carl')` assertions now use `toContain('opencarl')` (lowercase)
  </action>
  <verify>
    <automated>npm test -- tests/javascript/unit/command-parity.test.ts --passWithNoTests 2>&1 | head -20</automated>
    <manual>Verify no `*carl` or `CARL` token references remain</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All test prompts use *opencarl and assertions expect OPENCARL token</done>
</task>

<task type="auto">
  <name>task 2: Update plugin-lifecycle.test.ts fixtures</name>
  <files>tests/javascript/integration/plugin-lifecycle.test.ts</files>
  <action>
Update all occurrences of `/carl` command references:

1. Replace all `/carl` in test input with `/opencarl`
2. Ensure command name assertions use the new name

The file has ~6 occurrences.
  </action>
  <verify>
    <automated>npm test -- tests/javascript/integration/plugin-lifecycle.test.ts --passWithNoTests 2>&1 | head -20</automated>
    <manual>Verify no `/carl` references remain</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All test inputs use /opencarl command</done>
</task>

<task type="auto">
  <name>task 3: Update star-command.test.ts fixtures</name>
  <files>tests/javascript/e2e/star-command.test.ts</files>
  <action>
Update all occurrences of `*carl` command references:

1. Replace all `*carl` in test prompts with `*opencarl`
2. Update any CARL token assertions to OPENCARL

The file has ~2 occurrences.
  </action>
  <verify>
    <automated>rg '\*carl|/carl' tests/javascript/e2e/star-command.test.ts || echo "PASS: No old command references"</automated>
    <manual>Verify all prompts use *opencarl</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All E2E test prompts use *opencarl</done>
</task>

<task type="auto">
  <name>task 4: Update carl-command-parity-smoke.ts script</name>
  <files>scripts/carl-command-parity-smoke.ts</files>
  <action>
Update the smoke test script:

1. Replace all `*carl` prompts with `*opencarl`
2. Replace all `CARL` token references with `OPENCARL`

The file has ~4 occurrences.
  </action>
  <verify>
    <automated>rg '\*carl' scripts/carl-command-parity-smoke.ts || echo "PASS: No *carl in smoke script"</automated>
    <manual>Verify script uses *opencarl throughout</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>Smoke test script uses *opencarl commands</done>
</task>

</tasks>

<verification>
- [ ] No `*carl` references in test files
- [ ] No `CARL` token assertions (use OPENCARL)
- [ ] No `/carl` command references in tests
- [ ] All unit tests pass
- [ ] All integration tests pass
</verification>

<success_criteria>
All test fixtures updated to use *opencarl and /opencarl, with OPENCARL token assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/14-command-rebranding/14-03-SUMMARY.md`
</output>
