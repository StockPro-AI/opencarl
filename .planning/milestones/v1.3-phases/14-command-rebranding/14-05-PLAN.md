---
phase: 14-command-rebranding
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/javascript/unit/command-parity.test.ts
  - tests/javascript/integration/plugin-lifecycle.test.ts
autonomous: true
requirements: [CMND-03]
gap_closure: true

must_haves:
  truths:
    - "Test fixtures use *opencarl instead of *carl in prompts"
    - "Test fixtures use /opencarl instead of /carl in command strings"
  artifacts:
    - path: "tests/javascript/unit/command-parity.test.ts"
      provides: "Unit tests for command parity"
      contains: "OPENCARL_RULE_"
    - path: "tests/javascript/integration/plugin-lifecycle.test.ts"
      provides: "Integration tests for plugin lifecycle"
      contains: "/opencarl"
  key_links:
    - from: "command-parity.test.ts"
      to: "OPENCARL token matching"
      via: "fixture strings"
      pattern: "OPENCARL_RULE_"
    - from: "plugin-lifecycle.test.ts"
      to: "opencarl command handling"
      via: "test command strings"
      pattern: "/opencarl"
---

<objective>
Fix incomplete test fixture rebranding identified in verification gaps.

Purpose: The 14-03 execution updated prompt strings but missed the actual fixture content strings inside tests, causing 7 test failures.
Output: All test fixtures use OPENCARL_RULE_ and /opencarl consistently.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-command-rebranding/14-03-SUMMARY.md
@.planning/phases/14-command-rebranding/14-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Fix CARL_RULE_ fixtures in command-parity.test.ts</name>
  <files>tests/javascript/unit/command-parity.test.ts</files>
  <action>
    Replace all occurrences of `CARL_RULE_` with `OPENCARL_RULE_` in test fixture strings.

    The verification found 9 occurrences of `CARL_RULE_` that should be `OPENCARL_RULE_`.

    Context: The command rule pattern `^([A-Z0-9]+)_RULE_(\d+)$` extracts the token from before `_RULE_`. When fixtures write `CARL_RULE_1=...`, the token extracted is `CARL`, which doesn't match the `OPENCARL` token check in the source code.

    Search pattern: `CARL_RULE_`
    Replace with: `OPENCARL_RULE_`

    This is purely a string replacement in test fixture data - no logic changes needed.
  </action>
  <verify>
    <automated>grep -c "CARL_RULE_" tests/javascript/unit/command-parity.test.ts | xargs -I {} sh -c 'test {} -eq 0 && echo "PASS: No CARL_RULE_ found" || (echo "FAIL: Found {} CARL_RULE_ occurrences" && exit 1)'</automated>
    <manual>Verify all fixture strings now use OPENCARL_RULE_ prefix</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Zero occurrences of CARL_RULE_ remain in command-parity.test.ts; all fixture strings use OPENCARL_RULE_</done>
</task>

<task type="auto">
  <name>task 2: Fix /carl commands in plugin-lifecycle.test.ts</name>
  <files>tests/javascript/integration/plugin-lifecycle.test.ts</files>
  <action>
    Replace all occurrences of `/carl` with `/opencarl` in test command strings and descriptions.

    The verification found 6 occurrences of `/carl` that should be `/opencarl`.

    Context: These tests verify plugin lifecycle commands but are checking the old `/carl` trigger instead of the new `/opencarl` trigger. While tests pass, they don't validate that the rebranding was successful.

    Search pattern: `/carl`
    Replace with: `/opencarl`

    Focus on:
    - Command strings in test cases
    - Test descriptions mentioning /carl
    - Expected command patterns in assertions

    This is purely a string replacement - no logic changes needed.
  </action>
  <verify>
    <automated>grep -c '"/carl' tests/javascript/integration/plugin-lifecycle.test.ts | xargs -I {} sh -c 'test {} -eq 0 && echo "PASS: No /carl found" || (echo "FAIL: Found {} /carl occurrences" && exit 1)'</automated>
    <manual>Verify all command strings now use /opencarl</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Zero occurrences of /carl remain in plugin-lifecycle.test.ts; all command strings use /opencarl</done>
</task>

<task type="auto">
  <name>task 3: Verify all tests pass with fixed fixtures</name>
  <files>tests/javascript/unit/command-parity.test.ts, tests/javascript/integration/plugin-lifecycle.test.ts</files>
  <action>
    Run the affected test files to verify the fixture fixes resolve the failures.

    Run:
    1. `npm test -- tests/javascript/unit/command-parity.test.ts` - Should pass all 29 tests (was 22 passed, 7 failed)
    2. `npm test -- tests/javascript/integration/plugin-lifecycle.test.ts` - Should pass all 13 tests

    If any tests fail, analyze the failure output and fix any remaining CARL/opencarl mismatches.
  </action>
  <verify>
    <automated>npm test -- tests/javascript/unit/command-parity.test.ts tests/javascript/integration/plugin-lifecycle.test.ts 2>&1 | grep -E "(passed|failed|PASS|FAIL)" | tail -5</automated>
    <manual>All tests in both files pass without failures</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>All tests in command-parity.test.ts and plugin-lifecycle.test.ts pass; 7 previously failing tests now pass</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `grep -r "CARL_RULE_" tests/javascript/unit/command-parity.test.ts` - should return no matches
2. Run `grep -r '"/carl' tests/javascript/integration/plugin-lifecycle.test.ts` - should return no matches
3. Run `npm test -- tests/javascript/unit/command-parity.test.ts tests/javascript/integration/plugin-lifecycle.test.ts` - all tests pass
</verification>

<success_criteria>
- [ ] All CARL_RULE_ occurrences replaced with OPENCARL_RULE_ in command-parity.test.ts
- [ ] All /carl occurrences replaced with /opencarl in plugin-lifecycle.test.ts
- [ ] All 29 tests in command-parity.test.ts pass (was 22 passed, 7 failed)
- [ ] All 13 tests in plugin-lifecycle.test.ts pass
- [ ] CMND-03 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/14-command-rebranding/14-05-SUMMARY.md`
</output>
