---
phase: 14-command-rebranding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/integration/plugin-hooks.ts]
autonomous: true
requirements: [CMND-02]

must_haves:
  truths:
    - "User can type /opencarl to trigger fallback command mode"
    - "/opencarl command is recorded as opencarl signal"
  artifacts:
    - path: "src/integration/plugin-hooks.ts"
      provides: "Slash command fallback handling"
      contains: 'commandName === "opencarl"'
  key_links:
    - from: "src/integration/plugin-hooks.ts:215"
      to: "recordCommandSignals"
      via: "command name check"
      pattern: 'commandName === "opencarl"'
---

<objective>
Rename /carl slash command fallback to /opencarl in the plugin hooks.

Purpose: Users type /opencarl instead of /carl to trigger fallback command-mode guidance.
Output: Updated command name comparison and signal recording in plugin-hooks.ts
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-command-rebranding/14-RESEARCH.md

@src/integration/plugin-hooks.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Update slash command from /carl to /opencarl</name>
  <files>src/integration/plugin-hooks.ts</files>
  <action>
Update the slash command fallback handling:

1. Line 215: Change `if (commandName === "carl")` to `if (commandName === "opencarl")`
2. Line 217: Change `recordCommandSignals(input.sessionID, ["carl"])` to `recordCommandSignals(input.sessionID, ["opencarl"])`

The comment on line 216 already references *opencarl (from Phase 12 rebranding), so the code logic now needs to match.

Do NOT modify:
- The /opencarl setup command handlers (lines 220-269)
- Any other command handling logic
  </action>
  <verify>
    <automated>rg 'commandName === "opencarl"' src/integration/plugin-hooks.ts && rg '\["opencarl"\]' src/integration/plugin-hooks.ts && echo "PASS: /opencarl fallback updated"</automated>
    <manual>Verify lines 215 and 217 use opencarl instead of carl</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>Lines 215 and 217 in plugin-hooks.ts use "opencarl" instead of "carl"</done>
</task>

</tasks>

<verification>
- [ ] Slash command /opencarl triggers fallback guidance
- [ ] Command signals recorded with "opencarl" token
- [ ] No references to `commandName === "carl"` remain
</verification>

<success_criteria>
Slash command /opencarl is recognized and triggers command-mode guidance recording.
</success_criteria>

<output>
After completion, create `.planning/phases/14-command-rebranding/14-02-SUMMARY.md`
</output>
