---
phase: 14-command-rebranding
plan: 06
type: gap_closure
wave: 3
depends_on: [14-01, 14-02, 14-03, 14-04, 14-05]
files_modified: [tests/javascript/e2e/star-command.test.ts]
autonomous: true
requirements: [CMND-03]
gap_closure: true
must_haves: true
---

<must_haves>
<truths>
- "E2E test file compiles without TypeScript errors"
- "All 13 E2E tests execute successfully"
- "Test fixtures use *opencarl commands consistently"
</truths>
<artifacts>
<path>tests/javascript/e2e/star-command.test.ts</path>
<provides>E2E star command tests with proper structure</provides>
<contains>opencarl status, opencarl list, opencarl toggle</contains>
</artifacts>
<key_links>
<from>tests/javascript/e2e/star-command.test.ts</from>
<to>Real OpenCode Docker container</to>
<via>E2E test execution</via>
<pattern>13 tests pass with *opencarl commands</pattern>
</key_links>
</must_haves>

<objective>
Fix structural issues in E2E test file preventing test execution and compilation.

Purpose: The file has duplicate test code, orphaned code blocks, wrong indentation, and brace mismatch causing TypeScript compilation errors and 0 tests run.

Output: Clean, compilable E2E test file with all 13 tests passing
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-command-rebranding/14-VERIFICATION.md
@tests/javascript/e2e/star-command.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: Remove orphaned test code block (lines 100-119)</name>
  <files>tests/javascript/e2e/star-command.test.ts</files>
  <action>
    Remove lines 100-119 which contain orphaned test code without proper `it()` wrapper.

    The orphaned code at lines 100-119 has:
    - No `it()` wrapper
    - No closing brace
    - Proper code structure but incorrectly positioned

    This orphaned code was likely created when describe block structure was changed without proper re-nesting.
  </action>
  <verify>
    <automated>sed -n '98,125p' tests/javascript/e2e/star-command.test.ts | head -3 | grep -q "describe('opencarl status'" && sed -n '98,125p' tests/javascript/e2e/star-command.test.ts | grep -q "it('should show active status" || (echo "FAIL: Orphaned code not properly removed" && exit 1)</automated>
    <manual>Verify describe('opencarl status' immediately followed by proper it() wrapper</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Orphaned test code block removed; describe('opencarl status' immediately followed by proper it() wrapper</done>
</task>

<task type="auto">
  <name>task 2: Remove duplicate test code block (lines 143-164)</name>
  <files>tests/javascript/e2e/star-command.test.ts</files>
  <action>
    Remove lines 143-164 which contain duplicate test code with old 'carl status' reference.

    The duplicate block contains:
    - Line 145: `carl status` reference (old command trigger)
    - Full test code structure that duplicates proper implementation
    - Causes TypeScript compilation errors

    This duplicate code was not removed during previous fixes and introduces errors at lines 208, 331-332.
  </action>
  <verify>
    <automated>grep -n "carl status" tests/javascript/e2e/star-command.test.ts | head -5 || echo "PASS: No carl status found"</automated>
    <manual>Verify duplicate test code block removed</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Duplicate test code block removed; no 'carl status' references remain in E2E test file</done>
</task>

<task type="auto">
  <name>task 3: Fix indentation of describe('opencarl list' at line 165</name>
  <files>tests/javascript/e2e/star-command.test.ts</files>
  <action>
    Fix indentation from 4 spaces to 6 spaces at line 165.

    The describe block at line 165 is currently:
    ```
        describe('opencarl list', () => {
    ```

    It should be:
    ```
          describe('opencarl list', () => {
    ```

    This fixes the wrong nesting level and resolves TypeScript compilation error at line 208.
  </action>
  <verify>
    <automated>sed -n '165p' tests/javascript/e2e/star-command.test.ts | sed 's/ //' | od -c | head -3 | grep -q "describe" || (sed -n '165p' tests/javascript/e2e/star-command.test.ts | head -c 10 | grep -q "  " && echo "PASS: Indentation fixed to 6 spaces") || (echo "FAIL: Indentation still wrong" && exit 1)</automated>
    <manual>Verify line 165 has 6 leading spaces</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>describe('opencarl list' block properly indented at 6 spaces</done>
</task>

<task type="auto">
  <name>task 4: Fix extra closing braces at lines 331-332</name>
  <files>tests/javascript/e2e/star-command.test.ts</files>
  <action>
    Fix extra closing braces at lines 331-332.

    The file currently has 2 closing braces at the end (lines 331-332), but should have only 1.

    Line 331: });
    Line 332: });  # <-- EXTRA

    Remove line 332 to balance the braces.
  </action>
  <verify>
    <automated>tail -5 tests/javascript/e2e/star-command.test.ts | grep -c "^});$" | xargs -I {} sh -c 'test {} -le 1 && echo "PASS: Proper brace count" || (echo "FAIL: Still has extra braces" && exit 1)'</automated>
    <manual>Verify file ends with single closing brace</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Extra closing brace removed; braces properly balanced</done>
</task>

<task type="auto">
  <name>task 5: Verify file compiles without errors</name>
  <files>tests/javascript/e2e/star-command.test.ts</files>
  <action>
    Run TypeScript compiler to verify file compiles without errors.

    Run:
    npx tsc --noEmit tests/javascript/e2e/star-command.test.ts

    Expected: No compilation errors
  </action>
  <verify>
    <automated>npx tsc --noEmit tests/javascript/e2e/star-command.test.ts 2>&1 | grep -E "(error TS|Found)" || echo "PASS: File compiles without errors"</automated>
    <manual>Review TypeScript compilation output</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>File compiles without TypeScript errors</done>
</task>

<task type="auto">
  <name>task 6: Verify all 13 E2E tests pass</name>
  <files>tests/javascript/e2e/star-command.test.ts</files>
  <action>
    Run E2E test suite to verify all tests execute successfully.

    Run:
    npm test -- tests/javascript/e2e/star-command.test.ts

    Expected:
    - 13 tests pass
    - 0 tests fail
    - No syntax errors
  </action>
  <verify>
    <automated>npm test -- tests/javascript/e2e/star-command.test.ts 2>&1 | grep -E "(PASS|FAIL|Tests)" | tail -10</automated>
    <manual>Verify all 13 tests pass</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All 13 E2E tests pass with 0 failures</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. TypeScript compilation passes: `npx tsc --noEmit tests/javascript/e2e/star-command.test.ts` (no errors)
2. No 'carl status' references remain: `grep -n "carl status" tests/javascript/e2e/star-command.test.ts`
3. Brace count correct: Count `{` and `}` characters, should be balanced
4. All tests pass: `npm test -- tests/javascript/e2e/star-command.test.ts` (13/13 pass)
5. No orphaned or duplicate code blocks
</verification>

<success_criteria>
- [ ] Orphaned test code block removed (lines 100-119)
- [ ] Duplicate test code block removed (lines 143-164)
- [ ] describe('opencarl list' indentation fixed to 6 spaces
- [ ] Extra closing braces removed (line 332)
- [ ] TypeScript compilation passes with no errors
- [ ] All 13 E2E tests pass (0 failures)
- [ ] No 'carl status' references remain in file
- [ ] Braces properly balanced throughout file
</success_criteria>

<output>
After completion, create `.planning/phases/14-command-rebranding/14-06-SUMMARY.md`
</output>
