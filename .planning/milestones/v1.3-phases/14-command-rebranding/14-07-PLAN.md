---
phase: 14-command-rebranding
plan: 07
type: gap_closure
wave: 4
depends_on: [14-06]
files_modified: []
autonomous: false
requirements: [CMND-03]
gap_closure: true
must_haves: true
---

<must_haves>
<truths>
- "Phase 14 Command Rebranding is fully complete"
- "All 4 requirements (CMND-01 through CMND-04) are satisfied"
- "All tests pass with new *opencarl and /opencarl commands"
</truths>
<artifacts>
<path>.planning/phases/14-command-rebranding/14-VERIFICATION.md</path>
<provides>Verification report showing all gaps closed</provides>
<contains>14-06 gap closure evidence</contains>
</artifacts>
<key_links>
<from>14-06 gap closure execution</from>
<to>CMND-03 requirement satisfaction</to>
<via>E2E test fix</via>
<pattern>13/13 tests pass</pattern>
</key_links>
</must_haves>

<objective>
Document gap closure and verify Phase 14 is complete.

Purpose: The gap closure process has fixed structural issues in E2E test file. This plan documents the closure and verifies all requirements are satisfied.

Output: Verification report showing all gaps closed and Phase 14 complete
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-command-rebranding/14-VERIFICATION.md
@.planning/phases/14-command-rebranding/14-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Verify all 4 Phase 14 requirements are satisfied</name>
  <files>
  .planning/phases/14-command-rebranding/14-VERIFICATION.md
  </files>
  <action>
    Verify all 4 Phase 14 requirements are met:

    **CMND-01**: `*carl` command trigger is renamed to `*opencarl`
    - Evidence: `src/opencarl/command-parity.ts:172` uses `token === "OPENCARL"`

    **CMND-02**: `/carl` fallback command is renamed to `/opencarl`
    - Evidence: `src/integration/plugin-hooks.ts:215,217` use "opencarl"

    **CMND-03**: All test fixtures with `*carl` or `/carl` strings are updated
    - Evidence: 14-05 fixed unit/integration tests, 14-06 fixed E2E tests
    - Evidence: All 13 E2E tests pass in star-command.test.ts

    **CMND-04**: Help text and command descriptions reference `*opencarl` and `/opencarl`
    - Evidence: `CARL-OVERVIEW.md` and `manager.md` updated

    Mark each requirement as SATISFIED in the verification report.
  </action>
  <verify>
    <automated>echo "Verifying all requirements satisfied:" && \
    echo "CMND-01: $(rg 'token === \"OPENCARL\"' src/opencarl/command-parity.ts | wc -l) matches" && \
    echo "CMND-02: $(rg 'commandName == \"opencarl\"|opencarl' src/integration/plugin-hooks.ts | wc -l) matches" && \
    echo "CMND-03: All test files pass: $(npm test -- tests/javascript/ 2>&1 | grep -c 'Tests.*passed' || echo 'Checking E2E')" && \
    echo "CMND-04: $(rg '\\*opencarl|/opencarl' resources/skills/carl-help/CARL-OVERVIEW.md | wc -l) references" && \
    echo "PASS: All 4 requirements verified"</automated>
    <manual>Review each requirement evidence in verification report</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All 4 Phase 14 requirements (CMND-01 through CMND-04) are satisfied</done>
</task>

<task type="auto">
  <name>task 2: Verify test suite passes completely</name>
  <files>
  tests/javascript/
  </files>
  <action>
    Run full test suite to verify no regressions and all tests pass:

    1. Run all tests: `npm test`
    2. Check test counts: All unit, integration, and E2E tests should pass
    3. Verify no 'carl' or 'CARL' references remain in test fixtures
    4. Verify 'opencarl' and 'OPENCARL' are used consistently

    Expected results:
    - All 29 unit tests in command-parity.test.ts pass
    - All 13 integration tests in plugin-lifecycle.test.ts pass
    - All 13 E2E tests in star-command.test.ts pass
    - Total: 55+ tests pass, 0 fail
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "(Test Suites|Tests.*passed|Tests.*failed)" | tail -5</automated>
    <manual>Review full test output</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Full test suite passes with 0 failures</done>
</task>

<task type="auto">
  <name>task 3: Update verification report with gap closure evidence</name>
  <files>
  .planning/phases/14-command-rebranding/14-VERIFICATION.md
  </files>
  <action>
    Update the verification report to reflect gap closure:

    1. Change status from `gaps_found` to `verified`
    2. Update score from `3/4` to `4/4` must-haves verified
    3. Document closure of gap #3: E2E test file structural issues
    4. Update artifact status for star-command.test.ts from FAILED to VERIFIED
    5. Add evidence: 13 E2E tests pass, no TypeScript errors, no 'carl status' references
  </action>
  <verify>
    <automated>grep -q "status: verified" .planning/phases/14-command-rebranding/14-VERIFICATION.md && \
    grep -q "4/4" .planning/phases/14-command-rebranding/14-VERIFICATION.md && \
    grep -q "CMND-03.*SATISFIED" .planning/phases/14-command-rebranding/14-VERIFICATION.md && \
    echo "PASS: Verification report updated"</automated>
    <manual>Review verification report content</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Verification report shows 4/4 requirements satisfied, status: verified</done>
</task>

<task type="auto">
  <name>task 4: Update STATE.md with Phase 14 completion</name>
  <files>
  .planning/STATE.md
  </files>
  <action>
    Update STATE.md to reflect Phase 14 completion:

    1. Change Phase 14 status from "complete — ready for verification" to "complete"
    2. Update progress: Phase 14: Command Rebranding → 5/5 plans complete
    3. Add SESSION LOG entry documenting gap closure completion
    4. Update total plans in phase to 5 (added 14-06 and 14-07)
  </action>
  <verify>
    <automated>grep -q "Phase 14: Command Rebranding.*5/5" .planning/STATE.md && \
    grep -q "14-06-PLAN.md" .planning/STATE.md && \
    echo "PASS: STATE.md updated"</automated>
    <manual>Review STATE.md changes</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>STATE.md reflects Phase 14 completion with all 5 plans done</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All 4 requirements (CMND-01 through CMND-04) are marked SATISFIED in verification report
2. Full test suite passes with 0 failures
3. Verification report status changed from gaps_found to verified
4. STATE.md updated to show Phase 14 completion
</verification>

<success_criteria>
- [ ] CMND-01: OPENCARL token matching verified
- [ ] CMND-02: opencarl command handling verified
- [ ] CMND-03: All test fixtures updated and all tests pass (55+ tests)
- [ ] CMND-04: Help text references new commands verified
- [ ] Verification report shows 4/4 requirements satisfied
- [ ] Full test suite passes with 0 failures
- [ ] STATE.md updated to show Phase 14 complete
</success_criteria>

<output>
After completion, create `.planning/phases/14-command-rebranding/14-07-SUMMARY.md`
</output>
