---
name: setup
description: Initialize OpenCARL. This command is handled by the plugin - no action needed.
---

The `/opencarl setup` command is executed automatically by the OpenCARL plugin. The setup results are logged to console.
